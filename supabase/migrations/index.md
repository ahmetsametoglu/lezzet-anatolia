# Migration günlüğü

Her migration'ın bir satırlık ne-yaptığı (WORKFLOW §2). Dosya adları bir süre sonra yetmez.

**Yalnız ileri (WORKFLOW §2) — canlıya çıktıktan SONRA:** uygulanmış dosya düzenlenmez, yeni numaralı
dosya eklenir. **Şu an greenfield** (prod yok, veri yok): şema kaynağı temiz kalsın diye kolonlar ek
migration yerine doğal tablosunda düzenlenir; değişiklikten sonra yerelde `pnpm db:refresh`. İlk canlı
dağıtımdan itibaren bu serbestlik biter.

| # | Dosya | Ne yapar |
| --- | --- | --- |
| 0001 | `0001_auth_user_profiles.sql` | Auth kimlik + kullanıcı profili tabloları; RLS deny-by-default (erişim sunucudan service_role ile). **+ `customer_phone` — kimlik anahtarı kendi kaydında** (04.10 · DOMAIN §10): `customer_phone(customer_id, phone, verified_at, last_seen_at, delivery_failed_at, retired_at)` + `customer_phone_active_key` (aktif satırlarda tekil); **`user_profiles_phone_key` KALDIRILDI.** **Kök:** o kolon iki işi birden yapıyordu — formdan yazılan *iletişim* numarası ve bul-veya-oluştur'un okuduğu *kimlik anahtarı*. Doğrulanmamış bir dize kimlik kurabildiği için **önceden sahiplenme** mümkündü: birinin hesap kartına yazdığı numara, gerçek sahibi WhatsApp'tan yazdığında onun konuşmasını/siparişini/puanını yabancı hesapta gösteriyordu (en sık hâli kaza — eşin numarası, tuşlama hatası, aile telefonu). **Satır VARSA doğrulanmıştır** (`verified_at not null`): nullable bırakmak aynı hatayı bir tablo ötede tekrarlardı. Bugünkü tek yazıcı imzalı Meta webhook'u (15.7) — DOMAIN §10'un *"bu güvenin dayanağı webhook imzasıdır"* cümlesi buranın temeli. **Tekillik `retired_at is null` süzgeçli:** devredilmiş hat emekliye ayrılır (satır SİLİNMEZ, geçmiş durur) ve numara yeni sahibine açılır — kolon modelinde bu ancak eski bağı `null`'layarak, yani geçmişi silerek yapılabiliyordu. `retired_at`ın bugün YAZICISI yok ve kolon yapısaldır (indeks onsuz kurulamaz); emekliye ayıran zaman aşımı kararı 04.10'un devamında, gerekçe kolonu da o gün yazıcısıyla birlikte doğar. **`delivery_failed_at` + `mark_customer_phone_delivery` (25.08):** taşıyıcının `failed` beyanı kimlik şüphesinin ERKEN tetiği — tahmin değil beyandır (numara kapanmış ya da bizi engellemiş), 3 aylık sessizliği beklemenin anlamı yok. Tek deyimlik RPC: `failed` damgalar, başarılı teslim SİLER (sonraki teslim öncekini gerçekten çürütür; bayat damga her dönüşte gereksiz soru doğururdu). Numarayla çağrılıyor çünkü taşıyıcının elinde bizim satır kimliğimiz yok, `recipient_id` var. **Ters yön bilerek açık:** bir hesabın kaç numara taşıyacağı kullanıcı kararına bırakıldı; yapı iki seçeneği de taşıyor. **Neden ayrı migration DEĞİL:** tablo bir tur `0049` olarak yazıldı ve koşmadı — `0040`ın `preview_customer_merge`i (SQL gövdeli, oluşturulurken doğrulanan) bu tabloyu okuyor ve 0040 önce koşuyor (ölçüldü, `db:refresh` orada kesildi). Yeri zaten burasıydı |
| 0002 | `0002_auth_user_profile_trigger.sql` | `auth.users` → profil satırı trigger'ı (kayıt olunca profil doğar) |
| 0003 | `0003_email_verification_otp.sql` | E-posta doğrulama OTP tablosu (Supabase mail göndermez; Resend ile OTP akışı) |
| 0004 | `0004_catalog_category_collection.sql` | `category` + `collection` (düz gruplama, `LocalizedText` ad, benzersiz slug, sort_order, is_active); koleksiyonda ayrıca `description` + `image_key` — paylaşılabilir vitrin sayfası / OG kartı. **+ `product_family`** (05.15): çeşit ekseni — üye = ürün, ad yalnız operasyona görünür (tek dilli) |
| 0005 | `0005_catalog_product.sql` | `product` + `product_variant` + `product_collections` çoklu bağı (`position` ile koleksiyon içi kürasyon sırası + sıralı okuma indeksi); alerjen/tarih-tipi enum'ları. **+ `price`** (eskiden ayrı dosyaydı): varyant fiyatı — kanal listesi + müşteriye özel + tarihli geçerlilik, `channel`/`currency` enum'ları, çözüm indeksi. **+ aile üyeliği** (05.15): `family_id` · `family_label` (üç dilli kart etiketi, aile doluyken ZORUNLU — kısıt) · `family_position` (aile içi sıra; `sort_order` katalog sırasıdır, karışmaz). **09.08:** `product_variant.pieces_count` (05.14) — paket içi adet ("12'li baklava"). Kolonu olmadığı için üreteç adedi adın İÇİNDE bırakıyordu ve slug ayrıştığı için **tek baklava dört ayrı ürüne bölünüyordu** (ölçüldü: 10 kayıt → 2 ürün; `05.15`'in "benzer ürünlerde aynı baklavanın üç gramajı" açığının kökü de buydu). Gramajın YERİNE değil YANINA: 72'lik kutu hem 72 adet hem 2500 g'dır, ikisi ayrı soruya cevap verir. `null` = bildirilmemiş (dökme), sıfır DEĞİL |
| 0006 | `0006_stock.sql` | Stok partisi + rezervasyon (06.2) — temel denklem `KULLANILABİLİR = FİİLİ − AKTİF REZERVASYON`; "ayrılmış toplam" hiçbir yerde SAKLANMAZ, `reservation` satırlarından türetilir. `available_stock` görünümü. **+ `temperature_log`** (eskiden ayrı dosyaydı): hijyen denetiminin ilk istediği veri, sensör yok — basit tutulur ki gerçekten girilsin |
| 0007 | `0007_reserve_stock.sql` | Atomik rezervasyon RPC'si (06.3) — iki müşteri aynı anda son birimi isteyebilir; "önce oku sonra yaz" arasına başkası girer ve ikisi de ayırır (aşırı-satış). Kontrol + yazım TEK transaction |
| 0008 | `0008_observability.sql` | **Gözlemleme ailesi** (18.5) — üç tablo bir arada: `job_run` (zamanlanmış iş izi, tek satır/iş, süreç belleğinde tutulmaz); `error_log` + atomik `capture_error` RPC'si (aynı parmak izli AKTİF hata tek satırda birikir, "çözüldü"den sonra tekrar gelen YENİ satır açar → regresyon görünür; kısmi unique `where resolved_at is null` bunu zorlar); `system_health_snapshot` (cron `*/2 dk`, `status` eşiklerden TÜRETİLİR, saklama 14 gün). Üçü eskiden üç ayrı dosyaydı |
| 0009 | `0009_stock_adjustment.sql` | İmha/fire/sayım farkı (06.6) — stok azalışının SATIŞ DIŞI her sebebi; "bu üründen yılda ne kadar çöpe attım" sorusunun tek cevabı. RPC: kayıt + fiili düşüm bölünemez. `stock_adjustment_detail` görünümü aramayı taşır (09.18). **+ imha/sayım OLAY referansı** (eskiden ayrı dosyaydı, 10.5): numara SATIR başına değil OLAY başına — bir imhada üç parti giderse üç numara vermek, eşleştirilmek istenen kâğıdı üçe bölerdi |
| 0010 | `0010_supply.sql` | Tedarik zinciri (06.8–06.10) — tedarikçi, ürün-kod eşlemesi, tedarik siparişi, mal kabul. İlke: **sistem önerir, siparişi insan verir**; sistem tedarikçiye hiçbir şey GÖNDERMEZ. `purchase_order.reference_no` gönderimde doğar (06.12) |
| 0011 | `0011_customer_fields.sql` | Müşterinin ticari alanları + adres (04) — **ayrı `customer` tablosu YOKTUR**: müşteri bir ROLDÜR, kimlik tek tabloda. Vade/kapıda ödeme/kişisel indirim alanları  **+ WhatsApp bağlama jetonu** (04.10): `wa_link_token` + `wa_link_expires_at` + tekil kısmi indeks + çift kısıtı (biri olmadan öteki yazılamaz). **Neden gerekiyor:** gelen mesaj numaranın zilyetliğini kanıtlar ama hangi HESAP olduğunu söylemez — web'den kaydolmuş müşteri kendiliğinden yazdığında yeni bir taslak doğardı. Jeton, giriş yapmış müşteriye üretilir ve ÖNCEDEN YAZILI mesajın içine konur; webhook ikisini birden görür. **Biz mesaj GÖNDERMEYİZ:** akışı müşteri başlatır (`wa.me`), yani ücretli `authentication` şablonu ve Meta şablon onayı gerekmez — üstelik gelen mesaj 24 saatlik ücretsiz pencereyi de açar. **6 haneli güvenlik koduyla KARIŞTIRILMAZ:** DOMAIN §10'un *"koddan kimliğe gidilmez"* kuralı o kısa ÇAPA kodu içindir ve gücünü numaraya bağlı olmaktan alır; burada sorgunun yönü zorunlu olarak jetondan kimliğedir (mesaj gelene kadar kimin yazdığı bilinmez), o yüzden güvenlik ENTROPİDEN gelir — `readableCode(12)` ≈ 60 bit + 15 dk ömür + tek kullanım. Tahminle bulunan jeton, bulanın numarasını başkasının hesabına yazdırırdı (hesap devralma). **Süre ZORUNLU**, `notification_token`un tersi: o jeton yalnız tercihleri açar ve yıllar yaşamalı, bu bir KİMLİK anahtarı yazdırır  **+ KİMLİK ÇAPASI** (04.10 · DOMAIN §10): `email_anchored_at` · `anchor_email`(+`_at`) · `security_code_hash` · `security_code_attempts` + iki çift kısıtı + `user_profiles_single_anchor`. Numaranın kanıtlanması *"bu hat BUGÜN bu kişide"* der; çapa *"bu numaranın GEÇMİŞİ kimin"* sorusunu cevaplar — **devredilmiş hattın yeni sahibi hattı da gelen kodu da meşru olarak alır**, OTP bunu çözmez; çözen tek şey şüphe doğmadan ÖNCE kurulmuş bir sırdır. İki çapa iki AYRI kitle: e-posta (kod e-postaya, cevap WhatsApp'tan — kanıtın gücü çaprazlıktan) ve 6 hane (e-posta istemeyene). **Yedek değiller:** e-posta kanıtlanınca kod silinir ve kısıt bunu ZORLAR — ihlali sessiz olurdu, iki anahtar taşıyan kayıt hata vermez yalnız sızacak yüzeyi ikiye katlar. **`email_anchored_at` ayrı bir damga çünkü `email is not null` yetmiyor:** o kolon elle işlenen DM'de operatör klavyesinden de dolabiliyor — telefonda yaptığımız "verilen ≠ kanıtlanan" ayrımının aynısı. **Bekleyen adres SATIRDA:** doğrulama numaradan kimliğe, kimlikten bekleyen adrese gider; adres cevabın içinden okunsaydı kodu ele geçiren onu istediği adresle eşleştirebilirdi (`koddan kimliğe gidilmez` kuralının ikinci yüzü). Kod özetlenerek saklanır — canlı DB'ye yazana karşı değil, **yedek sızarsa** (numara, kod) listesi çıkmasın diye  **+ BEKLEYEN KİMLİK SORUSU** (04.10, 25.08): `challenge_reason` + `challenge_raised_at` + çift kısıtı + değer kısıtı (`silence`/`delivery_failed`). **Neden türetilemiyor:** tetiğin ölçütü `customer_phone.last_seen_at` ama o damga, soruyu doğuran mesajın KENDİSİ tarafından tazeleniyor — sonradan bakan kod üç aylık boşluğu sıfır saniye görür. Karar, boşluğun hâlâ görülebildiği tek anda (kanıt tazelenmeden hemen önce) verilip buraya yazılıyor. Saklanmasaydı dönen yabancının iki kez "merhaba" yazması soruyu buharlaştırırdı. **Numarada değil PROFİLDE:** kapının koruduğu şey kimliktir ve cevabı da kimliğin çapasıdır; numaraya yazsaydık iki numaralı müşteride "birinden sorulmuş, ötekinden cevaplanmış" hâli doğardı |
| 0012 | `0012_order.sql` | Sipariş omurgası (07.6) — sipariş, kalemleri, kalem-parti eşlemesi, durum geçiş kaydı. `order_counts` RPC'si sayaçları listeyle AYNI süzgeçten hesaplar. **+ `cart`** (eskiden ayrı dosyaydı, 07.1): sepet SUNUCUDA kalıcı; sepette hold YOK — stok ayrılmaz, fiyat sabitlenmez |
| 0013 | `0013_settings.sql` | İşletme ayarı (02.6/02.7) — kesim saati, minimum sepet, kapıda ödeme tavanı env'e/koda gömülmez; işin sahibinin kararıdır ve dağıtım beklemeden değişmeli. Kapsamlı çözüm (bölge → kanal → ülke → global) |
| 0014 | `0014_delivery_zone.sql` | Teslimat bölgesi (07.2) — **rota-içi/dışı SAKLANMAZ, türetilir**: adresin posta kodu aktif bir bölgeye düşüyorsa rota içi. Sınır ötesi posta kodları (ADR-002) |
| 0015 | `0015_record_preparation.sql` | Hazırlık onayı (06.5'in yazım yarısı) — depo ekranı FEFO'ya göre partiyi ÖNERİR, depocu sapabilir; yazılan şey onun ONAYLADIĞI gerçektir. Kalem-parti eşlemesi + stok düşümü bölünemez |
| 0016 | `0016_deliver_order.sql` | Teslim ve kapanış (07.7) — İKİ AYRI AN, iki ayrı fonksiyon: teslim (fiziksel) ve kapanış (mali). Ayrım `DOMAIN §12`'den geliyor |
| 0017 | `0017_quick_sale.sql` | Hızlı satış (07.10) — tam yol yedi durumdan geçer, kapı önü BİR adımdır (`draft → completed`). Müşteri karşıda, ara durumların anlamı yok |
| 0018 | `0018_money.sql` | **Para ailesi** (12) — dört dosya bir arada: hesaplar + hareketler (12.1, TEK MANTIK: para bir hesapta durur, hareketlerle girer/çıkar; kasa ile banka aynı mekanizma, bakiye türetilir); siparişin para bağları (12.2, `amount_collected`/`amount_refunded` bir CACHE'tir, kaynağı hareketlerdir); muhasebe export zemini (12.7, `order_sale` — "hangi siparişler" değil "hangi SATIŞLAR"); banka ekstresi import'u (12.4, dosya bir GERÇEK KAYNAĞIDIR + profil sütun eşlemesi). Dördü eskiden dört ayrı dosyaydı |
| 0019 | `0019_product_counts.sql` | Ürün ekranının başlık sayaçları — TEK okuma (05.12): önce dört ayrı istek gidiyordu (üç `HEAD` sayım + kategori) |
| 0020 | `0020_order_return.sql` | Kısmi karşılama (07.8) + iptal/iade (07.9) — ikisi de aynı soruyu sorar: mal gitmediyse ya da geri geldiyse fiziksel gerçek nasıl düzeltilir |
| 0021 | `0021_bundle_read.sql` | Paket listesi okuma fonksiyonu (05.5) — okumada RPC eşiğinin üç koşulu birlikte sağlanıyor |
| 0022 | `0022_webhook_event.sql` | Dış sağlayıcı olayları (07.5) — ödeme sağlayıcısı aynı olayı BİRDEN ÇOK KEZ gönderir; bu bir arıza değil sözleşmenin kendisi. Olay kimliği tekil, ikinci gelen sessizce atlanır |
| 0023 | `0023_notices.sql` | **Yer/talep bildirimleri ailesi** — üç tablo bir arada, ve **birleştirilmezler**, yalnız aynı dosyada dururlar: `postal_code_demand` (anonim toplu sayaç, kod → talep adedi + atomik RPC; ziyaretçi kimliği/IP/e-posta TUTULMAZ, tablo buna yer bırakmaz) · `zone_notice` ("bölgeye gelince haber ver", `notified_at` ile **tek** hatırlatma sözü) · `variant_stock_notice` (eskiden ayrı dosyaydı: "geliyoruz ama bu ürün burada yok" — anahtar YER (`ülke, kod`), depo değil; bölge başka depoya bağlansa da söz ayakta kalır). İlk ikisi anonim sayaç ↔ kişisel söz ayrımını taşır; üçüncüsü ürün eksenlidir. **04.08:** `zone_notice` `locale` kolonu kazandı (14.10) — kayıt hesapsız bırakılabildiği için haber gönderilirken dili çözecek profil çoğu zaman yoktur; kaydedilmeseydi tahminle gönderilir ve Alman müşteri Fransızca haber okurdu (`null` = bilinmiyor). **09.08 (21.16):** `zone_notice` üç kolon daha kazandı — `country` · `place_name` · `source`. Mobil şerit bölge dışı müşterinin "buraya gelin" cevabı için ayrı bir `delivery_interest` tablosu istemişti; **açılmadı** (`CLAUDE §1`): iki tablo da aynı soruya cevap verir ve haber işi ikisini birden okumak zorunda kalırdı — o gün biri unutulur, o listedeki müşteri hiç haber almaz. `country` bir kolaylık değil DÜZELTME: kayıt yalnız kodu taşırken haber işi iki ülkeyi de deneyip *"biri tutarsa kapsanmış say"* diyordu ve ölçüldü (09.08) — 16 878 satırlık kod tablosundaki **610 kod iki ülkeye birden çözülüyor**, yani Fransa'da açılan bölge aynı kodu yazmış Alman müşteriye gidebilir ve damga yazıldığı için bir daha düzeltilemezdi (küçük kardeşi `variant_stock_notice` bu dersi 19.8'de almıştı). Tekillik indeksi de ülkeyi aldı |
| 0024 | `0024_discount.sql` | `discount` — kupon ve otomatik kampanya TEK varlıkta (ayrımları yalnız tetik); alanlar motorun `DiscountRule` sözleşmesiyle birebir, kısıtlar tutarsız kuralı DB'de reddeder (kodsuz kupon, hedefsiz kapsam, ters tarih, %100 üstü). `discount_use` kullanım KAYDI — sayaç tutulmaz, sayım ondan türetilir |
| 0025 | ~~`0025_courier_day_close.sql`~~ | **KALDIRILDI (18.08)** — kurye×gün kapanışı SEFER eksenine indi (kullanıcı kararı: "fark hangi seferde doğdu" cevaplanabilmeli); halefi `0046_delivery_run.sql`teki `delivery_run_close` + `delivery_run_collection` + `close_delivery_run`. Dosya silindi çünkü kapanış artık `delivery_run`a FK veriyor ve o tablo 0045'teki `vehicle`a bağlı — sıra zorunlu, yerinde revizyon imkânsızdı (greenfield: yama migration'ı yazılmaz) |
| 0026 | `0026_ticket.sql` | Müşteri talebi/şikâyeti (16.1) — **karmaşık ticket sistemi DEĞİL**: atama, öncelik matrisi, SLA sayacı yok; üç durumlu sade döngü (`open → in_progress → resolved`, yeniden açılabilir). Ölçüt: müşteri kolay iletsin, biz siparişe ve ürüne bağlı net veri görelim |
| 0027 | `0027_product_feedback.sql` | Ürün geri bildirimi + ürün skoru (17.1, 17.3) — yıldız · yazılı yorum · beğen/geç TEK varlıkta; ayrımları biçimden ibaret, müşteri/ürün/tekillik/puan/skor katkısı üçünde de aynı. `discount`'ın kupon+kampanyayı tek tabloda tutmasıyla aynı gerekçe |
| 0028 | `0028_points.sql` | Puan defteri (17.4) — **ledger, sayaç değil**: bakiye Σ `points`, hiçbir yerde saklanmaz. `balance` kolonu olsaydı düzeltmeyi unutan tek bir yol bakiyeyi kalıcı yanlış gösterirdi. İstismar tavanı defterin kendisinde |
| 0029 | `0029_feedback_request.sql` | Alım-sonrası değerlendirme daveti (17.2) — teslimden ~10 gün sonra giden kişisel bağlantı; **token oturum yerine geçer** (araya giriş ekranı akışı kırar). `feedback_due_order` + `feedback_request_progress` görünümleri; ilerleme türetilir, saklanmaz |
| 0030 | `0030_create_order.sql` | `create_order` RPC'si — başlık + kalemler + indirim kullanımı TEK transaction'da (kolon listesi katalogdan türer); `order_discount_balance` ertelenmiş kısıtı: başlıktaki indirim kalem paylarının toplamına eşit olmak zorunda |
| 0031 | `0031_warehouse.sql` | **Depo ağı** — `warehouse` (+ülke başına tek aktif kargo deposu kısmi unique), depo bazlı eşik, `warehouse_transfer(_line)`; tüm `warehouse_id` FK'lerinin bağlanması; `available_stock` (depo grain) + `available_stock_total` + `purchase_order_progress`; sipariş↔parti↔rezervasyon depo değişmezleri (ertelenmiş kısıtlar); `dispatch_transfer`/`receive_transfer` |
| 0032 | `0032_product_listing.sql` | Katalog fiyat sıralaması — `variant_effective_price` + `product_listing`. Depo boyutu alınca yeniden yazıldı: her aktif depo için satır + **yeri bilinmeyen okuma için `warehouse_id is null` satırı** (liste fiyatı + `has_near_expiry_offer` bayrağı, teklif tutarı gizli) |
| 0033 | `0033_postal_code_place.sql` | **Posta kodu referansı — ŞEMA** (elle bakılır). Ülke müşteriye SORULMAZ, koddan türer (sürtünme + vergi: serbest seçilen ülke KDV'yi etkilerdi). `postal_code` indeksi `text_pattern_ops` sınıfıyla: önek araması (`like '672%'`) varsayılan sınıfta tabloyu geziyordu — 36,9 ms → 0,11 ms (19.19) |
| 0034 | `0034_postal_code_place_data.sql` | **Posta kodu referansı — VERİ** (ÜRETİLMİŞ, elle düzenlenmez; GeoNames FR+DE 16.878 satır, CC-BY). Veri tanımın parçasıdır: tablo boşken her kod "tanınmadı"ya düşer, ülke çözülemez. Üreteç `scripts/build-postal-codes.mjs` (`pnpm postal:build`) ve artık YALNIZ bu dosyayı yazar |
| 0035 | `0035_analytics.sql` | **Analitik olay defteri** (13.1) — kurallar `docs/architecture/ANALYTICS.md`'de. Üç tablo: `analytics_event` (ham iz, **kimlik kolonu YOK**, aylık bölümlenmiş, 25 ay) · `analytics_session` (oturumun UTM künyesi) · `analytics_daily` (ekranların okuduğu özet, 24'lük saat dizisi + **terk sebebi boyutu**). Saklama **bölüm düşürerek** işler, satır silerek değil. Özetin tekil anahtarı `nulls not distinct` — `null` boyutlu satır yoksa sessizce çoğalırdı |
| 0036 | `0036_analytics_signals.sql` | **Analitik sinyal özetleri + rapor okumaları** (13.2 · 13.4 · 13.5). Üç özet tablosu: `analytics_daily_product` (gün × ürün — ilgi/dönüşüm, vitrin seçkisi de bunu okur) · `analytics_daily_search` (gün × terim × sıfır-sonuç kovası, **25 ay**) · `analytics_daily_source` (gün × kaynak × kampanya; doğrudan trafik `null` kovasında — sol birleşim). Dört RPC: dönem sinyalleri (`analytics_product_signals` · `analytics_search_signals`), **kampanya cirosu ilk-temas atfıyla** (`analytics_campaign_revenue`) ve müşteri segmentleri (`analytics_customer_segments` + `analytics_segment_members`). Segment SAKLANMAZ, türetilir; eşikler parametrik. `purge_analytics_before` oturum künyesini ve arama özetini ham defterle aynı 25 ayda süpürür. **`analytics_order_base` görünümü "hangi sipariş ciro sayılır"ı TEK yerde tanımlar** (taslak/iptal/iade dışarıda) — üç okuma da onu kullanır, üç kez yazılsaydı biri iadeyi düşer öteki düşmezdi. Ayrıca `analytics_order_revenue` (dönem cirosu gün × kanal, süzgeç SİPARİŞ tarihinde) ve `analytics_postal_code_orders` (talep sayacının karşı ucu — kullanıcının "çok soruluyor az alınıyor" sorusu) |
| 0037 | `0037_account_erasure.sql` | **GDPR silme: hesabın anonimleştirilmesi** (`anonymize_customer`). "Silme" bir `DELETE` DEĞİL — `order` profile `restrict` ile bağlı ve fatura kaydı yasal olarak duruyor; yapılan şey kimliği boşaltmak. Üç kova tek yerde: **silinir** (adres · talep yazışması · bildirim isteği · puan geçmişi · kişiye özel fiyat), **kimliksizleşir** (ürün geri bildirimi — puan kalır, yazı gider: satırı silmek başkalarının gördüğü ürün puanını geriye dönük değiştirirdi), **kalır** (sipariş · para hareketi · `address_snapshot`, çünkü Fransız hukuku faturanın ad-adres içermesini zorunlu kılar). İdempotent; **personelde hata fırlatır** (istihdam kaydı müşteri talebiyle silinmez). Damga `user_profiles.anonymized_at` — bayrak değil tarih |
| 0038 | `0038_recipe.sql` | **Tarifler — "Sofradan Fikirler"** (05.16). İki tablo: `recipe` (slug tek, dil-bağımsız · yedi çok dilli metin alanı · ImageMeta · editoryal `sort_order`) ve `recipe_item` (bağ VARYANTA — sepet varyantla çalışır; `product_id` de tutmak bir gün ayrışan iki gerçek demekti). **Üç dil dolmadan yayın yok ve kural VERİDE** (`recipe_publish_requires_all_locales` + `has_all_locales(jsonb)`): boş dize dolu sayılmaz, `is_active` varsayılanı bu yüzden `false` — `true` olsaydı tek dille açılan her tarif kısıtta patlar, operatör tarifi hiç oluşturamazdı. Yan kazancı dil yedek zincirinin ortadan kalkması: yayındaki tarifte eksik dil yok, yani Fransız müşteriye Türkçe hazırlanış adımı düşmez. **Hesaplanmayan alan tip taşımaz:** `duration`/`serves` metindir ("3–4 kişilik" bir ARALIKTIR), sayı kalan tek alan `qty`. Malzeme toplamı KOLON DEĞİL (fiyat personaya ve depoya bağlı; saklanan toplam ilk gün yalan söyler). `variant_id` `restrict` — tarifte duran varyant silinemez; aynı varyant bir tarifte iki kez yazılamaz (adet artırmak `qty`'nin işi) |
| 0039 | `0039_conversation.sql` | **WhatsApp konuşma zemini** (15.1/15.2). `conversation` (kimlik bağı · opt-in · 24s servis penceresi · son hareket) + `message` (yön · tür · jsonb gövde). **Bir kişi bir konuşma:** tekillik `(source, external_ref)` — WhatsApp'ta thread yoktur, aynı numaradan gelen her mesaj aynı sohbetin devamıdır; indeks olmasaydı ikinci mesaj yeni satır açar, geçmiş ikiye bölünürdü. `customer_id` **nullable** ve öyle kalmalı: adım 2'de webhook mesajı önce yazar kimliği sonra çözer — kimlik çözülemediği için mesajın kaybolduğu bir yol olamaz. İki RPC: `open_conversation` (tek deyimlik upsert — oku-sonra-yaz yarışırdı; `coalesce` ile mevcut müşteri bağı EZİLMEZ, yeniden bağlama bir birleştirme kararıdır ve insana aittir) ve `record_message` (mesaj + konuşma damgaları tek turda). **24 saat SQL'de DEĞİL:** pencere bitişini motor hesaplar (`serviceWindowExpiry`), RPC yalnız taşır; `p_window_expires_at` null ise pencereye dokunulmaz — giden mesaj pencereyi açmaz, açsaydı ücretsiz süreyi kendimize uzatmış olurduk. `ticket.conversation_id` FK'si nihayet bağlandı ve **`no action`**: `set null` seçilseydi konuşma silmek `ticket_source_link` kısıtına çarpar, hata adı hiç geçmeyen bir talepte görünürdü; `restrict` ise anında baktığı için hesap silinirken yarışan iki cascade zincirinin sırasına duyarlı olurdu. **`template_category`** (marketing/utility/authentication) mesajın kendisinde saklanıyor: fiyatı belirleyen alan bu, `kind` değil — ve defter olanı yazar, kategori Meta tarafında yeniden sınıflandırılınca geçmiş faturamız değişmesin diye şablon adına bakıp türetilmiyor. Kolon defterle birlikte doğdu: yazılırken atlanan boyut geriye dönük doldurulamaz. **21.08 (15.15, ADR-006):** kaynak enum'u üç kanala genişledi (`whatsapp`/`messenger`/`instagram`), `provider_account_ref` (işletme hesabı) + `profile_name` (sağlayıcı profil adı) kolonları geldi, `source`'un DB varsayılanı KALDIRILDI (üç kanal dünyasında sessiz varsayılan hata fabrikası), `record_message`'a "template yalnız whatsapp" kuralı kondu (kısıt kaynağı göremez — source `conversation`'da) |

| 0040 | `0040_customer_merge.sql` | **Müşteri birleştirme** (09.10). Aynı insan iki kayıtta: WhatsApp'tan taslak açılıyor (yalnız telefon), sonra aynı kişi siteye e-postayla giriyor — `resolveIdentity` ikisini birleştiremiyor çünkü ortak anahtar yok. `merge_customers` (tek işlem: yarısı taşınmış iki kayıt, hiç birleştirilmemiş iki kayıttan KÖTÜDÜR) + `preview_customer_merge` (onay dökümü, taşımayla AYNI sorgudan — iki liste ayrışırsa operatör onayladığından farklısını taşımış olur) + `user_profiles.merged_into_id/at/by`. **İşin kalbi kimlik anahtarları:** hedef telefonu da e-postayı da almalı, yoksa aynı kişi yarın üçüncü kez taslak açar; boş dolar, dolu EZİLMEZ. **Sıra zorunlu ve testle ölçüldü:** önce kaynak kapanır (anahtarlar boşalır), sonra hedefe yazılır — tersi `user_profiles_phone_key`e çarpıyordu ve birleştirmenin asıl işlevi hiç çalışmıyordu. Üç tekillik çakışması veriden geliyor ve üçünde de **hedefinki kalır** (sepet PK'si `customer_id` — iki sepeti birleştirmek uydurma olurdu; `product_feedback (müşteri,ürün,bağlam)`; `points_entry` aynı gün ziyaret) — üçü de ön izlemede SAYILIYOR, çünkü yalnız kazanımı gösteren onay ekranı kaybı gizler. Kaynak silinmez KAPANIR (`order.customer_id` `restrict`, silme zaten reddedilirdi). Zincir (A→B→C), personel, anonimleştirilmiş kayıt reddedilir; kilit kimliğe göre sıralı (ters yönde iki operatör deadlock doğururdu) |

| 0041 | `0041_conversation_inbox.sql` | **Konuşma gelen kutusu** (15.5). `conversation_inbox` görünümü: konuşma + müşteri adı + son mesajın metni/yönü/türü + mesaj sayısı + `awaiting_reply` — hepsi TÜRETİLİR, kopya tutulmaz (`ticket_queue` deseni). **Neden görünüm:** 30 satırlık kuyruk bunları ayrı ayrı çekseydi 90 tur olurdu; ekran şeridi "son mesajı `last_message_at` ile eşleştir" kestirmesini denemiş ve haklı olarak vazgeçmiş — aynı ana düşen iki mesajda sessizce YANLIŞ satırı gösterir. `awaiting_reply` durumdan çıkarılamaz (konuşmanın durumu yok), son mesajın YÖNÜNDEN türer. **Bilerek yok:** devralma alanı (ajan yok, 15.13'ün işi) ve okunmamış sayacı ("okundu"yu yazan yüzey yok → sayaç ilk günden yalan söylerdi). `conversation (last_message_at desc nulls last)` indeksi keyset için; `nulls last` çünkü mesajsız konuşma kuyruğun başına değil sonuna düşmeli |

> **NUMARALAR 03.08'DE BAŞTAN VERİLDİ — 0001–0034, boşluksuz** (02.11 · kullanıcı kararı). 43 dosya
> 34'e indi: kırıntı dosyalar aile içinde birleşti ve numaralar sıfırdan sıralandı. Eski numaraların
> hiçbiri artık geçerli değil; kodda ya da dokümanda `0042` gibi bir atıf görürseniz **bayattır.**
>
> **Bu ancak bugün yapılabilirdi ve bir daha yapılamaz.** Yeniden numaralandırma, aynı numarayı
> farklı içerikle görmüş iki ortamı sessizce ayrıştırır — kimse hata almaz, şemalar farklıdır.
> Bugün güvenliydi çünkü **canlı yok, veri yok ve tek ortam var**; iş tek ajanın tek penceresinde
> yapıldı ve arkasından `db:reset` geldi. İlk üretim dağıtımından sonra bu kapı kapanır: o günden
> itibaren dosya düzenlenmez, yeni numaralı dosya eklenir (WORKFLOW §2).
>
> **Neden birleştirildiler (denetim P2):** kırıntı dosyalar aynı konuyu üç yere bölüyordu; bir
> tabloyu okumak isteyen üç dosya açıyordu. **Sıra korundu:** her taşıma öncesi bağımlılıklar
> tarandı — taşınan dosya, hedefinden SONRA doğan hiçbir nesneye şema düzeyinde değmiyor. (Tek
> istisna görünen `adjust_stock_batch`, `warehouse`'a **fonksiyon gövdesinden** değiyor; plpgsql
> gövdesi geç çözüldüğü için bu zaten depo dosyasından önce çalışıyordu ve taşıma bunu değiştirmedi.)
>
> **Birleşme "aynı dosyada" demektir, "aynı şey" değil:** `0023`'ün üç tablosu birbirinden ayrı
> kalır (anonim sayaç ↔ kişisel söz ↔ ürün eksenli söz) ve her taşınan bloğun başında ayrı bir
> dosyadan geldiğini söyleyen bir ayraç durur.
>
> **`vehicle` tablosu DÜŞTÜ** (aynı pencerede): servisi yoktu, `from('vehicle')` hiç geçmiyordu,
> sıfır satır taşıyordu ve hiçbir tasarım aracı bir VARLIK olarak kullanmıyordu. Zod şeması da
> düştü — karşılığı olmayan bir tip, okuyanı "araçlar sistemde tutuluyor" diye inandırırdı.
> Ölü şemayı `knip` YAKALAYAMAZ (SQL ve Zod onun kapsamı dışında), yani kendiliğinden hiç görünmezdi.
>
> **Posta kodu dosyası İKİYE ayrıldı** (denetim P1): şema `0033` (elle bakılır) + veri `0034`
> (üretilir). Eskiden tek dosyaydı, 1,8 MB ≈ 450k token — açan aracın bağlam bütçesi bitiyordu.
> Dosya kendisiyle de çelişiyordu: başlığı "elle düzenlenmez" diyordu ama şema yorumları elle
> bakılan metindi, ve fiilen elle düzenlendiler — `text_pattern_ops` indeks düzeltmesi üretece
> değil ÇIKTIYA yazılmıştı; bir sonraki `postal:build` ölçülmüş bir kazancı (36,9 ms → 0,11 ms)
> sessizce geri alacaktı. Üreteç artık yalnız veri dosyasını yazıyor: kayma yüzeyi sıfır.
>
> **TEK indeks burasıdır** (denetim P3, 03.08): `supabase/MIGRATIONS.md` silindi. erken bir migration'da donmuş
> ikinci bir indeksti — sonraki migration'ların hiçbirini görmemiş, repo genelinde tek referansı yoktu ve okuyan
> ajana yanlış gerçeklik öğretiyordu (*"`customer` tablosu şu alanlarla büyüyecek"* diyordu; oysa
> ayrı `customer` tablosu hiç doğmadı, kimlik tek tabloda toplandı). Taşınacak tekil içeriği
> yoktu: numara-çakışması vakası `docs-check.mjs:184`'te yaşıyor, büyüme planı ise zaten yanlıştı.
>
> **Tablo artık makine tarafından denetleniyor** (denetim A5, 03.08): `docs:check` her
> `NNNN_*.sql` dosyasının burada bir satırı olduğunu doğruluyor. Önceki hâlde tablo eski numaralamada donmuştu ve künyesindeki *"yazan ajanlar tamamlar"* sözü tutulmamıştı — yumuşak kural okunmayan
> kuraldır. Eksik yedi satır aynı turda dosya başlıklarından yazıldı.

| 0042 | `0042_assistant_proposal.sql` | **AI asistanının onay kuyruğu** (22.3 · `AI_ADMIN_ASSISTANT §5`). `assistant_proposal(kind, payload jsonb, summary, status, expires_at, decided_*, applied_at, result, error)`. **Tasarımın kalbi:** asistanın yazma araçları hiçbir tabloya dokunmaz — niyetlerini buraya yazar, patron tek tek onaylar, uygulama ONDAN SONRA ve **normal servis/motor yolundan** koşar (kuyruk ikinci bir yazma yolu AÇMAZ, yoksa DOMAIN kuralları atlanabilirdi). `payload` bir KOMUT değil DİLEKÇEDİR (ham SQL asla); şekli `kind`'a göre değişir ve Zod'la doğrulanır — şeklin kuralı iş kuralıdır, kolon kısıtı değil. **`kind` neden enum:** yeni tip = asistana YENİ BİR YAZMA YETKİSİ ve o, kullanıcı kararı gerektirir (§9.4) — enum kararı migration'a, yani gözden geçirilen bir yere taşır. `summary` patronun okuyacağı TEK cümledir (ekran JSON göstermez: operatörden kod okumasını istemek onayı formaliteye çevirir). `expires_at` tazelik kapısı: bayat öneriyi patronun önüne hiç koymamak, reddedilecek kararı sormaktan iyidir — uygulama anındaki motor doğrulamasının YERİNE değil ÖNÜNE geçer. `failed` ≠ `rejected` ("sistem yapamadı" ile "patron istemedi" tek renge düşmemeli). Kısıt: karar anı ile durum ayrışamaz (`expired` karar-verilmemiş taraftadır — süre dolması karar değil, kararın kaçırılmasıdır) |
| 0043 | `0043_site_image.sql` | **Sayfa görselleri** (09.16 · müşteri şeridinin talebi 09.08). `site_image(slot, image_key, odak/zoom, image_alt, image_updated_at)` + `site_image_slot` enum (`home_hero` · `packages_hero` · `professionals_hero` · `empty_cart`). **Kardeşlerinden farkı sahibi:** ürünün/kategorinin/tarifin görseli KENDİ satırının künyesinde durur, çünkü görsel varlığa aittir ve varlık silinince anlamsızlaşır; bunlar bir varlığa değil bir **SAYFA YERİNE** aittir — "boş sepet çizimi"nin karşılık geldiği bir satır yok ve olmayacak. **Anahtar enum, serbest metin değil:** slot kümesi kapalıdır ve yeni slot ancak onu çizen ekran doğunca doğar; serbest metin ekranda karşılığı olmayan bir görselin yüklenmesine izin verirdi (yüklendiği anda kaybolan dosya). **`image_key` not null:** satırın kendisi görseldir, anahtarsız satır ekranı "dolu" sanmaya iter — **boş slot = satır YOK** ve okuyan taraf yer tutucusunu çizmeye devam eder. Çok dillilik gerekmiyor (görselin içinde metin yok, aynı fotoğraf üç dile hizmet eder) ama `image_alt` dilli kalır: o görselin kendisi değil ekran okuyucuya söylediği cümledir. Odak/zoom burada da var, çünkü aynı fotoğraf 16:9 ve 3:2 çerçevelere farklı oturur |
| 0044 | `0044_neighbor_invite.sql` | **Komşu daveti** (17.10 · kullanıcı kararı 11.08). `neighbor_invite(token unique, inviter_id, order_id unique, delivery_zone_id, delivery_date, max_uses)` + `order.neighbor_invite_id` FK. **Getiren davetinden AYRI bir kavram** ve ayrılması şart: `referred_by` hesapsız birini MÜŞTERİ yapmayı ödüllendirir (anahtarı kişi, ömrü sonsuz), bu var olan bir SEFERE ikinci sipariş eklemeyi (anahtarı `(bölge, gün)`, ömrü o günün kesim saati). Davet edilen kişi zaten müşterimiz olabilir — o hâlde `referral` hiç doğmaz ama `neighbor` puanı doğar; ikisi aynı turda da doğabilir ve bu çift ödeme değildir. **"Sefer" yeni bir varlık DEĞİL:** rota günü zaten `(delivery_zone_id, delivery_date)` ile tanımlı; ayrı bir `trip` tablosu bugün türetilen bir gerçeği saklamak ve iki kaynağın ayrışmasını göze almak olurdu. **İkili yine de KOPYALANIYOR** (bilinçli snapshot): siparişin günü operasyonda değişse bile komşuya verilen söz değişmemeli. **Kullanım SAYILMAZ, türetilir** (`order.neighbor_invite_id` taşıyan iptal-olmayan siparişler): azalan sayaç iptal edilen siparişte elle geri alınmak zorunda kalır ve biri unuturdu — defterin ve para hareketlerinin aynı kuralı. **Güncelleme yolu yok:** paylaşılmış bir bağlantının sözü sahibinin haberi olmadan değişmemeli. **`expires_at` yok:** geçerlilik kesim saatinden HESAPLANIR (`deliveryRunWindow` + `order_cutoff_time`); saklanan bir son kullanma anı, ayar değiştiği gün yalan söylerdi |
| 0045 | `0045_storage_area_vehicle.sql` | **Ölçüm noktaları** (19.28 · kullanıcı isteği 17.08). `storage_area(warehouse_id, name, kind, target_min_c/max_c, is_active, sort_order)` + `storage_area_kind` enum (`frozen` · `chilled` · `ambient` · `staging`) + `vehicle(plate unique, label, warehouse_id, is_active, sort_order)`; `temperature_log` serbest metin `location` yerine `storage_area_id`/`vehicle_id` alır ve **tam biri dolu** olur (`num_nonnulls(...) = 1`). **Kök:** nokta serbest metindi ve üç şeyi bozuyordu — yazım farkı geçmişi bölüyordu (`Dolap 1` ≠ `Dolap-1`), "bu nokta genelde şu kadar okuyor" uyarısı o bölünen geçmişe dayanıyordu, ve **ölçülmeyen tespit edilemiyordu**: var olduğu bilinmeyen bir dolabın eksik ölçümü de bilinemez — oysa hijyen denetiminin ilk sorusu tam olarak budur. **`vehicle` GERİ GELEN tablodur:** 0031'de tanımlanmış, 03.08'de tüketeni olmadığı için düşürülmüştü ve künyesi *"gerçekten gerekince geri gelir ve o gün doğru soruyu sorarız: araç bir depoya mı, bir güne mi, bir kuryeye mi bağlanır"* diyordu — cevap **araç bir ölçüm noktasıdır** (soğuk zincirin yoldaki yeri). **İki tablo, tek `storage_point(type)` değil:** ortak tablo sıcaklık kaydının bağını tekilleştirirdi ama zorunlulukları yalanlardı (dolabın deposu zorunlu, aracınki değil; plaka araçta benzersiz, alanda karşılığı yok) — tek tabloda hepsi nullable olur, kural veriden ekrana kaçardı. **Polimorfik anahtar da reddedildi:** FK yazdırmaz, silinen bir dolabın kayıtları sessizce sahipsiz kalırdı. **Aracın `warehouse_id`'si nullable ve zorlanmıyor** — K8'in reddettiği şey bağın KISIT olmasıydı ("araç bir depoya aittir"); buradaki alan bir künye ("genelde buradan yükler"), araç hâlâ hiçbir güne/kuryeye bağlanmıyor. **Hedef aralık ya iki uçlu ya hiç** (`storage_area_target_pair`): tek uçlu aralık "altı mı üstü mü serbest" sorusunu okuyana bırakırdı; boş bırakmak meşru (rafın beklentisi yoktur) ve o hâlde sapma noktanın kendi alışkanlığından tahmin edilir. **Nokta silinmez** (`restrict`), susturulur: denetim geçmişi noktanın adına değil kaydına bağlı |
| 0046 | `0046_delivery_run.sql` | **SEFER — gerçekleşen teslimat rotası + sefer kapanışı** (11.7 · kullanıcı kararları 18.08, etüt `docs/feature/sefer.md`). `delivery_run(reference_no SF-, zone, date, warehouse snapshot, courier, vehicle?, created/departed/returned_at)` + `order.delivery_run_id` + `delivery_run_close` + `delivery_run_collection` görünümü + iki RPC (`start_delivery_run` · `close_delivery_run`). **İki "sefer" var, burası yalnız GERÇEKLEŞENİ saklar:** planlanan `(zone, date)` türetilmiş kalır (0044 kararı yürürlükte); bu satır araç fiilen çıkarken doğar. **`order.courier_id` sökülmedi, seferden SENKRONLANIR:** beş sahiplik kapısı ve ~90 okuma ona yaslanıyor; start, seferi süren kuryeyi aynı kolona yazar — "siparişin kuryesi seferin kuryesinden gelir". **Rota+gün başına TEK sefer** (mutlak unique; eşzamanlılık kilidi indekste — iki kurye aynı rotayı başlatırsa ikincisi `already_started` alır). **Durum makinesi YOK:** üç damga, hâl türetilir. **Kapanış sefer eksenli** (0025'in halefi): fotoğraf çözümden önce, takılı `out_for_delivery` duraklar kapanışın kendisi tarafından motorun "ulaşılamadı" kenarıyla (`ready`) çözülür — K4: geçiş otomatik, yeni GÜN sevkiyatçının kararı (16.08 "görünür devir" korunur). **Claim durum geçişi yapmaz:** `ready → out_for_delivery` izni motorda, dört-liste sözleşmesi uygulama katmanında |
| 0047 | `0047_barcode.sql` | **Ürün barkodu** (Modül 23 · kararlar 17.08, operasyon 21.08). `variant_barcode(variant_id, code unique, kind unit|case, qty_per_code, created_by)` + `barcode_kind` enum. **Dış dünyanın kimliği:** paket EAN-13, koli GTIN-14 — "bu hangi mal"; bizim bastığımız kutu QR'ı ("bu hangi kayıt") AYRI iştir ve tasarım dönünce kendi migration'ıyla doğar. **Varyanta kolon değil ayrı tablo:** aynı varyantın birden çok kodu olur ve ÇARPAN KODUN ÜSTÜNDE durur (koli kaç adetse kodu söyler; `supplier_product.pack_qty` tedarikçinin satış bilgisidir, okutulanın kanıtı değil — iki tedarikçinin kolisi farklı olabilir). **Kod GLOBAL unique:** bir kod iki varyanta bağlanamaz; yanlış varyanta bağlanabilir ve düzeltme SİLMEDİR (eşleme tarihçesiz bir kayıttır, beyan değil — geri alma web varyant editöründen). **`unit` çarpanı 1'e kilitli** (check): "tek paketin çarpanı" diye bir serbestlik yok. **Tablo kabulde KENDİNİ doldurur** (öğrenen eşleme): tanınmayan kod için ekran "bu kod hangi ürün?" diye sorar; `created_by` o izi taşır (null = sistem/seed kaydı) ve `set null` — personel silinse eşleme yaşar. RLS politikasız (yalnız service-role; müşteri yüzeyinin barkodla işi yok) |
| 0048 | `0048_order_box.sql` | **Sipariş kutusu** (Modül 23 · 23.6; ekranlar mevcut desene göre — kullanıcı kararı 22.08). `order_box(order_id, warehouse_id, box_no, code unique, sealed_at/by, printed_at, loaded_at/by)` + `order_box_item(box_id, order_item_id, qty)` + `seal_order_box` RPC. **İki kimlikten İKİNCİSİ:** 0047 dış dünyanın kodunu aldı ("bu hangi mal"), bu tablo bizim bastığımız QR'ı ("bu hangi kayıt"). **Kutu kodu `reference_no` DEĞİL ve ondan türetilemez** (Netleşecek 4): referans müşteriye görünür, kutu kodu teslim kaydı düşürür — üreteç `orderBoxCode` (`KT-YY-` + 10 karakter, kardeşlerinden bilerek uzun: referans telefonda okunur, kutu kodu kamerayla). **Kapanış RPC ile TEK transaction:** kutu içeriği + `record_preparation` + mühür bölünemez — "kutu var ama parti izi yok" doğamaz (STACK §13). ⚠ 0015 picks yazımı ABSOLÜT olduğundan çok kutulu birleşimi `sealBox` kapısı kurar; RPC **Σ kutu = fulfilled_qty** eşitliğini denetler ve bozuksa tümünü geri alır — kutulanmış kalem kutusuz akışla karışamaz (çift akış sipariş düzeyinde meşru, kalem düzeyinde değil). **Açık kutu yüklenemez** (check): yüklemenin okuttuğu QR ancak kapanmış kutunun etiketindedir. `box_no` kimlik değil insan sayısı (unique sipariş içinde; boşluk zararsız). RLS politikasız (yalnız service-role; kutu kodu müşteriye hiç gösterilmez) |
| 0049 | `0049_notification.sql` | **Bildirim KAYDI + teslim defteri** (14.12 · kurgu incelemesi 26.08). `notification(profile_id, kind text, target_type/id, warehouse_id?, payload jsonb, dedupe_key, read_at, dismissed_at)` + `notification_delivery(notification_id, channel, status, reason, ref)`. **Kök:** olay doğduğu an maile dönüşüyor ve HİÇBİR İZ kalmıyordu — müşteri maili silerse haber yok olur, uygulamanın zili boş yer tutucuya bakar (21.13 defteri), okundu diye bir kavram hiçbir yüzeyde yoktu. Kuyruktan türetme ELENDİ (kullanıcı 26.08): mobil tasarımın satırlarının yarısının arkasında kuyruk yok ("Rota güncellendi" bir AN'dır). **Bildirim ≠ kuyruk:** kuyruk maddesi buraya yazılmaz — yazılsaydı "47 bildiriminiz var" ekranı doğar, içi bugünkü iş listesi olurdu. **Metin saklanmaz, VERİ saklanır** (`payload`): dil müşterinin tercihine bağlı; ama 20 satırlık liste hedefe N+1 gitmesin, hedef silinince (0037 talebi TÜMÜYLE siler) satır cümlesiz kalmasın diye dil-bağımsız kimliksiz küçük veri satırda durur. **Teslim AYRI tablo, tek kolon değil:** BELGE sınıfı "mail her zaman + push da" der — tek satır birden çok teslim doğurur ve notifier zaten dizi döndürüyordu; ayrıca "none" iki farklı şeyi söylerdi (kanal yoktu / vardı-düştü). **`kind` DB'de text, enum değil:** küme `packages/types`ta ve her modülle büyür; DB enum'u her türde migration isterdi, emekliye ayrılan tür eski satırları kırardı. **Tekillik `(profile_id, dedupe_key)` kısmi:** fan-out aynı olayı birden çok personele yazar; formülü OLAY tanımlar — istisna olaylarında (eksik/iade) anahtar YOK, her düzeltme ayrı haberdir. **`warehouse_id` depo boyutu:** depo-bağlamlı personel olayı rol × depo kesişimiyle dağıtılır (CLAUDE değişmezi; süzgeci unutulan dağıtım tek depolu veride doğru görünürdü). Cascade FK → purge ve GDPR silmesi (0037'ye satır eklendi) ek hedef istemez |
| 0050 | `0050_push_device.sql` | **Push cihaz jetonu** (14.14). `push_device(profile_id, token unique, platform check, disabled_at, last_seen_at)` + `register_push_device` RPC. **Kolon `profile_id`, `customer_id` DEĞİL:** operasyon kabuğu da push alacak — "customer" adı personel jetonuna ikinci tablo doğururdu. **Tekillik TABLO GENELİ ve upsert SAHİP DEVREDER** (son giren kazanır — cihaz fiziksel olarak onun elindedir): aile telefonunda A çıkar B girer, devir olmasaydı A'nın bildirimi B'nin ekranına düşerdi (kişisel veri ifşası, kurgu incelemesi 8). "Önce sil sonra yaz" iki deyimdi ve arada düşen süreç jetonu sahipsiz bırakırdı — RPC kısıtın üstünde atomik. **`disabled_at` izin karası** (inceleme 10): OS'ta bildirimi kapatanın jetonu canlı kalır ve Expo "gönderdim" der; uygulama her açılışta izni raporlar, kapalı cihaz gönderilebilir sayılmaz ve sıra maile düşer. **'web' platformu bilerek yok** (KARARLAR 26.08). RLS deny; jeton bir YETKİDİR, hiçbir uçtan geri okutulmaz. Cascade FK → purge/0037 ek hedef istemez (0037'ye beyan satırı yine eklendi) |
