# 07 — Sipariş, Checkout ve Ödeme

## Kapsam

Siparişin doğuşundan kapanışına kadar tüm akış: sepet, checkout (teslimat + ödeme seçimi), rezervasyon–ödeme sırası, Stripe, durum geçişleri, kısmi karşılama, iade, hızlı satış. `domain-core` kararları (03) burada DB'ye ve dış dünyaya bağlanır. Sistemin en kritik ve en çok tabloya dokunan akışı — çok-tablolu yazımlar **RPC** ile atomik.

## Okunacaklar

- `DOMAIN.md §4` (rezervasyon/TTL/geç ödeme), `§5` (fiyat/indirim dağıtımı), `§6` (teslimat/kargo ücreti/teslim onayı), `§7` (ödeme/vade/kapıda), `§8` (kısmi karşılama)
- `ORDER_LIFECYCLE.md` — tamamı
- `STACK.md §13` (webhook güvenliği + çok-tablolu RPC — taslak)

## Bağımlılık

`03-domain-core` (kararlar), `04-auth-kimlik` (müşteri bağı), `06-stok` (rezervasyon/teslim RPC'leri). Stripe kurulumu → Netleşecekler.

## Başlarken verilecek izah (örnek)

> "Sipariş akışını kuruyoruz: müşteri sepeti onaylayınca stok ayrılır, ödeme başlar, mal hazırlanıp teslim edilir ve sipariş kapanır. En hassas yer ödeme–stok sırası: önce stoğu ayırıp sonra parayı çekiyoruz ki karşılığı olmayan tahsilat olmasın. Ödeme sayfasının süresini stok ayırma süresine eşitliyoruz, gecikmiş ödeme için de emniyet kuralı koyuyoruz. Birden çok tabloya birden yazan her adımı (onay, teslim) tek işlemde yapıyoruz ki yarıda kesilince tutarsız kayıt kalmasın."

## Görevler

- [x] (07.1) **Sepet (Cart):** sunucu-kalıcı sepet servisi (giriş yapmış müşteri); sepetteki fiyat **gösterimdir** (`unit_price` kopyası — değişiklik tespiti için), bağlayıcı fiyat checkout başlangıcında sabitlenir; anonim → giriş sonrası devralma
  - *Bitti:* cihaz değişince sepet duruyor; sepette bekleyen kalem checkout'ta güncel fiyatla karşılaştırılıyor
  - **Durum (27.07):** `0012_order.sql` · `CartService` · `cart.schema.ts`. Müşteri başına TEK satır (birincil anahtar `customer_id` — "tek sepet" kuralı şemada zorlanıyor); kalemler jsonb (sepet kalemi bir varlık değil, geçici seçim — sorgulanmaz, okunur). Aynı satır tekrar eklenince adet birleşir; adedi 0'a indirmek satırı siler; boşaltma satırı tamamen kaldırır. **Teklif satırı (partiye çıpalı) normal satırdan ayrı yaşar** — aynı ürün hem indirimli partiden hem normal fiyattan sepette olabilir. **Devralmada sunucudaki sepet korunur**, gelen kalemler üstüne eklenir: giriş yapmak daha önce eklenmiş bir ürünü sessizce kaybettirmemeli. 12 test.
  - **Kural düzeltmesi (27.07, kullanıcı kararı):** görev başta "fiyat sepete eklenince sabitlenir" diyordu; sepet aylarca beklediği için bu süresiz fiyat garantisi anlamına geliyordu (maliyeti oynayan donuk gıdada zarar; fiyat düşünce müşteriye haksızlık). **Sabitleme anı checkout başlangıcına alındı** — stok ayırma ve ödeme oturumuyla AYNI 30 dk'lık pencere, ayrı süre/cron yok. Arttıysa bildir+onay, düştüyse sessiz uygula, tükenen teklif partisi aynı akıştan geçer. `DOMAIN §5`'e yazıldı.
- [x] (07.2) **Checkout — teslimat:** adres → posta kodundan zone türetimi → rota-içi gün(ler)/kargo; cut-off saatine göre gün hesabı; kargo yasak ürün (`shippable=false`) kuralı
  - *Bitti:* tek gün otomatik, çok gün seçtiriliyor; cut-off sonrası sipariş ertesi güne düşüyor (test)
  - **Durum (27.07):** `0014_delivery_zone.sql` · `DeliveryZoneService` · motor `domain-core/delivery/delivery-days.ts` · kapı `apps/web/lib/order/delivery.ts`. 13 birim + 7 entegrasyon testi.
  - **Rota içi/dışı SAKLANMIYOR:** adresin posta kodu aktif bir bölgeye düşüyorsa rota içi. Saklansaydı bölge sınırı admin tarafından değiştirilince ertesi gün yalan olurdu (`address.in_route` kolonu bilerek yok).
  - **Kesim saati sınırı dâhil:** tam 16:00'da gelen sipariş de kaçırmış sayılır (araç yükleniyor). Kesim saati `Setting`'ten okunur; bozuk değer akışı kilitlemez, kesim uygulanmaz.
  - **Tek tarih varsa seçim sunulmaz** (`requiresDateChoice`) — arayüz kararı veriden türüyor, ekranda `if` yazılmıyor.
  - **Soğuk zincir kuralı yönlü:** kargolanamayan ürün yalnız ROTA DIŞI adreste engel; rota içinde kapı teslimi zaten mümkün olduğu için sorun değil.
- [x] (07.3) **Checkout — ödeme seçenekleri:** bağlama göre (online/kapıda/vadeli); kapıda tavan + nakit uyarısı + `cod_allowed`; vade freni (03 kararı) uygulanır; `shipping_fee` hesabı + KDV
  - *Bitti:* tavan aşan sipariş kapıda seçeneğini gizliyor; vadesi dolu müşteride "hesaba" kapalı
  - **Durum (27.07):** motor `domain-core/delivery/shipping-fee.ts` (yeni) + `payment/checkout-options.ts` (03.7/03.8'den) · kapı `apps/web/lib/order/checkout-options.ts`. 10 birim + 14 entegrasyon testi. Tavanlar, eşikler ve ücret artık `settings`'ten okunuyor — kodda sabit yok.
  - **Açık bakiye ve gecikme TÜRETİLİYOR** (DOMAIN §7): ödenmemiş vadeli siparişlerden hesaplanıyor, hiçbir yerde saklanmıyor. İptal edilen sipariş bakiyeye sayılmıyor (testli). Saklanan bakiye kayarsa fark edilmez; türetilen kayamaz.
  - **Kargo ücretinin KDV'si tek orana bağlanmadı:** taşıma bedeli **taşıdığı malın oranını izler** (FR uygulaması). Sepette hem %5,5 hem %20 ürün varsa ücret kalem tutarlarına oransal bölünüyor ve her parça kendi oranından vergileniyor; kuruş kaybı yok (Σ parça = ücret). Tek oranlı sepette sonuç tek parça. **Bu bir vergi işlemi kararıdır — muhasebenizle teyit etmekte fayda var**, kod tek satırda tek orana çevrilebilir.
  - **Ücretsiz kargoya kalan tutar** dönüyor (`remainingForFreeShippingCents`) — "20 € daha ekleyin" mesajını arayüz hesaplamıyor, veriden alıyor.
- [x] (07.4) **Rezervasyon → ödeme sırası:** atomik ayır (06 RPC) → Stripe checkout session (TTL = rezervasyon süresi, session expiry eşit); ayrılamazsa ödeme hiç başlamaz; `acquisition_source` ilk siparişte yazılır; izin kutusu (`marketing_consent`)
  - *Bitti:* stok yetmezse ödeme açılmıyor; session süresi Setting TTL'iyle eşit
  - **Durum (28.07):** kapı `apps/web/lib/order/checkout-session.ts` · istemci `apps/web/lib/stripe.ts`. 6 test.
  - **EKSİK HALKA bulundu ve yazıldı (28.07):** `createCheckoutSession` ortada duran bir `draft` sipariş varsayıyordu, `resolveDelivery` posta kodunu, `resolveCheckoutPayment` sepet tutarını — üçü de girdisinin hazır olduğunu kabul ediyordu. **Sepeti taslak siparişe çeviren kapı hiç yazılmamıştı**; `OrderService.create` bugüne dek yalnız testlerden çağrılıyordu. `lib/order/checkout-draft.ts` o boşluğu kapatır: sepeti sunucuda yeniden okur (bağlayıcı fiyat DOMAIN §5 uyarınca burada sabitlenir), teslimatı ve ödeme seçeneklerini çözer, seçimleri doğrular, taslağı açar. 6 test.
    **İstemciden yalnız SEÇİMLER alınır** (adres · gün · yöntem); tutar, kargo ücreti, indirim ve teslimat türü hiç kabul edilmez. Aksi hâlde tarayıcı konsolundan gönderilen bir `total` siparişin parasını belirlerdi. Her seçim yeniden doğrulanır — ekran açıkken bölge kapanmış, kesim saati geçmiş, tavan değişmiş olabilir; testte uydurma bir gün ve başkasının adresi reddediliyor ve **ortada yarım taslak kalmıyor**.
    **Paket BURADA parçalanır.** Müşteri tek bir paket satırı görür (DOMAIN §13) ama sipariş varyant kalemlerinden oluşur: depo raftan paket değil ürün toplar, stok varyanttan düşer, kâr varyant maliyetinden çıkar. Kalem `bundle_id` taşır → nereden geldiği kaybolmaz. **Birim fiyat paketin PAYIDIR, katalog fiyatı değil** — katalog fiyatı yazılsaydı kalemler toplamı paketin fiyatını aşar ve sipariş kendi toplamıyla çelişirdi; indirim tam da o farktır (testte 20+10 katalog → 18+9 pay).
  - **KDV işlemi artık siparişte ÇÖZÜLÜYOR (03.08):** `resolveVatTreatment` (03.10) yazılıydı, testliydi ve **hiçbir yerden çağrılmıyordu** — `vat_treatment` kolonunu yazan tek şey `default 'domestic'`ti. Bu bir özellik eksiği değil **yanlış faturaydı**: VIES ile doğrulanmış vergi numarası olan Alman B2B müşteriye Fransız KDV'si kesiliyor, faturaya "Autoliquidation" ibaresi girmiyor ve müşteri kendi ülkesinde beyan edemediği bir KDV ödüyordu. Muhasebe dışa aktarımı ve kâr hesabı (`accounting/export`, `accounting/profit`) bu kolonu zaten okuyor, yani kayıt hep "yurt içi" diyordu. Taslak artık kanalı ve teslimat ülkesini motora veriyor, `vat_treatment`'ı yazıyor ve **%0'ı kalem ORANINDA uyguluyor** — kargonun KDV'si taşıdığı malın oranını izlediği için ücret kendiliğinden sıfırlanıyor. `vat_number_snapshot` yalnız %0 uygulandığında yazılıyor (denetim kanıtı, `address_snapshot` ile aynı kural). 2 entegrasyon testi `checkout-shipping-order.test.ts`'e eklendi — DE kargo deposu ülke başına tek olduğu için (`warehouse_single_online`) ayrı dosya paralel koşuda çakışırdı. **Doğrulanmamış numara %0 AÇMIYOR** ve asıl sınanan dal bu: `vatNumberValid` üç değerli (`null` = hiç sorulmadı), yanlış %0 uygulamanın bedelini biz öderiz.
  - **Durum (29.07 · indirimin kalem PAYI yazılmıyormuş — para hatası):** kullanıcı bildirimi: *"3 €'luk indirim yapıldı ama neden anlamadım; mailin altında `À régler à la livraison : 3,00 €` yazıyor."* İki soru, aynı kök. Sipariş başlığa `discount_amount = 3` yazıyordu ama **kalemlerin `line_discount_amount`'ı 0 kalıyordu** — `expandToOrderItems` payı hiç geçirmiyordu.
    **Sonucu para hatası:** ödeme durumu motoru "müşteri ne kadar borçlu" sorusunu KALEMLERDEN topluyor (`derivePaymentStatus`, DOMAIN §7). Payı 0 gören motor beklenen tutarı indirimsiz hesaplıyor: tamamı online ödenmiş sipariş (47,50 € tahsil / 47,50 € toplam) `partial` görünüyor, `amountToCollectCents` tam olarak indirim kadar (3,00 €) çıkıyor ve bildirim maili müşteriden **kapıda o parayı istiyordu**. Sipariş kendi toplamıyla çelişiyordu.
    **Motor haklıydı, yazan taraf eksikti:** `derivePaymentStatus`'ın künyesi payı zaten *girdi* olarak tanımlıyor ("pay dağıtımı burada girdi olarak alınır, yeniden hesaplanmaz") — kararın sahibi indirim motoru (`distributeDiscount`, `Σ pay = indirim` garantili). Payı checkout'ta yeniden bölüştürmek kuruş artığını iki yerde farklı yuvarlardı; yapılması gereken tek şey taşımaktı.
    **Hiza korunuyordu, kullanılmıyordu:** `lib/cart/read.ts` muaf satırlar için bile tam bir kayıt gönderiyor (yorumu da bunu söylüyor: *"pay dizisi kalem sırasıyla hizalı kalsın"*), yani `lineShares[i]` ↔ `cart.lines[i]` zaten kuruluydu. Pakete pay 0 yazılır (DOMAIN §13: pakete sepet indirimi binmez).
    **Kapsam:** üretimde sipariş kalemi yazan tek yer bu kapı. Test kategori kapsamlı bir kampanya kuruyor, sepet kapsamlı değil — paylaşılan yerel veritabanında sepet kapsamlı aktif bir kampanya, o sırada koşan başka bir ajanın siparişine de inerdi (CLAUDE.md §4b). 7 test (biri yeni: payların toplamı = başlıktaki indirim **ve** tamamı ödenmiş siparişin `paid` olması).
    **Yereldeki eski sipariş DÜZELTİLMEDİ:** veri kullanıcının, üstelik greenfield — yeni sipariş doğru yazılıyor.
  - **Kapsamlı inceleme + müdahale (29.07, kullanıcı isteğiyle; diğer ajanlar durduruldu):** yukarıdaki düzeltme yazım yolunu kapattı ama hatanın **eni daha genişti**. Dört bulgu:
    1. **Seed İKİNCİ ve KALICI bir kaynaktı.** ~~`scripts/seed/orders.ts`~~ (dosya 01.09'da silindi — besleme artık sipariş yazmıyor, künye `scripts/seed.ts` → §SİPARİŞ) başlığa `discountAmount` yazıp kalemlere pay geçirmiyordu (`SiparisKalem` tipinde alan bile yoktu) — yani her `db:reset` bozukluğu yeniden üretiyordu. Aynı `distributeDiscount` ile düzeltildi.
    2. **Yayılma alanı maille sınırlı değildi.** `line_discount_amount` üç yerde daha okunuyor: `accounting/line.ts` `lineGrossCents` (→ **12.7 muhasebe export'u ve 12.6 kârlılık ciroyu indirim kadar FAZLA sayıyordu**; teslim/tamamlanmış iki siparişte ölçüldü: +8,00 € ve +9,42 €, KDV de o oranda şişiyor), operasyon sipariş detayının satır toplamı, ve bildirim mailinin ödeme hapı.
    3. **Yereldeki dört sipariş onarıldı** (tek seferlik ~~`scripts/repair-discount-shares.mjs`~~ — kuru çalışma varsayılan, `--apply` ile yazar, idempotent; **işini görüp SİLİNDİ**, `6fda39c`'de geçmişte durur. Aynı arıza tekrarlarsa oradan çıkarılır — repoda duran ve bir daha koşulmayacak bir betik, bir gün yanlış veriye koşulacak bir betiktir). İkisinin `payment_status`'ü `partial` → **`paid`** oldu (LA-26-RMQUFL, LA-26-99C7YN); mail artık "kapıda 3,00 € ödenecek" yerine "Payée en ligne" diyor.
    4. **Motor bu hata sınıfına BAĞIŞIK kılındı.** `derivePaymentStatus` artık hazırlık kesinleşmeden beklenen tutarı kalemlerden yeniden toplamıyor; siparişin kendi `total`'ini kullanıyor (`orderTotalCents`) — indirim ve kargo zaten onun içinde. Aynı gerçeği iki yoldan hesaplamak, birinin bozulmasına açık kapı bırakmaktı. 4 birim test: kalem payı yazılmamış girdi ile yazılmış girdi **aynı sonucu** veriyor.
    - **Kalan risk KAPATILDI (30.07):** `order.discount_amount = Σ line_discount_amount` değişmezini artık veritabanı zorluyor — bkz. bir alttaki durum notu.
  - **Durum (30.07 · siparişin doğuşu tek transaction'a alındı — RPC borcu kapandı):** `OrderService.create` üç ayrı ifadeydi (başlık → kalemler → indirim kullanım kaydı) ve yarım kalan yazım "telafi silmesi" ile geri alınmaya çalışılıyordu. **Telafi bir garanti değildir:** silme de düşebilir (o zaman kalemsiz bir sipariş kalır), süreç arada ölebilir, ve telafi yalnız hata anında çalışır — sessiz bir yarım yazım hiç fark edilmez. Üstelik iki ifade arasındaki bozuk hâl okunabilir durumdadır.
    **Yazım tek RPC'de:** `create_order` (`0030_create_order.sql`) üç tabloya da tek transaction'da yazar. Kolonlar tek tek SAYILMAZ — gelen gövdenin anahtarları tablonun katalogdaki kolonlarıyla kesiştirilir, gelmeyen kolon ifadeye hiç girmez ve tablonun kendi varsayılanını alır. Gerekçe deneyimden: sayılsaydı `order`'a eklenen her yeni alan RPC'yi de düzeltmeyi gerektirirdi ve bir gün unutulurdu — nitekim aynı hata seed'de yaşandı (`line_discount_amount` eklendi, seed'in kalem tipine geçirilmedi, her `db:reset` bozukluğu yeniden üretti).
    **Değişmez artık VERİDE:** `order_discount_balance` ertelenmiş kısıt tetikleyicisi (`deferrable initially deferred`) COMMIT anında `discount_amount = Σ line_discount_amount` denetler. Ertelenmiş olması şart — başlık kalemlerden önce yazılır, yani ifade ifade bakıldığında denge geçici olarak bozuktur. Kısıt yazım yolunda değil veride durduğu için yarın açılacak ikinci bir yol (elle giriş 09.8, WhatsApp ajanı, onarım betiği, doğrudan SQL) da bozuk sipariş yazamaz. Başlık tarafında yalnız `discount_amount` değişimi izleniyor: her `order` güncellemesine bağlansaydı durum geçişi/teslim/tahsilat da bu denetimi ödetirdi.
    10 test (`packages/database/src/services/order.test.ts`). Çoğu **hatanın ardından ne kaldığına** bakıyor: "fırlattı" yetmez, telafi mantığının kapattığı sanılan açık tam olarak buydu. Kısıt ayrıca **dört mevcut test fikstürünü** yakaladı — üçü başlığa indirim yazıp kalem payını 0 bırakıyordu (yani 29.07'de düzelttiğimiz hatanın kendisini kuruyordu), biri tersini yapıyordu.
  - **Sıra tersine çevrilemez:** önce stok ayrılır, sonra ödeme açılır. Tersi, müşterinin parayı ödedikten sonra "mal kalmamış" cevabını almasıdır — sistemin en pahalı hatası. Ayrılamayan tek kalem bile varsa sağlayıcıya HİÇ gidilmez (testli).
  - **Yarıda kalan ayırma temizlenir:** üçüncü kalem ayrılamazsa ilk ikisi geri bırakılır; yoksa stok, hiç doğmayacak bir sipariş için TTL boyunca kilitli kalırdı.
  - **Pencereler eşitlendi:** rezervasyon TTL'i (`reservation_ttl_minutes`, 30 dk) ile oturumun `expires_at`'i aynı dakika. Oturum uzun olsaydı ödenmiş ama stoğu gitmiş sipariş; kısa olsaydı boşuna kilitli stok doğardı.
  - **Sağlayıcı yoksa stok AYRILMADAN dönülür** (`provider_unavailable`) — açılamayacak bir ödeme için mal kilitlenmemeli. Anahtarsızlık sessiz başarı değildir.
  - **Ödeme oturumu bir PORT** (`CheckoutSessionCreator`): bugünkü uygulaması Stripe, test sahte üreteç verir. Sıra kuralı ağa çıkmadan sınanıyor (12.4'teki AI portuyla aynı desen).
  - **Anahtar doğruluğu ayrı bir sorudur, ayrı araçla sınanır:** testler sağlayıcıya ağdan gitmez (port). `pnpm stripe:smoke` gerçek anahtarla oturum açar, ayrılan adedi ve pencereyi yazar, sonra Stripe'taki oturumu kapatıp verisini siler. Kurulumdan ve anahtar değişiminden sonra çalıştırılır.
  - **Yerel env tuzağı (28.07'de yaşandı):** Node var olan bir ortam değişkenini EZMEZ — ilk yükleyen kazanır. Kök `.env`'de BOŞ bir `STRIPE_SECRET_KEY=` satırı varsa `apps/web/.env.local`'deki gerçek anahtarı gölgeler ve akış `provider_unavailable` döner. Stripe anahtarlarının kökte hiç işi yok; script bu hâli tespit edince ekrana sebebini yazar.
  - **`acquisition_source` yalnız BOŞSA yazılır:** müşteriyi ilk getiren kaynak sonraki kampanyalarla ezilmez (testli). İzin kutusu baştan işaretsiz, yalnız işaretliyse yazılıyor — "izin vermedi" ile "sormadık" ayrı şeylerdir.
- [x] (07.5) **Stripe webhook (~~apps/backend~~ → `apps/web` — SAPMA, ADR kayıtlı; ayrıntı Durum notunda):** imza doğrulama + `WebhookEvent` idempotency (aynı olay no-op) + ödeme onayı → `confirmed` + `reference_no` üretimi; geç ödeme dallanması (yeniden ayır / olmazsa otomatik iade)
  - *Bitti:* aynı event iki kez gelince tek kayıt; rezervasyon düşmüş siparişte geç ödeme iki dalıyla doğru
  - **Durum (28.07):** `0022_webhook_event.sql` + `WebhookEventService.claim` · işleyici `apps/web/lib/order/stripe-webhook.ts` · HTTP kabuğu `apps/web/app/api/webhooks/stripe/route.ts`. 7 test.
  - **SAPMA — webhook `apps/backend`'de DEĞİL, `apps/web`'de.** Görev satırı ve `STACK §7` backend diyor. Ama ödeme onayının yaptığı iş (tahsilat hareketi + durum geçişi + bildirim) uygulama kapılarında yazılı; backend'e taşımak ya o mantığı ikinci kez yazmayı (duplication yasağı) ya da kapıları ortak bir pakete çıkarmayı gerektirirdi. İkincisi doğru hamle ama ayrı bir iş ve iki ajanın açık dosyalarıyla kesişiyor. Gövde ince tutuldu: taşınmak istendiğinde tek dosya taşınır. → `ARCHITECTURE_DECISIONS.md`
  - **İdempotens tek satırda:** `(provider, event_id)` benzersiz. "Önce sorgula, yoksa yaz" arasına ikinci webhook girerse iki işleyici birden "yeni" der ve tahsilat iki kez yazılır — kontrol ile yazım tek ifadede (`ignoreDuplicates`), yarışın kazananı bir tanedir. Testte aynı olay iki kez gönderiliyor, tahsilat 20 € kalıyor.
  - **`processed_at` idempotensin kaynağı değil, İZİDİR:** "geldi ama işlenemedi" ile "işlendi" ayrımı elle tekrar denemenin ve alarmın girdisidir. Hata halinde damga atılmaz, sebep yazılır ve 500 dönülür — Stripe tekrar gönderir.
  - **Tahsilat sipariş toplamından değil, GERÇEKTEN ödenenden yazılır.** İkisi ayrıldığında doğru olan paranın kendisidir; ödeme durumu zaten ondan türetilir (12.2).
  - **Geç ödemede siparişin kararı EN KÖTÜ kalemin kararıdır:** bir kalem bile bulunamıyorsa yarım sipariş göndermek yerine para iade edilir ve sipariş iptal olur (elle karar beklenmez, DOMAIN §4).
  - **Oturum süresi dolduğunda sipariş TASLAK kalır**, yalnız ayrılmış mal bırakılır: müşteri aynı sepetle tekrar deneyebilmeli, iptal onun kararıdır.
- [x] (07.6) **Durum geçişleri + log:** `domain-core` durum makinesini bağlayan servis; her geçiş `OrderStatusLog`'a (from/to/kim/zaman); izinsiz geçiş `{data,error}` ile reddedilir
  - *Bitti:* teslim/kapanış/iptal geçişleri loglanıyor; yasak geçiş hata dönüyor
  - **Durum (27.07):** `0012_order.sql` — sipariş omurgası (`order`, `order_item`, `order_item_batch`, `order_status_log`) + `transition_order_status` RPC · `OrderService`/`OrderItemService`/`OrderStatusLogService` · kapı `apps/web/lib/order/transition.ts`. 12 test.
  - **İki ayrı "hayır" karıştırılmaz:** `forbidden` = geçiş kurallara aykırı (motorun cevabı, ör. `draft → delivered`); `stale` = geçiş uygun ama sipariş artık o durumda değil, araya biri girdi (veritabanının cevabı — depocu "hazır" derken kurye "yolda" demiş). İkisi farklı arayüz davranışı ister: biri hata mesajı, diğeri "ekranı tazele".
  - **Neden RPC:** iki koşul birden (STACK §13). (a) eşzamanlılık — koşullu update olmadan biri diğerini sessizce ezer (10 eşzamanlı testte tek yazan doğrulandı); (b) bölünemez yazım — durum + log birlikte yazılmalı, log düşerse teslim anı ve geri bildirim zamanlaması izsiz kalır.
  - **Zaman damgaları türetilir:** siparişte `delivered_at`/`completed_at` kolonu YOK; teslim anı, kapanış anı ve geri bildirim zamanlaması (~10 gün) `order_status_log`'dan okunur (`firstEntryAt`). Aynı gerçeği iki yere yazmamak için.
  - **Referans numarası bir kez üretilir:** ilk kalıcı durumda (motor karar verir), RPC de `coalesce` ile mevcut numarayı ezmez — çift emniyet.
  - **Kalemsiz sipariş kalmaz:** başlık + kalemler + indirim kullanım kaydı tek transaction'da yazılır (`create_order`, 30.07). Rezervasyon ayrı kalır ve kalmalı: taslak açılırken değil, ödeme başlarken ayrılıyor (sıra DOMAIN §4) ve ayırmanın kendi atomik RPC'si var. Tahsilat da giremez — Stripe'a gidiş ağ üstünden bir dış çağrıdır, veritabanı transaction'ının içinde yeri yoktur.
- [x] (07.7) **Teslim RPC'si:** tek transaction — Reservation düş + Stock fiiliden düş + `OrderItemBatch` (06'daki FEFO onayından) + kâr snapshot'ları (COGS/teslimat/komisyon/paketleme) + `delivery_proof`
  - *Bitti:* teslim sonrası tüm kayıtlar tutarlı; yarıda kesme testi rollback yapıyor
  - **Durum (27.07):** `0016_deliver_order.sql` (`deliver_order` + `close_order`) · `OrderService.deliver/close` · kapı `apps/web/lib/order/fulfillment.ts`. 7 test.
  - **Görev satırından SAPMA — kâr snapshot'ı teslimde değil, KAPANIŞTA.** `DOMAIN §12` birebir şöyle diyor: *"doğrudan gider kalemleri sipariş kapanışında sabitlenir; kapanış = `completed`'a geçiş anıdır"*. Teslim ile kapanış arasında iade/kısmi düzeltme olabilir; maliyeti teslimde dondurmak o düzeltmeleri kârın dışında bırakırdı. Bu yüzden iki fonksiyon: teslim malın **fiziksel gerçeğini** değiştirir, kapanış **parayı sabitler**. (Mimari kazanır — build/README kuralı.)
  - **COGS gerçek maliyettir:** tüketilen partilerin KENDİ alış fiyatından, ortalamadan değil. Testte 4×2 € + 2×3 € = 14 € çıkıyor; ortalama alınsaydı 15 € olurdu.
  - **Stok tam bir kez düşer:** teslim yalnız `out_for_delivery`'den olur (koşullu), ikinci çağrı `stale` döner ve stoğa dokunmaz (testli). Hiç hazırlanmamış sipariş teslim edilirse stok düşmez, teslim kaydı yine yazılır (kısmi karşılama).
  - **`payment_fee` yazılmıyor:** komisyon oranları para modülüyle (12) gelir. Uydurma oranla doldurmak kârı sessizce yanlış gösterirdi — null kalıyor.
- [x] (07.8) **Kısmi karşılama:** `fulfilled_qty` düşümü + para dallanması (peşin → `order_refund` hareketi; kapıda → tahsilat düşer); `payment_status` türetimi (03) uygulanır
  - *Bitti:* eksik + kuponlu sipariş doğru iade tutarını üretiyor
  - **Durum (28.07):** `0020_order_return.sql` (`adjust_fulfillment`) · `OrderService.adjustFulfillment` · kapı `apps/web/lib/order/refund.ts`. 07.9 ile birlikte 14 test.
  - **Ödeme yöntemine bakan `if` YOK.** Görev satırı "peşin → iade, kapıda → tahsilat düşer" diye dallanıyor; kod dallanmıyor çünkü ikisi tek sayının iki işareti: iade borcu = net tahsilat − karşılanan tutar. Peşin ödenmişse pozitif çıkar (fark iade edilir), kapıda ödenecekse sıfır çıkar (`amountToCollect` düşer). Dallansaydı ödeme yöntemi sonradan değişen sipariş yanlış kovaya düşerdi.
  - **İade hesabı sorulmaz, türetilir:** para hangi hesaba girdiyse oradan çıkar (son `order_payment` hareketi). Çağıran isterse başka hesap verir (Stripe'tan tahsil, nakit iade). Hesap türetilemezse iade YAZILMAZ ama düzeltme geçerli kalır — borç `amountToCollect`'te açıkta durur; sessizce yanlış hesaba yazmaktan iyidir.
  - **Kupon payı karşılanan orana bölünür:** 3 adet, 6 € kupon, 5 € kargo, 29 € tahsilat → 2 adet gidince iade **8 €** (10 değil). Bölünmeseydi her kısmi iade müşteri lehine kayardı.
  - **Sıra: önce mal, sonra para.** Tersi olsaydı düzeltme düştüğünde "parası iade edilmiş ama hâlâ karşılanmış görünen" sipariş kalırdı.
- [x] (07.9) **İptal ve iade:** ödenmiş iptal → tam otomatik iade; `returned → completed` kapanışı; iade hareketleri `order_refund` tipiyle; `amount_*` cache güncellemesi (kaynak MoneyMovement)
  - *Bitti:* iptal sonrası `payment_status=refunded`; cache ile hareket toplamı tutuyor
  - **Durum (28.07):** `0020_order_return.sql` (`cancel_order`) + `0019`'da `close_order` artık `returned`'dan da kapanıyor · `OrderService.cancel` · kapı `apps/web/lib/order/refund.ts`.
  - **İptalin sınırı motorda:** yoldaki ve teslim edilmiş sipariş iptal EDİLMEZ, `returned` yolundan gider (durum makinesi zaten böyle diyordu; kapı ona soruyor). İptalde mal hiç çıkmadığı için fiili stok değişmez, ayrılmış geri bırakılır, kalem–parti kaydı silinir — o kayıt "müşteride kalan mal" demektir.
  - **Malın nereye gittiği maliyeti belirler (DOMAIN §8), her kayıp TAM BİR KEZ sayılır:** `restock` → depoya döner (fiili artar, `return_restock` kaydı) ve kalem–parti kaydından düşer, maliyet siparişten çıkar · `discard` (teslim sonrası) → fiiliye dokunulmaz (teslimde zaten düştü, ikinci düşüm çift sayım olurdu), kayıt kalır, maliyet o siparişin kârında görünür · `discard` (hiç çıkmadan, araçta bozuldu) → fiiliden burada düşer + fire kaydı · `goodwill` → miktar da stok da değişmez, tutarı operatör verir (borç türetilemez, mal gitmiş sayılır).
  - **`returned → completed` kapanışı:** iade süreci bitince sipariş kalıcı `returned`'da kalmaz. COGS kendiliğinden doğru çıkar — stoğa dönen mal kalem–parti kaydından düştüğü için hesaba girmez (tamamı iade edilen siparişte `cogs_amount = 0`, testli).
- [x] (07.10) **Hızlı satış yolu (door):** `draft → completed` tek adım; rezervasyon atlanır, fiiliden düşülür; ödeme anında; `reference_no` `completed`'de üretilir
  - *Bitti:* kapı önü satış tek işlemde kapanıyor, stok anında düşüyor
  - **Durum (27.07):** `0017_quick_sale.sql` (`quick_sale`) · `OrderService.quickSale` · kapı
    ~~`apps/web/lib/order/quick-sale.ts`~~ → **`packages/application/src/order/quick-sale.ts`
    (terfi 26.08, mobil şerit · `21.118`)**. 8 test → 10.
    **Taşınma sebebi yüzey kararıdır:** yerinde satış (depo kapısı ve kuryenin aracı) native
    uygulamanın işi oldu (`DOMAIN §17` — *"Admin yerinde satış yapmaz"*), yani çağıran
    `apps/mobile-api` olacak ve o `apps/web/lib`den import edemez. **Kimsenin işini kesmedi:**
    ölçüldü, motorun web'de üretim çağıranı yoktu — tek çağıranı testleriydi. İmza paket kuralına
    uydu: `quickSale(db, input)`.
  - **Adım atlanır, İZ atlanmaz.** Tam yolun yedi adımı burada bir adım; ama kalem–parti kaydı, `reference_no`, geçiş logu ve kâr kalemleri tam yoldakiyle **aynı yerlere** yazılır. Geri çağırma ("bu parti kime gitti") ve gerçek COGS hızlı satışta da çalışır.
  - **Önce kontrol, sonra yazım.** Yetersiz stok bir hata değil, bir **cevaptır** (kasiyer ekranında kalan miktar yazar) — bu yüzden `return` ile bildirilir, `return` transaction'ı geri almadığı için kullanılabilirlik kontrolü tek satır yazılmadan önce biter. Kontrol iki katmanlı: **varyant toplamında** (başkasına ayrılmış mal kapıda satılamaz) ve **parti bazında** (seçilen parti tükenmiş olabilir).
  - **Kapıda hazırlık ekranı yok → partiler FEFO ile türetilir** (06.5 önerisi yeniden kullanılır); operatör isterse elle geçer. FEFO önerisi parti bazında baktığı için varyant-toplamı rezervasyonunu görmez; **son söz RPC'nindir** — emniyet, öneriyi üretenin değil, yazımın olduğu katmanda durur.
  - **`delivery_cost = 0`:** kapı önünde teslimat yapılmadı; rota birim maliyeti bu satışa yazılamaz. **Nakitte `payment_fee = 0`** — bu uydurma değil, olgudur (DOMAIN §12); kart/online oranları modül 12'de, o zamana kadar null.
  - **Ödeme durumu yine TÜRETİLİR** (03.6): kapıda bir kalem eksik verilirse tahsilat tam olsa bile durum kendiliğinden doğru çıkar. Yeni ayar: `door_packaging_unit_cost_cents` (varsayılan **0** — mal elden gidiyor, soğuk zincir paketi yok).
  - **Güncelleme (28.07, 12.2 ile):** tahsilat artık RPC'de yazılmıyor. `p_amount_collected`/`p_payment_status` kaldırıldı; kapı, satıştan sonra `recordOrderPayment` çağırıyor — kapı önü nakdi gerçekten kasanın bakiyesine düşüyor, sipariş cache'i ondan türüyor. Hesap yoksa satış yine kapanır (`paymentRecorded:false`): mal gitmişken satışı bloke etmek de, olmayan parayı "ödendi" yazmak da yanlış olurdu.
- [x] (07.11) **Sağlayıcıya iade (Stripe `refunds.create`):** karta dönen iadenin gerçek çağrısı; `PaymentIntent` kimliğinin siparişten bulunması, kısmi iade tutarı, idempotency, hata halinde hareketin yazılmaması; `charge.refunded` webhook'uyla mutabakat
  - *Bitti:* kartla ödenmiş siparişin iadesi karta dönüyor; çağrı düşerse hareket yazılmıyor; panelden yapılan iade deftere düşüyor
  - **Neden ayrı bir satır:** 07.9 iadenin **muhasebesini** kapattı — hareket doğru hesaba, doğru tutarla yazılıyor ve `payment_status` ondan türüyor. Kapanmayan şey paranın **fiilen dönmesi**ydi: "karta geri" seçilen bir iadede hareket Stripe hesabına düşüyor, kart hiç görmüyordu.
  - **Durum (30.07):** port `apps/web/lib/order/provider-refund.ts` · sıra `lib/order/refund.ts` (`settleRefund`) · mutabakat `lib/order/stripe-webhook.ts` · ekran `operations/orders/[id]`. 12 test.
    **SIRA TERSİNE ÇEVRİLEMEZ: önce sağlayıcı çağrısı, sonra hareket.** Ters sırada başarısız bir iade defterde kapanmış görünür, para dönmemiş olur — ve hiçbir ekranda iz bırakmaz. Çağrı düşerse hareket HİÇ yazılmaz.
    **Ödeme künyesi TAHSİLATTA saklanır** (`money_movement.meta.providerRef`). Siparişe değil harekete yazılır: referans o ödemenin künyesidir, siparişin değil (bir siparişin birden çok tahsilatı olabilir). İade anında sağlayıcıda `metadata` ile arama yapmak (`search`) gecikmeli çalışır ve güvenilmez. Künye yoksa iade **tahmin edilmez** — yanlış niyete yapılan bir iade başka bir müşterinin parasını geri gönderirdi.
    **Sağlayıcı çağrısı HESABIN TÜRÜNE bağlı**, siparişin ödeme yöntemine değil: operatör kartla ödenmiş bir siparişi kasadan nakit iade etmeyi seçebilir (`refundAccountId`) — o zaman dönülecek bir sağlayıcı yoktur.
    **Sessiz sıfır yasak:** iade yazılamadığında sonuç `refundBlocked` ile sebebini söyler ve ekran sarı bir şeritle operatöre ne yapacağını yazar. Sessizce 0 dönmek "borç yoktu" ile aynı görünürdü; operatör iadeyi yapılmış sanır, müşteri parasını beklerdi.
    **Yeniden deneme AYRI bir kapı** (`retryRefund`): sağlayıcı düştüğünde düzeltme/iptal zaten yazılmıştır ve geri alınmaz — `cancelOrder` ikinci kez koşamaz, `adjustFulfillment` koşarsa adetleri ikinci kez uygular. Bu kapı olmasaydı ekrandaki "tekrar deneyin" cümlesinin karşılığı olmazdı. Borç yeniden türetilir: aradan başka bir hareket geçtiyse iade ikinci kez yazılmaz.
    **Mükerrer iade anahtarı** sıra numarası + tutardan üretilir: aynı iadenin tekrarı aynı anahtarla gider (Stripe ilk sonucu döner, para iki kez çıkmaz), gerçekten yeni bir kısmi iade farklı anahtar taşır.
    **`charge.refunded` mutabakatı** iki yolu da doğru işler: biz başlattıysak toplamlar zaten eşittir → yazılmaz (yoksa iade iki kez düşerdi); panelden elle yapıldıysa **fark** deftere düşer. Olay FARK değil TOPLAM taşır — olay tekrar gelirse fark iki kez yazılırdı. Sipariş, olaydaki künyeden değil bizim sakladığımız `providerRef`'ten bulunur: `charge` metadata'sı niyetinkini her zaman taşımaz.

- [~] (07.12) **Taşıyıcı + kargo takip numarası:** `order.carrier` (tanımlı küme: `colissimo · chronopost · dhl · ups · other`) + `order.tracking_number`; ikisi de yalnız `delivery_type = 'shipping'` siparişlerde anlamlı — kısıt veride (rota siparişine takip numarası yazılamaz). Numarayı hazırlık ekranı girer (paketi kapatan kişi etiketi elinde tutar), ayrı sevk adımı açılmaz. Takip bağlantısı taşıyıcının URL kalıbından üretilir; `other` seçilirse bağlantı gösterilmez, numara düz metin durur — touches: `supabase/migrations/`, `packages/types/`, `apps/web/app/(operations)/`, `apps/web/app/(customer)/`
  - *Bitti:* müşteri sipariş listesinde ve detayında takip numarasını görüyor; rota siparişinde bölüm hiç çizilmiyor
  - **Neden tanımlı küme:** serbest metinden takip bağlantısı üretilemez, ve üretilemeyince tasarımdaki "Kargoyu takip et ↗" düğmesinin karşılığı olmaz. `other` kümeyi kapatmamak için var — yeni taşıyıcı bir migration bekleyemez.
  - **Kaynak:** müşteri yüzeyi şeridinin talebi (`yer-ekseni-arka-uc-talebi.md` §5). 19 kapsamı DEĞİL: kargo çok depodan önce de vardı, alan eksikliği bağımsız bir açıktı.
  - **Durum (01.08):** `carrier` enum'u (`colissimo · chronopost · dhl · ups · other`) + `order.carrier` + `order.tracking_number` + `OrderService.setShipment`. **Kısıt VERİDE:** `order_carrier_only_shipping` — rota siparişine taşıyıcı yazılamaz. Ekran unutabilir, veritabanı unutmaz; unutulduğunda müşteri hiç çalışmayacak bir takip bağlantısı görürdü. 2 entegrasyon testi (biri kısıtın fiilen reddettiğini çiviyor).
  - **`other` kümeyi kapatmamak için var:** yeni bir taşıyıcıyla çalışmaya başlamak migration beklememeli. Seçilince bağlantı gösterilmez, numara düz metin durur — takip URL kalıbı yalnız tanımlı taşıyıcılarda var.
  - **BEKLEYEN(08.5):** ~~müşteri sipariş detayındaki "Kargoyu takip et ↗" bölümü~~ **yazıldı (09.08)** — künye masaüstünde Teslimat kartında, mobilde kendi kartında; takip adresi `lib/order/carrier.ts`'te ve `other` için `null`, yani yukarıdaki *"bağlantı gösterilmez, numara düz metin durur"* kuralı ekranda birebir karşılandı. **Kalan yarı:** hazırlık ekranının numara girişi — operasyon şeridinin işi, `setShipment`'in bugün HİÇ çağıranı yok (ölçüldü 09.08: yalnız testler çağırıyor), yani numarayı sisteme sokan bir yüzey henüz yok ve seed'deki dört kargolu sipariş dışında veri doğmuyor.
    - **Kullanıcı kararı (09.08): elle giriş AYRICA yazılmaz, bu görevin turunda taşıyıcı seçimiyle AYNI hamlede gelir.** Soru şuydu: *"normal şartlar altında kargo satın alındığında takip numarası bağlanan servisten otomatik gelmeyecek mi?"* — gelecek (aşağıdaki Sendcloud tasarımı), yani bugün ayrı bir elle-giriş turu açmak aynı formu iki kez yazmak olurdu. **Ama elle giriş kanal geldikten sonra da SİLİNMEZ:** `other` taşıyıcı meşru bir seçimdir ve kanal dışı gönderi numarasını Sendcloud'dan almaz, düzeltme (yanlış/iptal edilmiş etiket) de elle yapılır. Yedek şerittir, geçici yama değil.
  - **KARGO KANALININ TASARIMI VERİLDİ (04.08, müşteri şeridinin hazırlık kaydı) — kod bu görevin turunda inecek.** Sağlayıcı referans projedeki ile aynı: **Sendcloud**. Karar önden yazılıyor ki o gün mekanik bir ekleme olsun (`CLAUDE §4`).
    - **Ölçü/ağırlık `product_variant`'a, ürüne DEĞİL:** ölçü satılan şeyin özelliğidir — ürün bir kavram, varyant bir pakettir. Aynı ürünün 1 kg ve 500 g'ı farklı kolilenir; ürüne koymak 500 g'lık kutuyu 1 kg'ın ölçüsüyle fiyatlandırırdı. Alanlar `weight_g` · `length_mm` · `width_mm` · `height_mm` — **milimetre**, çünkü ondalık kalınlık (1,5 cm) tam sayı alanında sessizce yuvarlanır.
    - **Ölçüsüzlük `null`'dur, sıfır değil** (`CLAUDE §1`) ve koli planı yedek sabite düştüğünü SÖYLEMELİ. Bu kozmetik değil: hacimsel ağırlık (`en×boy×yükseklik ÷ 5000`) barajı **yukarı** doğru işliyor — hangisi büyükse onun tarifesi uygulanıyor — ve Mondial Relay ağırlık hatasında tarife farkına ek **15 € idari ücret** yazıyor. Referans projedeki sessiz yedek (`19×10×13 cm`) tam olarak bu riski üretiyor.
    - **Sendcloud istemcisi kendi paketine (`@lezzet/sendcloud`) ve bir PORT arkasına girer** (`ShippingRateProvider`), `packages/ai` deseniyle: sağlayıcı bir uygulamadır, sözleşme değil; test sahte sağlayıcıyla ağa çıkmadan koşar.
    - **Koli planı `domain-core`'a** (saf karar). Referanstan iki şey ALINMAYACAK: sabit adet böleni (`ceil(Σadet / N)` — kupa hepsi aynı boyken doğruydu, bizde 250 g kutu ile 5 kg toptan koli aynı sayıda sığmaz; ölçüt **hacim + ağırlık tavanı**) ve `STACK_LIMIT` üstü "toplu koli dış ölçüsü" varsayımı (donmuş ürün üst üste konur ama **ezilir**; hasar talebi kuyruğun en pahalı kalemi).
    - **Fiyat modeli hibrit:** canlı teklif + eşik üstü ücretsiz kargo. Ölçümlerde sabit/canlı arasında net kazanan yok; ayrım katalogun homojenliğinde ve **bizim katalog çeşitli**. `resolveShippingFee`'nin GİRDİSİ değişir, motor değişmez; eşik mekanizması (`free_shipping_threshold`) aynen kalır.
    - **İstemci hangi seçeneği seçtiğini söyler, KAÇA olduğunu söylemez** — fiyat sipariş anında sunucuda yeniden hesaplanır. Aksi hâlde checkout'a `priceEur=0` göndermek mümkün olurdu; `createCheckoutDraft` zaten bu kuralı uyguluyor, kargo teklifi de aynı kapıdan geçer.

  - ✅ **DURUM (28.08) — KANAL KURULDU: ölçü → kutu → plan → teklif → duyuru → etiket zinciri çalışıyor.** Tasarım kaydı `kargo-kanali-tasarimi.md`, çalışma günlüğü `kargo-kanali-gunluk.md`.
    - **Ambalajlı ürün ölçüsü** varyanta yazıldı (`05.38`): `packed_weight_g` + üç `*_mm`. Giriş iki kapıdan — operatör formu ve MCP dilekçesi; araç künyesi modele *"tahmin etme"* diyor (ölçü ambalajda yazmaz, tartılır).
    - **Kargo kutusu kataloğu** (`0052`): tek tablo, sistem kutusu ŞABLON ve depo benimserken KOPYALANIR. Kural veride — `order_box`a bileşik FK, şablon da başka deponun kutusu da seçilemiyor.
    - **Koli planı** (`domain-core/delivery/parcel-plan.ts`): ölçüt hacim + ağırlık tavanı, adet değil. **Ölçüsüz kalem planı DURDURUR — yedek sabit YOK** (07.12'nin kuralının bir adım ötesi).
    - **`@lezzet/sendcloud`**: REST v3, resmî SDK olmadığı için kendi istemcimiz. **POST yeniden denenmez** (idempotency anahtarı yok — ikinci çağrı ikinci koli açar, gerçek para). Gram/mm dönüşümsüz gidiyor.
    - **Teklif TEK KAPIDAN** (`application/shipping/quote.ts`). ⚠ **Çok kutulu siparişte `multicollo` desteklemeyen seçenek listeden düşüyor** — canlı ölçümde 17 seçeneğin 10'u destekliyor ve **Mondial Relay'in hiçbiri desteklemiyor**, üstelik en ucuz üçün ikisi o. Süzgeç olmasa etiket satın alma anında reddedilir, sipariş sevk edilemez kalırdı.
    - **Gönderi + olay defteri** (`0053`): sipariş kutusu = taşıyıcının kolisi (kullanıcı kararı 28.08), ayrı koli varlığı yok. İki kimlik uzayı baştan ayrı (`provider_shipment_id` ≠ `provider_parcel_ref`). Duyuru kapısı altı ön koşulu ÇAĞRIDAN ÖNCE ölçüyor ve **sağlayıcı düşerse hiçbir satır yazmıyor** (referansın "öksüz koli" runbook'unun sebebi tersiydi).
    - **CANLI DOĞRULAMA:** `pnpm sendcloud:smoke` gerçek hesapla 17 seçenek döndürdü (para harcamayan teklif çağrısı). Depolar ekranında kutu benimseme tarayıcıda denendi.
    - **Testler:** 13 + 20 + 6 birim · 11 + 7 + 9 entegrasyon. Tam paket yeşil.
    - **KALAN (kanal açık ama uçları bağlanmadı):** ~~*(a)* checkout/sepet ekranlarının canlı teklifi göstermesi~~ → **yapıldı 07.12**; *(b)* etiket basımı (23.7 zinciri; `printPDF` ölçüldü ve var, kalan iş çağıranı + fiziksel prova); ~~*(c)* webhook + durum zinciri~~ → **yapıldı 07.12**; ~~*(d)* takılı/öksüz gönderi nöbeti~~ → **yapıldı 07.12**; *(e)* native ekranlar (mobil şeride talep).
    - **E2E (29.08):** `e2e/operations/cargo-shipment.smoke.ts` — çok kolili gönderi kutu başına
      takip numarasıyla çiziliyor · kargo kulvarında kurye/sefer satırları çizilmiyor · **taşıyıcı
      TESLİM deyince sipariş teslim oluyor**. Kargo kanalının ilk uçtan uca kanıtı: o güne kadar her
      halkanın kendi testi vardı ama hiçbiri BİR ARADA koşmuyordu. Sağlayıcı sahte — ağ yok, para
      harcanmıyor.
      **İkinci senaryonun ilk hâli YANLIŞ SEBEPTEN geçiyordu** (ölçüldü): sayfada "Teslim edildi"
      metni aranıyordu, oysa aynı cümle GÖNDERİNİN durumu için de basılıyor — zincir koparıldığında
      test yine yeşildi. İddia siparişin kendi rozetine daraltıldı; artık koparınca kırmızıya
      dönüyor.
    - **Durum (28.08 · webhook + durum zinciri + nöbet yazıldı, testli):** kargo siparişi artık `ready`de takılı kalmıyor. Uzlaştırma tek kapıda (`application/shipping/sync-status.ts`) ve **webhook da nöbet cron'u da oradan geçiyor**: taşıyıcı olayı `handed_over`/`in_transit`/`out_for_delivery` gösterirse sipariş yola çıkar, **tüm koliler** teslim olunca `deliver_order`dan teslim edilir. Ara adım atlanmaz — RPC yalnız `out_for_delivery`den teslim ediyor.
      **`returned`/`error` siparişe YAZILMAZ ve bu bilinçli:** iade stok ve paraya dokunur, stok etkisi de malın FİZİKSEL depoya dönüşüne çıpalıdır (`DOMAIN §4`). Taşıyıcının "gönderene dönüyor" demesi malın depoda olduğu anlamına gelmez. Gönderi durumu ve ham kod deftere yazılır, kararı operatör verir.
      **Durum eşleme tablosu ÖLÇÜLDÜ ve baştan yazıldı:** `GET /api/v3/parcels/statuses` taksonominin tamamını veriyor (35 kod, HTTP 200) — ilk yazımın *"kamuya açık liste yok"* varsayımı yanlıştı. Sezgisel (metin araması) tablo gerçek listeye karşı koşturulunca **yedi kod yanlış, on biri tanınmıyor** çıktı; en tehlikelisi `CANCELLATION_FAILED` → `cancelled` (iptal EDİLEMEDİ demekken koliyi defterden düşürüyordu) ve `SHIPMENT_ON_ROUTE`/`DRIVER_ON_ROUTE` tanınmadığı için siparişin `out_for_delivery`ye HİÇ geçememesiydi.
      **Üçüncü cevap eklendi (`recognized`):** "kodu tanımıyorum" ile "kodu tanıyorum ama yer söylemiyor" (adres değişti, iptal sürüyor) ayrıldı. Ayrım olmasaydı her adres değişikliği "tanınmayan kod" alarmını şişirir, hep açık duran bir alarm da alarm olmaktan çıkardı.
      **Webhook `apps/backend`'de** (`/webhooks/sendcloud`) — Stripe'ınki web'de ama o künyesinde gerekçesi yazılı bir sapma; burada ters yönde bir bağ var: aynı uzlaştırmayı nöbet cron'u da çağırıyor ve o zaten bu süreçte. İmza HAM gövde üzerinden HMAC-SHA256 (sağlayıcının yayımladığı vektörle sınandı). **İşlenememiş olayın tekrarı yeniden denenir** — damgasız kayıt "işlendi" sayılmaz; Stripe kapısı koşulsuz `duplicate` diyor, bu kulvarda ayrım gerekli çünkü en olası düşüş sebebi geçici.
      **İki nöbet:** takılı gönderi (saatlik, `:25`) ve öksüz/hayalet mutabakatı (haftalık, pazartesi). İkincisi yalnız TESPİT eder — düzeltme elle, runbook `docs/runbook/kargo-oksuz-gonderi.md`.
      **Ölçülerek bulunan bir hâl çivilendi:** kutusuz gönderi ilerlemez. Uzlaştırma BİZİM kutularımız üzerinden yürüyor; kutusu olmayan gönderide "en gerideki koli" yoktur, cevap "bilmiyorum"dur ve nöbet onu takılı raporlar. Sağlayıcının dizisine düşülseydi kutuları kaybolmuş bir gönderi sessizce teslim sayılabilirdi.
      **Takip müşteriye ULAŞTI (28.08, ikinci tur):** künye tek kapıdan (`shipping/tracking.ts` →
      `readOrderTracking`) ve üç yüzey de oradan besleniyor — "yolda" maili, müşteri sipariş detayı
      ve `/api/v1/me/orders/:ref`. Öncesinde `notification-data.ts` `tracking: null` sabitini
      taşıyordu: e-posta şablonu takip kutusunu çiziyordu ama kaynağı hiç yoktu.
      **Şema DİZİYE çevrildi** çünkü multicollo'da her kolinin ayrı numarası var; tek numara basan
      mail üç kutulu siparişin ikisini görünmez kılardı. Kutu sırası (`"2/3"`) dilden bağımsız
      seçildi — üç dile sözlük satırı eklemek yerine rakam çifti; tek kutuluda hiç basılmıyor.
      **İki meşru kaynak:** duyurulan gönderi konuşur, yoksa hazırlık panelinden elle girilen
      numaraya düşülür (sağlayıcının kapsamadığı taşıyıcı için bilinçli kapı); ikisi de doluysa
      gönderi kazanır. Elle girişte takip bağlantısı taşıyıcı kalıbından üretilmeye devam ediyor —
      atlansaydı bugün çalışan bir düğme sessizce kaybolurdu.
      **Mobil sözleşmesi EKLEMELİ genişledi** (`carrierName` + `parcels`): eski üç alan native
      ekranı kırmamak için duruyor ve İLK koliyi anlatıyor; native geçince silinecek
      (`docs/talep/not-mobil-cok-kutulu-kargo-takibi.md`).
      **Operasyon yüzeyi (28.08, üçüncü tur):** sipariş detayının Teslimat kartında taşıyıcı + gönderi
      durumu + **kutu başına** takip (tıklanabilir). Kaynak müşteri yüzeyiyle AYNI kapı — operatörün
      gördüğü numara ile müşteriye gösterilen ayrışamaz. Aynı turda ölçülen bir arıza da düzeldi:
      kargo siparişinde *"Kurye: sefer bekliyor / Sefer: açılmadı"* satırları sonsuza dek duruyordu
      (o iki satır rota kulvarının), artık kargo kulvarında çizilmiyor.
      **Tanınmayan kod SAYAÇ değil HATA KAYDI oldu:** tasarım `/operations/system`de sayaç
      öngörüyordu; yazarken görüldü ki sayaç "kaç tane" der, operatörün ihtiyacı "hangi kod"dur.
      `error_log`'a `warning` düşüyor — kod başına gruplanıyor ve çözülene kadar duruyor; sayaç
      pencere geçince sıfırlanırdı. `countUnmapped` çağıranı doğmadan silindi.
      **Besleme:** `shipment` tablosunda TEK satır yoktu, yani üç ekran da yalnız testlerde
      görülebiliyordu. Seed artık iki gönderi kuruyor — tek koli (taşıyıcıda) ve **çok koli** (yolda,
      iki ayrı takip numarası); `seed:coverage`'a "Kargo gönderisi" kovası eklendi. Çok koli kovası
      ZORUNLU: "her kolinin ayrı numarası" kuralı tek kolili veride hiçbir ekranda görünmez ve o
      kural bir kez tam bu yüzden yanlış yazılmıştı. Seed sağlayıcıya ÇIKMIYOR (duyuru gerçek para
      harcar) — satırlar doğrudan yazılıyor, sağlayıcı kimlikleri `seed-` önekli.
      **AÇIK — `.env.example`'a Sendcloud anahtarları yazılmadı** (`SENDCLOUD_PUBLIC_KEY` ·
      `SENDCLOUD_SECRET_KEY` · `SENDCLOUD_API_BASE_URL` · `SENDCLOUD_WEBHOOK_SECRET`). Dosya o an
      başka şeridin değişikliğini taşıyordu; yol adı vererek commit atmak onların satırlarını benim
      künyemin altına alırdı (`CLAUDE §0`). Dosya temizlendiğinde eklenecek — anahtar listesi
      olmadan yeni bir ortam kurmak, hangi değişkenin gerektiğini koddan aramak demek.
      **TAŞIYICI SEÇİMİ (kullanıcı kararı 29.08):** *"Teslimat noktasına kullanıcı kendisi seçiyorsa
      ve kargo parası siparişin üzerine ekleniyorsa olabilir. Ama eşiği geçtiyse ve kargo ücretsiz
      diyorsak evine teslim senaryosu devrede."* Ayıran şey PARANIN KİMDEN çıktığı — kural
      `domain-core/delivery/shipping-choice.ts`te (`requiresHomeDelivery` · `homeDeliveryOnly`).
      **Kural SEVKTE bağlayıcı, checkout'ta değil** ve bu ölçümle belirlendi: müşterinin seçtiği
      servis kodu HİÇBİR YERE yazılmıyor (29.08), taşıyıcıyı sevk anında depo seçiyor
      (`quoteOrderShipment`). Yalnız checkout'a koysaydık kuralı söylemiş ama uygulamamış olurduk.
      Checkout tarafı SORMAMA kısmı: eşik üstünde liste çizilmiyor, "Ücretsiz kargo — adresinize
      teslim" yazıyor (üç dil). Son adımı BİLİNMEYEN seçenek eve teslim sayılmaz.
      **AÇIK — eşik üstünde teklif çağrısı hâlâ yapılıyor ve sonucu kullanılmıyor:** ücret zaten
      sıfır, kod da saklanmıyor. Eşik `resolveCheckoutPayment` içinde okunduğu için tekliften ÖNCE
      bilinmiyor; çağrıyı atlamak sırayı değiştirmeyi ister. Bedeli sağlayıcıya boş bir tur
      (~300-500 ms) — ölçülüp kaydedildi, yamayla geçiştirilmedi.
      **ERTELENDİ — onaylı taşıyıcı listesi ve azami teslim süresi** (kullanıcı kararı 29.08:
      *"muallak bir konu, sona bırak, backlog'a not düşelim"*). Ölçüm yapıldı ve `BACKLOG §8`'e
      tabloyla yazıldı: Fransa içinde ham bir süre süzgeci bedelsiz (en ucuz zaten 48 sa), ama
      Almanya içinde 40 seçeneğin 21'i süresiz ve **en ucuz altısı da süresiz** — orada aynı süzgeç
      gönderi başına **1,82 €** pahalıya mal oluyor. Ayrıca süre YÖNE bağlı (FR içi · DE içi ·
      FR→DE üç ayrı süre), yani tek bir "azami 3 gün" ayarı üç yönü birden yönetemiyor.
      **Kapanış (`completed`) BU TURUN KONUSU DEĞİL** — ölçüldü: `closeOrder`ın bugün hiçbir üretim çağıranı yok, rota kulvarında da yok. Kargoya özel bir kapanış yazmak iki kulvarı ayrı kurallara bölerdi → görev satırı `(07.16)`.

- [x] (07.13) **Bağlayıcı fiyat sabitlenirken artış SESSİZ uygulanıyor** · `touches: apps/web/lib/order/checkout-draft.ts, apps/web/lib/cart/{cart-types,actions}.ts, apps/web/app/(customer)/[locale]/checkout/**`
  - *Bitti:* checkout ekranı açıkken fiyat artarsa sipariş açılmıyor; müşteri eski→yeni tutarı görüyor ve tekrar onaylayınca sipariş yeni fiyattan yazılıyor
  - **Ölçüldü, tahmin değil (07.08 · e2e senaryo taraması):** kural sepette işliyor — `getCartView`'un `previousPrices` parametresi artışı yakalıyor ve satıra `priceChange` yazıyor (`lib/cart/price-change.test.ts`, DOMAIN §5: *artarsa bildirilir, düşerse sessiz uygulanır*). `createCheckoutDraft` bu parametreyi **geçmiyor** (`checkout-draft.ts:182-187`) ve reddetme hâlleri arasında bir `price_changed` yok (`:43-85`). Yani karşılaştırma, fiyatın **bağlayıcı olduğu** anda hiç yapılmıyor.
  - **Neden önemli:** kural dokümana Fransız tüketici hukuku gerekçesiyle girdi — sessizce bozulması en pahalı sınıf, çünkü bozulduğunda hiçbir şey patlamaz, müşteri yalnız beklemediği bir tutar öder. Pencere dar (checkout ekranının açık kaldığı süre) ama sıfır değil: teklif fiyatları partiye çıpalı ve o partiler zaten tükenmek üzere olanlar.
  - **Yön:** `createCheckoutDraft` sepeti okurken saklanan fiyatları da geçirsin; artış varsa `price_changed` ile reddetsin (kalem adı + eski/yeni tutar). Ekran farkı gösterip yeniden onay istesin — sepetteki `priceChange` bloğunun cümlesi zaten var, ikinci bir sözlük gerekmiyor.
  - **Durum (07.08 · yazıldı, testli):** taslak sepeti artık `previousPrices` ile okuyor; artış varsa **en sonda** `price_changed` döner (kalem adı + eski→yeni). Kapının yeri bilinçli: kalemi alınamayan ya da günü kapanmış bir sipariş zaten açılmayacak, önce onay isteyip sonra "zaten olmuyor" demek müşteriyi iki kez durdururdu.
    **Reddederken saklanan fiyatlar GÜNCELLENİR — bu şart, süs değil.** Karşılaştırmanın "önceki"si sunucu sepetindeki fiyat ve onu yalnız `writeCartAction` tazeliyor; yazmasaydık müşteri uyarıyı okuyup tekrar onayladığında aynı reddi alırdı ve **hiçbir yerde hata görünmeden sipariş veremezdi** (sonsuz döngü). Kural sepettekiyle birebir aynı davranışa oturdu: *bir kez bildir, sonra sakla.* Testin biri yalnız bunun için var.
    **Düşen fiyat buraya hiç gelmez** (`priceChangeOf` yalnız artışta işaret koyar) — indirim için müşteriyi durdurmak, hediyesi yüzünden durdurmak olurdu.
    **İki yardımcı ortak eve taşındı** (`cart-types.ts`): `storedPrices` ve `itemOfEntry`. İkisi de `'use server'` dosyasında özeldi; `CLAUDE §2`'ye göre uç bir sözlük değildir ve ikinci tüketici doğduğu an kopyalanacaklardı — o zaman iki "önceki fiyat" tanımı olurdu.
    **`price_changed` ölçülmüyor ve bu bilinçli:** `checkout_blocked` sebep kümesi tiplidir (`not_shippable · out_of_stock · min_basket`); zam o sözlüğün konusu değil — müşteri engellenmiyor, onayı yenileniyor. Gerekçe `measureRejection` künyesinde yazılı.
    **Ekran tarafına kod GEREKMEDİ:** ret metni jenerik yoldan çiziliyor (`t.rejected[reason]` + `{detail}`), üç dilde cümle eklendi. Tutar `formatPrice` ile biçimlendi — ekranınkiyle aynı biçimlendirici, yani "12,50 €" ile "€12.50" ayrımı tek yerde.
    **Kanıtlandı:** karşılaştırma geçici kapatılınca iki test de düşüyor (`expected 'ok' to be 'price_changed'`) — testler gerçekten yakalıyor. 17/17 yeşil.

- [x] (07.14) **İptal edilen sipariş "tahsilat yapılmadı" diyor — parası iade edilmiş olsa bile** · `touches: apps/web/app/(customer)/[locale]/checkout/[reference]/**, apps/web/app/(customer)/[locale]/checkout/actions.ts`
- [~] (07.15) **Ayar KAPSAMI sepete/checkout'a bağlanmamış — bölge/kanal/ülke satırları hiç uygulanmıyor** *(uç senaryo hattının İLK CANLI KOŞUSUNUN bulgusu, 08.08 — `edge-min-basket.smoke.ts` kırmızı ve nöbette)*: `SettingsService.get` kapsam çözümü (bölge → kanal → ülke → global) çalışıyor ama `cart/read.ts:82-88` KAPSAMSIZ, `checkout-options.ts:70` `{ channel: undefined }` ile okuyor. Sonuç: b2b asgari sepet (120 €) ve b2b bedava-kargo eşiği (250 €) bireysel değerlerle EZİLİYOR, DE kargo ücreti (12,90 €) ve DE eşiği (90 €) yerine FR değerleri kesiliyor, bölge satırları ölü — seed'in "bölge kanalı ezer" öncelik sınavı hiç koşmuyor. Kablolar mevcut bağlamdan çekilir: `warehouseId` sepete zaten geliyor (19.9), kanal viewer'da, ülke/bölge yer çözümünde · `touches: apps/web/lib/cart/read.ts, apps/web/lib/order/checkout-options.ts, apps/web/lib/delivery/**`
  - **Durum (08.08 · arka uç) — ~~KABLOLAR BAĞLANDI~~ KAPSAM KURUCUSU HAZIR, İKİ EKSEN HÂLÂ BOŞ.** `settingScopeOf` (`apps/web/lib/settings-scope.ts`) tek kurucu ve kanal + depo eksenlerini gerçekten taşıyor. Anahtar ortaklığı 29.07'de yetmemişti; kapsam da ortak olmak zorundaydı.
  - ⚠ **DÜZELTME (08.08, aynı gün · `knip` bulgusu):** yukarıdaki "sepet ve checkout AYNI ekseni geçiriyor (kanal + ülke + bölge + depo)" cümlesi **YANLIŞTI.** Ölçüldü: `getCartView`'a `country`/`zoneId` geçiren **tek bir çağıran yok** — üçü de yalnız `readPlaceWarehouses()` yayıyor (`lib/cart/actions.ts:227` · `lib/order/checkout-draft.ts:203` · `app/(customer)/…/checkout/actions.ts:102`). Yani `readPlaceScope` yazıldı ve **hiç çağrılmadı**; DE kargo tarifesi ve bölge asgari sepeti hâlâ okunmuyor. Bu, aynı gün koleksiyon süzgecinde yaşananla **aynı sınıf hata**: tüketicisi olmayan bir kapı "bağlandı" diye teslim edilmiş.
  - ~~**Kalan ayak ÜÇ ÇAĞIRANIN HEPSİNDE AYNI TURDA yapılır**~~ → **YAPILDI (09.08).** Üçü de bağlandı: `lib/cart/actions.ts` (çerezten) · `lib/order/checkout-draft.ts` (adresten; `zoneId` kargo siparişinde `null` — kargo siparişi bir bölgeye ait değildir, `:350` ile aynı kural) · `app/(customer)/…/checkout/actions.ts` (adresten, müşteri şeridi 09.08'de yazdı ve iki fazlı deseni kurdu). `BEKLEYEN(07.15)` işareti kalktı; **`knip` artık `readPlaceScope`'u göstermiyor** — 08.08'den beri duran açık kapandı.
  - ⚠ **Bağlarken bir GERİLEME önlendi.** `readPlaceScope` 07.15'te üç alan dönüyordu (`country`/`zoneId`/`warehouseId`) ve o gün doğruydu: rota dışı müşteride `warehouseId` zaten kargo deposunu taşıyordu. **19.23 o kutuyu daralttı** (yalnız rota deposu). Kapı olduğu gibi bırakılıp `readPlaceWarehouses`ın yerine geçseydi rota dışı müşteri sepette kargo havuzunu SESSİZCE kaybederdi — düzeltmenin kendisi bir gerilemeye dönüşürdü. Kapı `shippingWarehouseId`i de döndürüyor.
  - **Ölçüldü (09.08, gerçek ayar satırlarıyla)** — bağlamanın etkisi:
    ```
                               min_basket   kargo   bedava_kargo_eşiği
    ÖNCE (yalnız depo)              0 €     7,90 €       60 €
    sepet · FR rota içi            45 €     7,90 €       60 €     ← bölge satırı ARTIK uygulanıyor
    DE müşteri                      0 €    12,90 €       90 €     ← ülke satırı ARTIK uygulanıyor
    checkout · KARGO siparişi       0 €     7,90 €       60 €     ← bölge yok, doğru
    ```
  - ✔ **YENİ GÖRÜNEN BİR KARAR — çözüldü (kullanıcı kararı 09.08): `min_basket_cents`te EN KATISI kazanıyor.** Kapsam önceliği `warehouse → zone → channel → country → global` ve "en dar kazanır" diyordu. `zoneId` bugüne dek hiç geçirilmediği için `zone` satırı **hiç eşleşmiyordu** ve kanal kazanıyordu; bağlandığı anda o bölgedeki **B2B müşteri 120 € yerine 45 €** asgari sepet görmeye başladı (ölçüldü). Hata değil — gözlenemeyen bir önceliğin ortaya çıkması; ama parayı ilgilendiriyor.
    - **Kullanıcının sorusu kararı belirledi:** *"bu 45 € özellikle B2B için mi belirlenmiş? Öyleyse kalmalı."* Ölçtüm: **değil.** Satır `scope_type: zone`; `settings` tek eksen taşıyor, "bölge + kanal" diye birleşik kapsam yazılamaz. Yani 45 €, o bölgeye düşen herkes içindir.
    - **İki satır rakip değil, iki AYRI koşul:** `channel: b2b` 120 € bir TİCARİ ŞART (toptan fiyat vermenin karşılığı, mesafeyle ilgisi yok); bölge satırı bir LOJİSTİK TABAN (aracın o tura çıkması anlamlı olsun). İkisi de karşılanmalı → `min_basket_cents` artık eşleşen kapsamların **en yükseğini** döndürüyor (`SettingsService.STRICTEST_WINS`). Kısa bir tur, kimseye toptan şartları hediye etmiyor.
    - **Ödünleşme yazılı:** bu anahtarlarda eşik dar kapsamda **yükseltilebilir ama düşürülemez**. Bugün kayıp yok (global satır 0). **Fiyat ayarları listede değil** (`shipping_fee_cents`): Alman müşteri Almanya tarifesini öder, "en pahalısını" değil.
    - **Kural bölge ↔ kanal ile sınırlı değil**, kullanıcının kendi ifadesiyle: *"bölge ve depo hangisi daha yüksekse onu seçelim… daha büyük olanı seçmek mantığı hâlâ mantıklı geliyor."* Uygulama tüm kapsam eksenlerinde aynı — eşleşen ne varsa en yükseği.
  - ⚠ **BÖLGEYE ÖZEL ASGARİ SEPET GERÇEK BİR KURAL DEĞİL — kullanıcı bildirimi 09.08:** *"ona göre fiyat verme diye bir şey yok benim bildiğim."* Satır yalnız beslemede var ve mekanizmayı örneklemek için kondu. **Kullanıcı kararı: kalsın** ("ileride kullanabilirim"), ama açıklaması artık ne olduğunu söylüyor: *"ÖRNEK — … Mekanizma gösterimi; gerçek bir iş kuralı değil"*. Gerekçe bugün yaşandı: satırın eski künyesi *"uzak bölge"* diyordu, kimse öyle bir kural koymamıştı ve o lafın peşinde vakit gitti. **Bölgeye bağlı GERÇEK ayar `order_cutoff_time`**: rota günleri bölgeye göre değişiyor, kesim saati de onunla.
  - ⚠ **Aynı turda bir BESLEME uydurması düzeltildi — "uzak bölge" diye bir şey yokmuş.** Kullanıcı sordu (*"uzak bölge ne demek?"*) ve ölçüm cevapsız bıraktı: `delivery_zone` yalnız ad · depo · haftalık günler · aktiflik tutuyor — **mesafe yok, maliyet yok, "uzak" işareti yok**. Bir bölgeye yüksek asgari sepet koymak bir hesaplama değil, operatörün kararıdır; seed onu taklit ediyor. Ama seçim `zones[2]` ile yapılıyordu ve künyesi *"adı değil SIRASI seçilir"* diyordu — o sıra **alfabetik** (`orderBy: 'name'`) ve aktif bölgeler `Illkirch` · `Schiltigheim` · `Strasbourg Merkez` diye diziliyor. Yani satır *"Uzak bölge asgari sepet (Strasbourg **Merkez**)"* oluyordu: merkeze uzaklık zammı. Artık merkez DIŞINDA kalan ilk bölge seçiliyor ve adı dürüst ("çevre bölge").
  - **Checkout'ta SIRA sorunu vardı:** `customer` ayarlarla aynı `Promise.all` içindeydi, yani kanal kendisini belirleyecek satır gelmeden okunuyordu — doldurmak isteyen biri de yapamazdı. Kapsam artık önce çözülüyor; bedeli bir okuma turu.
  - ⚠ **İlk yazımda kapsam `cookies()` okuyordu ve 34 test düştü** (*"cookies was called outside a request scope"*). Test kazası değil tasarım hatası: istek bağlamına bağlı bir okumayı orkestrasyonun içine koymak, onu cron/webhook/mobil uçtan ÇAĞRILAMAZ hâle getirir. Doğrusu deponun kendi disiplininde yazılıydı — `getCartView` yer eksenlerini parametre alıyor. Yer artık dışarıdan geliyor (`readPlaceScope`), kanal içeride çözülüyor (DB okuması, bağlam istemez).
  - **`[~]` ve `[x]` değil:** nöbetteki `edge-min-basket.smoke.ts` e2e koşusu arka uç şeridinin erişiminde değil; yeşile dönmesi işin bitti kanıtı ve o koşu ekran şeridinde.
  - ✅ **AYNI EKSENDE İKİNCİ BİR ÇELİŞKİ KAPANDI (25.08 · mobil şeridin ölçümü 11.08, defter):** kapsam doğru çözülüyordu ama **asgari sepet eşiği iki kapıda İKİ FARKLI TUTARA bakıyordu** — `checkout-draft.ts:401` indirim ÖNCESİNİ (`scope.subtotalCents`, doğru), `checkout-options.ts:159` indirim SONRASINI (`input.basketCents`, yanlış). Çelişki taslağın kendi içindeydi: aynı fonksiyon kendi kapısında doğru ölçüp, 419. satırda çağırdığı `resolveCheckoutPayment`ten yanlış `minBasketOk` alıyordu; ekran (snapshot) da onu okuyordu.
    **Kullanıcı kararı 11.08:** eşik **indirim ÖNCESİ** tutara bakar — teslimatın ekonomisi taşınan malın değerine bağlıdır, kampanya eşiği düşürmez. `CheckoutPaymentInput`a `subtotalCents` eklendi (`orderScopeOf` onu zaten döndürüyordu ve künyesi ayrımı yazıyordu; eksik olan tek şey kapıya geçirilmesiydi). `basketCents` yerinde kaldı — kargo ücreti, bedava kargo eşiği ve `orderTotalCents` indirim sonrasını istiyor. **İki alan, iki ayrı soru.**
    **Alan İSTEĞE BAĞLI yapılmadı:** varsayılanı `basketCents` olan bir alan, unutan çağıranda arızayı sessizce geri getirirdi — düzeltmeye çalıştığımız şeyin ta kendisi. Test dosyasında bir yardımcı kısaltıyor, kapıda zorunlu.
    **Regresyon çivisi:** `checkout-options.test.ts` → *"KAMPANYA eşiği düşürmez"* (mal 26 €, kampanya 3 €, eşik 25 € → geçer; tahsil edilecek toplam yine 23 €). Dosya 17/17.
  - *Bitti:* `edge-min-basket.smoke.ts` yeşil (bölge satırı sepette uygulanıyor); b2b müşterinin sepeti 120 € altında kapanıyor; DE adresli checkout 12,90 € kesiyor
  - *Bitti:* parası iade edilmiş her iptal artık iadeyi söylüyor; hiç tahsil edilmemişle aynı cümleyi paylaşmıyor. ~~`[~]` çünkü webhook'un ikinci iade dalı hâlâ eski cümleye düşüyor~~ — o dal 08.08'de kapandı (aşağıda)
  - **Ölçüldü (07.08):** onay ekranı yalnız `view.cancelled` bayrağına dallanıyor (`confirmation-sections.tsx:56-69`) ve üç ayrı yolu tek cümleye indiriyor — ödemesi hiç geçmemiş sipariş, süperse edilmiş taslak, **ve parası çekilip otomatik iade edilmiş sipariş**. Üçüncüsünde ekranın yazdığı *"Kartınızdan tahsilat yapılmadı"* cümlesi **yanlış**: `stripe-webhook.ts` iki yolda da (`order.status === 'cancelled'` · `refundAndCancel`) önce `stripe.refunds.create` çağırıyor.
  - **Neden en pahalı yalan:** iade ekstreye günler sonra düşer. O aralıkta müşteri "tahsilat yapılmadı" yazısını okumuş, hesabında para eksiktir — destek talebi kaçınılmaz ve güven kırarak gelir.
  - **Neden tek başına yapılamazdı:** ekranın ayırt edecek sinyali YOKTU. `view` yalnız `cancelled` biliyor; `paymentStatus` da ayırmıyor çünkü iade yolunda `recordOrderPayment` hiç çağrılmıyor (`stripe-webhook.ts` — tahsilat kaydı `refund` dalından SONRA yazılıyor), yani durum `pending` kalıyor. Talep açıldı ve **karşılandı**: `order.cancel_reason` (`payment_failed · superseded · out_of_stock · customer · staff`). Arka uç önerdiğim üç seçenekten 1.'yi seçti ve gerekçesi benim görmediğim yerdendi — `refund_issued boolean` yalnız BU ekranın sorusunu cevaplardı, sebep kolonu operasyonun iptal listesindeki "neden" sütununu da kapatıyor.
  - **Durum (07.08 · yazıldı, testli · kuralın İLK hâli — 08.08'de damgaya taşındı, en altta):** ekran iptalde iki ayrı cümle kuruyor; kural saf bir fonksiyonda (`isRefundedCancellation`) ve beş testle çivili. Yazılan sebepler: `superseded` (`supersedeOpenDrafts`) · `out_of_stock` (kapıda/vadeli ödemede rezervasyon tutmayınca).
    **SEBEP TEK BAŞINA YETMİYOR — ölçüldü.** Arka ucun tablosu *"`out_of_stock` → para çekildi ve iade edildi"* diyor ve bu KART yolunda doğru; ama aynı sebep bu yüzeyde ikinci bir yerde daha yazılıyor: kapıda/vadeli ödemede rezervasyon tutmazsa taslak `out_of_stock` ile kapanıyor ve orada **para hiç çekilmiyor**. Sebebi tek başına okuyup "iade edildi" demek, düzelttiğimiz yalanın tam tersini üretirdi. Kural bu yüzden ödeme yöntemini de okuyor: **`out_of_stock` + `online`**.
    **`payment_failed` YAZILAMADI ve bu bir eksik değil:** kart reddi sunucumuza hiç uğramıyor (`checkout/actions.ts` künyesi: *"o karar Stripe'ın kendi arayüzünde veriliyor"*). Reddedilen kartta sipariş `draft` kalıyor, müşteri tekrar deneyince `superseded` olarak kapanıyor. Enum'da değer duruyor ama bugün yazan yok — not düşüldü (`not-arka-uc-iptal-sebebi-iki-bosluk.md`).
    **Kalan boşluk ARKA UÇTA:** `stripe-webhook.ts`'in ilk iade dalı (`order.status === 'cancelled'`) parayı iade ediyor ama sebebi değiştirmiyor — sipariş `superseded` kaldığı için ekran yine "tahsilat yapılmadı" der. Dar bir ihtimal (3-D Secure penceresi + sayfa yenileme) ama sıfır değil ve para gerçekten çekiliyor. Çözümü sebep kümesinin sahibinde; not açıldı. Görev bu yüzden `[~]`.
    **KALAN BOŞLUK KAPANDI (08.08, arka uç) — satır `[x]`.** Webhook'un birinci iade dalı (`order.status === 'cancelled'`) parayı iade ediyor ama hiçbir iz bırakmıyordu. Sebebi değiştirmek çözüm DEĞİLDİ: sipariş `superseded` diye iptal edilmiş, `out_of_stock` yazmak yalan olurdu (stok kalmıştı). Ölçüm şunu gösterdi — **tek kolon iki soruyu birden taşıyordu.** `cancel_reason` "neden iptal oldu" der; ekranın sorduğu ise "para çekilip geri verildi mi". `out_of_stock` dalında ikisi çakıştığı için ilk turda fark edilmemişti.
      **Çözüm ayrı bir damga:** `order.provider_refunded_at`. İki iade dalı da tek ortak ayaktan geçiyor (`refundProviderPayment`) — kural tek yerde olmasaydı biri damgalanır öteki damgalanmazdı, ve damgalanmayan dalda ekran yine "tahsilat yapılmadı" derdi. Damga iadeden SONRA yazılıyor: önce yazsaydık, iadesi düşen bir ödeme "iade edildi" görünürdü.
      **Bayrak değil TARİH:** "ekstremde görünmüyor" diyen müşteriye tarih söylenir; boolean o soruyu bir daha cevaplayamaz. Müşteri yüzeyi `isRefundedCancellation`'ı sebepten değil bu alandan besleyebilir — iki dal aynı alanı dolduruyor, yani kural tek. Not bırakıldı.
      İki test: `out_of_stock` dalında damga düşüyor · zaten iptal siparişe geç gelen ödemede damga düşüyor ve **sebep değişmiyor**.
    **EKRAN DAMGAYA TAŞINDI (08.08, müşteri yüzeyi):** `isRefundedCancellation` artık `cancelled && refundedAt !== null`; görünüm modeli iptal sebebini HİÇ taşımıyor (`ConfirmationView.cancelReason` → `refundedAt`, kaynak `order.providerRefundedAt`). Kazanç iki katlı: **(a)** `superseded` dalı kendiliğinden doğru cümleyi alıyor — dünkü kural o dalda hâlâ "tahsilat yapılmadı" diyordu ve para gerçekten çekilmişti; **(b)** ekran "hangi sebepler para demektir" sorusunu artık hiç sormuyor, yani sebep kümesine yarın bir değer eklendiğinde bu ekran sessizce yanlışa düşemez. Test sayısı beşten üçe indi ve bu bir kayıp değil: silinen ikisi sebep enum'unun dallarını sayıyordu, o dallar artık ekranın kararı değil. Sebebi tek başına okumanın yetmediği ölçümü (yukarıda) hâlâ geçerli — kolonun ikiye ayrılmasının gerekçesi zaten oydu.
    **Kayda geçen uyarı (arka uç, 12-para'ya):** Stripe iade edilen ödemenin komisyonunu çoğu hâlde geri vermiyor. Defter net sıfır gösteriyor ve bu bizim tarafımızda doğru — gerçekte küçük bir maliyet hiçbir yerde görünmüyor. Bugün hacim yok; hacim gelince ele alınmalı.

- [ ] (07.16) **Sipariş KAPANIŞINI (`completed`) yazan hiçbir üretim çağıranı yok — iki kulvarda birden** *(ölçüldü 28.08, kargo durum zinciri yazılırken)* · `touches: packages/application/src/order/fulfillment.ts, apps/web/lib/order/**, apps/web/app/(operations)/**`
  - **Ölçüm:** `closeOrder` (`application/order/fulfillment.ts`) ve web köprüsü `apps/web/lib/order/fulfillment.ts` yazılı, testli ve **yalnız testlerden çağrılıyor**. `grep`in bulduğu tüm çağıranlar `*.test.ts`. Yani rota siparişi de kargo siparişi de `delivered`da kalıyor.
  - **Neden önemli:** kapanış kâr kalemlerinin SABİTLENDİĞİ andır (`DOMAIN §12`) — COGS, teslimat maliyeti, paketleme. Kapanmayan sipariş kârlılık raporuna hiç girmiyor ve bu sessiz: hiçbir şey patlamıyor, yalnız sayı eksik çıkıyor.
  - **Kargo kulvarına özel bir kapanış YAZILMADI ve bu bilinçli** (07.12 kararı): eksik olan halka değil zincirin tamamı; kargoya özel bir kapanış iki kulvarı ayrı kurallara bölerdi. Kargoda `actualDeliveryCostCents` girdisi hazır (`shipment.actual_cost_cents`), rota kulvarında ayardan birim maliyet okunuyor — ikisi de `closeOrder`ın içinde.
  - **Yön (karar bekliyor):** kapanış otomatik mi (teslimden N gün sonra, iade penceresi kapanınca) yoksa operatör eylemi mi? İade süreci `delivered → returned → completed` yolunu da kullanıyor; otomatik kapanış o pencereyi kapatmamalı.

## Netleşecekler

- **Stripe hesap/ürün kurulumu:** ~~hesap, webhook imza anahtarı, ödeme yöntemleri~~ **ÇÖZÜLDÜ (28.07):** hesap test modunda hazır, env yer tutucuları `apps/web/.env.example`'a eklendi (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`); anahtarları kullanıcı dolduruyor. Yerel deneme iki komutta: **`pnpm stripe:listen`** (webhook'u yerele yönlendirir; verdiği `whsec_` uç noktaya özeldir, `.env.local`'dekiyle aynı olmalı) ve **`pnpm stripe:smoke`** (gerçek anahtarla test modunda oturum açıp stok ayırmayı doğrular, sonra hepsini temizler). Kalan açık uç: Apple/Google Pay ve SEPA gibi ödeme tiplerinin panelden açılması (kod tarafı yöntem-agnostik). Önceki not: (karar 27.07): `.env.example`'ın kendi kuralı uygulama-özel anahtarın özelliğiyle birlikte eklenmesidir; erken eklenen değişken, karşılığı olmayan ölü kayıt olur. Yeri: **YALNIZ `apps/web`** — hem oturum hem webhook imza sırrı orada. Görev satırı ikiye bölünmesini öngörüyordu (webhook backend'de olacaktı); webhook `apps/web`'e alındığı için (`ARCHITECTURE_DECISIONS` Sapma 5) `apps/backend` Stripe anahtarı GÖRMEZ. Publishable key de yok: müşteri Stripe'ın barındırdığı sayfaya yönlendiriliyor, tarayıcıda Stripe.js çalışmıyor.
- **Çok-tablolu RPC listesinin son hali:** teslim (07.7) ve hızlı satış (07.10) sınırları kesinleşti — ölçüt 06.1'in "dar liste"si: eşzamanlılık yarışı **veya** bölünemez çok-tablolu yazım. Son halka `create_order` ile kapandı (30.07): listede yazılmayı bekleyen çok-tablolu yazım kalmadı.
