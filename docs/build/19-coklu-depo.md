# 19 — Çok Depo (Depo Ağı)

## Kapsam

Sistemi tek-depo varsayımından depo ağına taşır: depo varlığı, posta kodu → bölge → depo zinciri, depo bazlı stok/rezervasyon/FEFO, parçalı mal kabul, depolar arası transfer, depo kapsamlı roller, karma sepet (rota + ayrı ödemeli kargo dolgusu). **Fiyat modeli ve para modeli değişmez.** Kural seti `DOMAIN §17` (bağlayıcı); teknik kararlar `DATA_MODEL` Kalıcı kararlar 01.08 bloğu. Karar süreci ve dört bağımsız analizin sentezi `temp/multi-depo/` arşivinde.

## Okunacaklar

- `DOMAIN.md §17` (depo ağı kuralları) + §4/§5/§6/§7/§16 içindeki "Depo ağı (01.08)" çapaları
- `DATA_MODEL.md` Kalıcı kararlar — 01.08 bloğu
- `data-model/depo.md` — **henüz yok**; 19.1 ile birlikte doğar (alan tabloları migration'la aynı commit — `docs:check` ikisini karşılaştırır)

## Bağımlılık

`06-stok` ve `07-siparis` (tamam — üzerine kurulur). **Ters bağımlılık:** `10-depo` ve `11-kurye-rota` ekranları 19.1–19.3 bitmeden yazılmaz (tek-depo varsayımıyla yazılan ekran iki kere yapılır); `09.15` Rotalar, bölge→depo atamasını 19.5 ile birlikte alır.

## Kırıcı pencere (19.1–19.3) — paralel çalışma kuralı

**Tek ajan, tek dal.** `warehouse_id` kolonları not null olduğu için şema inince TÜM entegrasyon testleri birlikte kırılır ve ancak 19.3 sonunda birlikte düzelir; parça parça merge edilmez. Bu pencerede diğer ajanlar DB'ye vuran iş almaz (`CLAUDE.md §4b` — tek yerel Supabase paylaşılıyor); DB'siz işler serbest. Kapanış sırası: kullanıcı `db:reset` → `pnpm test` (kilitli tam paket) → merge. Sonrası: 19.4 arka uç, 19.5–19.6 operasyon, 19.7 müşteri şeridi — `touches` kümeleri ayrık, paralel koşabilir (`WORKFLOW §7`).

## Başlarken verilecek izah (örnek)

> "Sistemi birden çok depoya hazırlıyoruz. Müşteri posta kodunu girince onun bölgesine bakan depo belli oluyor; katalogdaki 'stokta var / tükendi' o depoya göre konuşuyor. Sipariş hiçbir zaman ikiye bölünmüyor — kendi deposunda olmayan kargoluk ürün ayrı ödemeli ayrı bir kargo siparişi olarak veriliyor, yolu stok belirliyor. Depocu yalnız kendi deposunu görüyor; mal kabul depoya yapılıyor, tek satın alma iki depoya parça parça gelebiliyor. Depolar arası taşıma kayıtlı: yola çıkan mal iki depoda da satışa kapalı."

## Görevler

- [x] (19.1) **Şema + görünümler + RPC'ler** *(kırıcı pencere)*: yeni `0031_warehouse.sql` (warehouse + transfer tabloları + `variant`-depo eşik tablosu + `vehicle` + tüm FK bağlama; ülke başına tek aktif kargo deposu kısıtı DB'de) ve mevcut migration'lara `warehouse_id` kolonları (stock, reservation, delivery_zone, order, stock_intake, purchase_order_item hedefi, temperature_log, user_profiles kapsamı + CHECK; bölge-posta bağ tablosu `(ülke, kod)` PK ile 0017'de); `available_stock` → `(warehouse_id, variant_id)` + `available_stock_total` + `purchase_order_progress`; RPC güncellemeleri: `reserve_stock`/`quick_sale` depo süzgeçli kilit, `receive_intake` (depo parametresi + koşulsuz PO kapanışı kalkar), `record_preparation` parti↔sipariş depo doğrulaması, `adjust_stock_batch` depo kodlu önek, `order_counts` süzgeci; yeni `dispatch_transfer`/`receive_transfer`; tek-depo ertelenmiş kısıtı (`order_discount_balance` deseni); `setting_scope + 'warehouse'`; seed: 2 depo + bölge ataması + 1 transfer — touches: `supabase/migrations/`, `scripts/seed/`, `docs/architecture/data-model/`
  - *Bitti:* migration'lar sıfırdan sıralı çalışıyor (kolonlar FK'siz doğar, FK'ler 0042'de bağlanır — `stock.intake_id` emsali); `data-model/depo.md` + `CLAUDE.md §1` depo değişmezi AYNI commit'te
  - **Durum (01.08):** 43 migration ayrı bir test veritabanında sıfırdan temiz koştu; değişmezler 21 senaryoyla fiilen sınandı. **Bağımsız ajan denetimi yapıldı, 14 bulgunun 10'u gerçek çıktı ve düzeltildi** — en ciddi üçü: sevk müşteriye söz verilmiş malı yola çıkarabiliyordu (ölçü fiiliydi, kullanılabilir değil), kabul edilmemiş satır transferi kapatıp malı buharlaştırıyordu, aynı satırın ikinci kabulü stok üretiyordu. Ayrıca kapsam tetikleyicimde SQL isim çözümleme tuzağı vardı (`unnest(...) as id` → koşul `w.id = w.id`, kontrol sessizce hep geçiyordu).
  - **Karar dokümanından dört sapma (gerekçeli):** posta kodu anahtarı `(ülke, kod)` oldu ve ülke bölgede değil bağ satırında durur (sınır ötesi bölge — ADR-002); "tek aktif kargo deposu" kayıt kapısı yerine kısmi unique indekse indi ve anahtarı ülke (K9'un geleceğini bugünden karşılar); katalog görünümü `0034` → `0043`'e taşındı (artık `warehouse`'a cross join yapıyor); belge önekine depo kodunu motor değil SQL ekliyor (aynı bilgi iki yerde üretilemez).
- [x] (19.2) **Types + database servisleri**: Zod şemaları (warehouse, transfer; `AvailableStockSchema`'ya `warehouseId` ZORUNLU alan — sessiz yanlış-anahtar riski tipte kapanır); `StockService` okumaları `warehouseId` zorunlu parametre, `ReservationService`, `StockIntakeService` (PO kalem bağı), `PurchaseOrderService`, `ReorderService` (depo başına öneri), `OrderService` süzgeçleri, `DeliveryZoneService` (bağ tablosu okuması); yeni `WarehouseService` + `WarehouseTransferService`; **depo test factory** (`packages/database/src/testing/`) — her test dosyası kendi depo kurulumunu tekrarlamasın — touches: `packages/types/`, `packages/database/`
  - *Bitti:* entegrasyon testleri factory kullanıyor; `getAvailableMap(warehouseId, …)` imzasız çağrı derlenmiyor
  - **Durum (01.08):** T8 amaçlandığı gibi işledi — imza değişimi 91 tip hatası olarak patladı, hepsi kapatıldı. Yol boyunca üç eksik daha çıktı: `partially_received` Zod enum'una eklenmemişti; FEFO okumaları (`listByVariant`/`listInStock`/`listByVariantWithDates`) depo süzgeci almıyordu; `setRoles` depocu rolünü kapsamsız yazmaya çalıştığı için **her çağrıda kısıtta patlıyordu** — rol ile kapsam artık tek çağrıda gidiyor (`setWarehouseScope` de eklendi).
- [x] (19.3) **domain-core motorları**: `resolveWarehouseForPostalCode` (ülke+kod → bölge → depo; belirsizlikte hata, sessiz "ilki kazanır" kalkar) · `decideCartAgainstWarehouse` (kalem sonucu: rota deposundan / kargo dolgusu / yok — yolu stok belirler) · `warehouseScope` (rol×kapsam; boş kapsam = hiçbir depo) · `transferDecision` (FEFO önerisi + parametrik ulaşım süresi uyarısı) · `documentPrefixFor` depo kodu · `DeliveryZoneCandidate` genişlemesi — touches: `packages/domain-core/`
  - *Bitti:* iki depolu yarış testi yeşil (aynı varyant, bir depo boş → red; süzgeçsiz eski davranış geri gelemez); kullanıcı `db:reset` + `pnpm test` tam paket → merge
  - **Durum (01.08):** dört motor + 40 birim testi. `findZoneForPostalCode` ülke-farkında hale geldi ve eşleştirme TEK KAYNAĞA indi (`matchZones`) — depo çözümü onu çağırıyor; ayrışan tek şey belirsizliğe verilen cevap: rota günü sorarken ilki seçilir, depo seçerken **hata döner** (yanlış depo = mal başka şehirde).
- [x] (19.4) **Uygulama kapıları**: `lib/order/` (delivery→`warehouseId`, checkout-draft ret hali + kargo dolgusu ayrımı, reserve, preparation depo süzgeci — hazırlık ekranının hata metni `BEKLEYEN(19.5)`), `lib/stock/` (fefo/intake/adjustment), `lib/storefront/` (read-context depo bağlamı; katalog süzgeci SQL'de — keyset bozulmaz), `lib/cart/read` (depo bağlamı iletilir; kargo grubu ayrımı ve "burada satılmıyor" ≠ "şu an tükendi" mesajları sepet EKRANIYLA birlikte — `BEKLEYEN(19.7)`, motoru hazır: `decideCartAgainstWarehouse`), `lib/delivery/` (yer çözümü v2), `lib/guard` (`requireWarehouseScope`; bugün hiç çağrılmayan `requireWarehouse` devreye girer) — touches: `apps/web/lib/`
  - *Bitti:* typecheck + birim + dokunulan entegrasyon testleri yeşil; depo süzgeçsiz stok okuması kalmadı (`grep` kanıtı)
  - **Durum (01.08):** `pnpm test` **1328/1328**. `resolveDelivery` artık depoyu da çözüyor (yer zinciri tek turda — ayrı okuma iki cevabı ayrıştırabilirdi) ve çözümsüz hâlde checkout sipariş açmıyor (`warehouse_unresolved`; sebep ayrık tutuluyor — "bölge dışısınız" ile "kargo deposu tanımlı değil" aynı cümle olamaz). Vitrin kapıları `warehouseId: string | null` alıyor: null = yer bilinmiyor → depo-üstü okuma ve **teklif tutarı gösterilmez** (yalnız varlığı), böylece kart ile sıralama ayrışmıyor.
  - ⚠ **Denetçinin uyardığı tuzak fiilen yaşandı:** `product_listing` depo boyutu alınca depo süzgeci uygulamayan katalog okuması aynı ürünü sayfada 8 kez gösterdi ve keyset imlecini bozdu. `listByPrice` artık süzgeci zorunlu kılıyor (null → `warehouse_id is null` satırı).
- [~] (19.5) **Operasyon — depo yönetimi + kapsam**: Depolar ekranı (CRUD + ülke başına tek aktif kargo deposu kuralı — kısıt DB'de, ekran hatayı okunur gösterir + bölge↔depo ataması — `09.15` bölge CRUD'u ile birlikte + personel kapsam ataması); stok/sipariş ekranlarına depo süzgeci (`order_counts` parametresi); kapsamında birden çok depo olan personele kapsamla sınırlı seçici — touches: `apps/web/app/(operations)/`, `components/operation/`
  - *Bitti:* depocu yalnız kendi deposunu görüyor (kapsam testi); admin tüm depoları süzebiliyor
  - **Durum (01.08 — EKSEN İNDİ, EKRANLAR BEKLİYOR):** iki katmanlı desen (bağlam + tablo süzgeci) kabuk, siparişler ve stok üzerinde çalışıyor; `.dc` çizimi olan her şey uygulandı. **Kalan üç parça `.dc` bekliyor**, kod değil: Depolar CRUD ekranı, bölge↔depo ataması (Rotalar sayfası henüz yok), personel kapsam ataması (Ayarlar sayfası henüz yok). Sayfa dokümanları yazıldı; çizim ısmarlanınca tamamlanır.
  - **İnen parçalar:** `writeWarehouseContext` + `setWarehouseContextAction` (çerezi yazan ilk kod — bağlam bugüne dek okunuyordu ama hiç yazılamıyordu, yani fiilen ölüydü) · **O3B** bağlam seçici sidebar'da dört hâlle (depo-üstü / kapsamlı / tek kapsamlı → seçici yok / tek depolu kurulum → hiçbir şey) · **O3C** aranabilir süzgeç çipi + "süzülüyor" şeridi · **O3D** satırda depo işareti (sipariş sütunu · stok kırılımı · parti kartında tam ad) · `WAREHOUSE_PARAM` ile URL sözleşmesi · iki `BEKLEYEN(19.5)` kalktı (`getNetworkAvailabilityMap` → `listAvailableAcross`).
  - **Kural 5 uygulamada ikiye ayrıldı ve bu ölçüldü:** "sayaçlar bağlamı, satırlar süzgeci izler" — ilk yazımda stokta İHLAL edilmişti (`?depo=KEHL` sayaçları 87→29 düşürüyordu) ve ekranın kendi şerit metni bu yüzden yalan söylüyordu. Düzeltme: partiler bağlamla okunur, seviye satırı in-memory daraltılır (partiler zaten sayfalanmıyor, ikinci sorgu gereksiz).
  - **Ortak komponent borcu kapandı:** `Combobox` çip biçimi aldı — dosyanın kendi yorumu boşluğu zaten kaydetmişti ("süzgeç şeridi aramasız `Select`'le idare ediyordu"), depo süzgeci beklenen tüketici çıktı. Tetikleyici görünümü `Select` ile tek kaynağa indi (`form/trigger.ts`); `Table` alt satır (`renderSubRow`) yeteneği aldı — kendi tablosunu yazan ekran, bir gün başlık hizasını da kaydırır.
  - **Süzgeç kodu taşır, kimliği değil** (`?depo=STR`): paylaşılan bağlantı okunabilir kalıyor ve kapsam dışı bir kod geldiğinde kullanıcıya GERİ YAZILABİLİYOR — uuid olsaydı adını söylemek için kapsam dışı bir satırı okumak gerekirdi (kural 7'nin bildirimi sessiz düşmeyi engelliyor).
  - ⚠ **Mobil bağlam seçici O11'i bekliyor:** tasarım onu "başlık barının altında tam genişlikte satır + bottom sheet" diye çiziyor ama operasyonun **mobil kabuğu yok** (`layout.tsx`, sidebar `w-[214px]` sabit). Masaüstü hâli `.dc`'ye birebir indi; mobil varyant kabukla birlikte gelir.
  - ⚠ **Tasarımdan bilinçli sapma — stokta satır elenmiyor:** `.dc` "tablo yalnız bu deponun satırlarını gösteriyor" diyor, ama seviye listesi ÜRÜN sayfalamasıdır (keyset) ve satır düşürmek imleci bozar. Süzgeç satırları değil ADETLERİ daraltıyor; şerit metni bunu açıkça söylüyor (`design/BACKLOG.md §5`).
  - **Durum (02.08 — DEPOLAR EKRANI İNDİ):** `/operations/warehouses` yayında (`page` → `warehouses-client` → `.desktop`/`.mobile`, yalnız yönetici — `requireAdmin` + `NoAccessPane`). Çizimin üç karesi de karşılandı: tesis listesi (sıralanabilir; sıra tüm depo seçicilerinde aynıdır), tesis rayı + kart (künye · hizmet alanı · karne · bağlı personel), mobil OKUMA kartı. Üç pencere: künye ekle/düzenle (`warehouse-dialog`), kapatma onayı (`close-warehouse-dialog`), bölge kurulumu (`zone-dialog`). Yazma kapıları `actions.ts`; 20 birim testi (`warehouses-read.test` · `warehouses-url.test`).
    - **Bağlam bu sayfayı DARALTMAZ** (tasarım §6): depo bir yönetim nesnesidir — kapalı olan da, kapsam dışı olan da listelenir. Kapsamla süzülseydi "ikinci depoyu nereden ekleyeceğim" sorusunun cevabı kendi içinde kaybolurdu.
    - **Kurallar VERİDE, cümleler ekranda.** Ülke başına tek kargo deposu (`warehouse_single_online`) ve posta kodunun tekilliği (`delivery_zone_postal_code_pkey`) DB kısıtlarıdır; ekran onları yeniden uygulamaz, `constraintOf` ile okunur cümleye çevirir (19.19'un indirdiği kapı ilk tüketicisine kavuştu). Bölge çakışmasında ayrıca ÖN OKUMA var — ama kuralı uygulamak için değil, "67100'ü Kuzey hattı tutuyor (COL)" diyebilmek için; kısıt yine son sözü söyler.
    - **Kapatma dört ayrı ağırlıkta anlatılıyor** (stok · bağlı bölge · tek kapsamlı personel · yoldaki sevkiyat) ve YALNIZ gerçekten var olanlar üretiliyor — sonucu olmayan tesiste uyarı basmak, sonraki gerçek uyarıyı da okunmaz kılardı. Onay deponun kodunu yazmayı istiyor ve **kapı da istiyor** (`setWarehouseActiveAction.confirmCode`): istemciye güvenerek yazılan yıkıcı bir eylem hiçbir yerde durdurulmaz. Kapatma `shipsOnline`'ı da düşürüyor — kısmi unique indeks yalnız aktif satıra baktığı için kayıt geçerdi ama "o ülkede kargo deposu var" diye okunan bir satır kalırdı.
    - **Karne SAYAR, listelemez:** her kutu Stok/Siparişler'e o depo bağlamıyla giden bir kapı. Adresler hedef ekranın kendi sözleşmesinden kuruluyor — bunun için `stockLink()` ve `ordersLink()` eklendi; elle `?depo=…&scope=…` yazmak parametre adlarını ikinci kez yazmak olurdu. "Yolda bekleyen" bilerek kapısız: yoldaki mal hiçbir deponun stoğunda değil, yani Stok'ta gösterilecek satırı da yok.
    - **Ölçülemeyen değer sıfıra düşmüyor** (`CLAUDE.md §1`): fiyatı girilmemiş partiden risk TUTARI çıkmaz → `null` ve ekran "tutar bilinmiyor" der. Alan adı da ölçtüğünü söylüyor: "son hareket" değil **"son mal girişi"** — çıkış defteri okunmadan "hareket" demek ölçülmeyen bir şeyi iddia etmek olurdu.
    - **Kopya düşen üç yer:** `riskCentsOf`/`totalRiskCents` stok ekranından `lib/stock/batch-labels`'a taşındı (karne aynı tutarı depo başına soruyor; iki yerde hesaplansa iki ekran aynı depo için farklı risk yazardı) · `COUNTRY_LABELS`/`COUNTRY_OPTIONS` müşteri künyesindeki özel kopyadan `components/operation/ui/labels`'a çıktı (enum'un yanına inmesi arka uçtan istendi, §4) · gün adları takvimin `WEEKDAYS` sözlüğünden okunuyor, ikinci bir gün dizisi yazılmadı.
    - ⚠ **Tasarımdan bilinçli sapmalar — kayıt BURADA, `design/BACKLOG.md`'ye taşınacak** (o dosya şu an başka şeridin elinde, commit'lenmemiş bir maddesi var; başkasının yarım işini commit'lemek yerine kayıt görev satırında duruyor — `CLAUDE.md §5`, durumun sahibi zaten bu satır): kartın iç başlığı ortak başlık barına taşındı (iki bar üst üste 09.19'un tam tersi olurdu) · adres tek serbest kutu yerine üç alan (sokak/kod/şehir — kartın iki satırlı gösterimi ancak böyle DOĞRU çıkar; şekil `AddressSchema`'dan türetildi) · gün kısaltmaları iki harfli (`Sa` · `Pe` · `Ct`), çizim üç harfli yazıyor.
    - **Kalanlar:** bölge kurulumunun HARİTASI (`19.20`) · sıralamanın tek turda yazılması ve tek turluk personel ucu (arka uç talebi §5 — ikisi de bugün çalışıyor).
      - ✅ **Personel kapsam ataması KAPANDI (03.08, `09.16`):** Ayarlar ekranının kullanıcı penceresi depo kapsamını yazıyor (`setRoles(id, roles, warehouseIds)`), kapsam rollerle BİRLİKTE gidiyor. Yalnız depocu/kurye rollerinde soruluyor — yönetici ve muhasebe depo-üstüdür, kapsamı hiç okunmaz. Kapsamsız bırakılan depocu/kurye için ekran uyarı yazıyor: boş dizi "hepsi" değil HİÇBİRİ (fail-closed), yani kişi hiçbir şey göremez.
- [ ] (19.6) **Operasyon — transfer**: sevk (parti önerisi + serbest sapma) / kabul (kısmi) / yoldakiler listesi; belge no `TRF-<depo>-yy-####` — touches: `apps/web/app/(operations)/`
  - *Bitti:* sevk→kabul akışında iki deponun stoğu doğru; yoldaki mal hiçbir depoda satılabilir görünmüyor
  - ✅ **Arka uç kapısı AÇIK (03.08): `cancel_transfer` yazıldı** — operasyon şeridi ekranı kurarken sordu, kapı yokken düğmeyi çizmemişti (doğru: ölü düğme yalan söyler). Artık `WarehouseTransferService.cancel({ transferId, actorId, reason })`. **Düğmenin metni "İptal" DEĞİL, "Sevk kaydını geri al"** — tek "iptal" iki ayrı gerçeği yutar ve stok yalan söyler: *(1)* sevk kaydı hatalıydı, mal hiç çıkmadı → bu yol, miktar kaynak PARTİYE geri yazılır (yeni parti doğmaz; `initial_qty` ve geri çağırma izi bölünmesin); *(2)* mal çıktı sonra döndü → bu yol DEĞİL, ters yönlü yeni transfer, çünkü mal fiilen iki kez yol gitti. Kabul edilmiş transfer geri alınamaz (RPC reddeder) ve ikinci geri alma da reddedilir — stoğu iki kez geri yazardı. Kayıt silinmez, damgalanır (`cancelled_by/at`, `cancel_reason`; kısıt durumla damgayı birbirine bağlar) çünkü `reference_no` kâğıt klasördeki numaradır. Testler: `warehouse.test` › *"sevk kaydı geri alınır…"* + *"geri alma İKİ KEZ çalışmaz…"*
- [~] (19.7) **Müşteri yüzeyi**: yer bağlamı v2 (`lezzet.place.v2` çerezi: `{country, postalCode}` — v1 `localStorage` kaydı geçersiz sayılır, yeniden sorulur) + belirsizlik seçici (ülke ALANI değil; yalnız `ambiguous`/`unknown` hâllerinde) + katalog/ürün "kargoyla gönderilir" işareti + sepette kargo grubu + "kargolu ürünleri ayrıca sipariş ver" iki-checkout akışı + posta kodu daveti deseni (tasarım paketinden) — touches: `apps/web/app/(customer)/`, `components/customer/` (lib dokunuşları 19.4 ile koordineli — kesişirse sıraya, `WORKFLOW §7`)
  - *Bitti:* posta kodu değişiminde kalem kaybolmuyor (`saved_items`); keyset sayfalama ve fiyat sıralaması bozulmuyor; yer bilinmiyorken yere bağlı vaat yok
  - ✅ **Yazarken şehir önizlemesi — YAPILMAYACAK (kullanıcı kararı, 01.08).** Öneri şuydu: kod 5 haneye tamamlandığı an alanın altında yerin adı belirsin ("67000 · Strasbourg"), müşteri "Göster"e basmadan doğru yazdığını görsün. Veri ve alan hazırdı; engel `resolvePlaceAction`'ın içindeki `recordDemand`di — her tuşlanan kod **"bölge dışı talep" sayacına** düşerdi. Kullanıcı ödünleşmeyi bu yönde kesti: *"buton kalsın, bölge dışı talebi kirletmesek daha doğru olur."* Gerekçe sağlam ve sayaçtan daha genel bir ilkeyi söylüyor — **ölçüm niyeti kaydeder, tuş vuruşunu değil**; yazım hatasıyla dolan bir sayaç, bölge açma kararını sessizce yanlış yere çeker (`CLAUDE.md §1`: bozuk ölçüm sağlıklı gibi okunur). Onay düğmesi hem niyeti hem ölçümün sınırını belirliyor. Adı yine görüyoruz, yalnız onaydan SONRA (hapta). Yeniden açılırsa önce sayacın niyet-dışı yazımdan nasıl korunacağı çözülmeli — kapıya bayrak eklemek yetmez, o bayrağı unutan ilk çağrı sayacı yine kirletir.
  - **Hap şehir adını yazıyor (01.08):** `placeName` her kodda dolu artık (19.8), `zoneName` değil — müşterinin zihnindeki ad "Strasbourg", "Strasbourg Merkez" bizim iç bölge adımız. Rota içi/dışı ayrımını hapın rengi söylüyor. Ülke eki yok: tek ülkeye hizmet verirken gürültü olurdu, hizmet kümesi büyüyünce belirsizlik seçicisiyle aynı türetmeden gelecek.
    - ⚠ **Ama gösterilen ad çok yerleşimli kodda YANLIŞ ve hap bugün canlı** (bkz. `19.17`): `67800`de hap Bischheim'lı müşteriye "Strasbourg" diyor. Adres formunda aynı gösterim GERİ ALINDI (orada ad kaydedilen veriye sızıyordu); hapta salt gösterim olduğu için bırakıldı, ama doğrusu `19.17`'nin güvenilirlik ayrımı gelince adın yalnız güvenilirken yazılması — kalanında çıplak kod. Bu şeridin işi, veriyi bekliyor.
  - **Ön koşulların TAMAMI açıldı (01.08):** 19.8 · 19.9 · 19.10 · 19.16 `[x]`. Yani bu şeridin dört işi artık yazılabilir: **(1) dört hâlin ekran dili** — `stockStatus` veri olarak geliyor ama HİÇBİR ekran okumuyor, kartlar/paneller hâlâ `soldOut` bakıyor (`storefront-cards.tsx`, `purchase-panel.tsx`, `package-card.tsx`); **(2) belirsizlik + tanınmayan kod ekranları** — `PlaceLookup` ayrık (`place-dialog.tsx` bugün üçünü de tek `t.invalid`e düşürüyor); **(3–4)** aşağıdaki kendi iki açığım. Bekleyen tek şey `elsewhere` hâlinin yanındaki "gelince haber ver" kaydı (19.12) — UI tam yazılır, arka uç stub (`CLAUDE.md §3`).
  - ⚠ **Katalog çipinin süzgeci kimin turunda — arka uca soruldu (01.08).** 19.10 satırı "adresime gönderilebilir" SQL süzgecini `BEKLEYEN(19.7)` diye bu şeride bırakıyor, ama iş `0032_product_listing.sql` + `ProductListingService`'te duruyor — ikisi de arka ucun `touches` kümesi. Önerim raporda: **süzgeç orada kalsın, çipin metni ve durumu burada.** Bugünkü hâlde çip yalnız yerel depoya bakıyor, yani kargoyla gelebilecek ürünü gizliyor — C3'ün yasakladığı "sistem müşteriyi tanıdıkça daha az gösteriyor" hâlinin katalogdaki karşılığı.
  - **İNDİ (01.08) — dört hâlin ekran dili + tazeleme.** Tasarım §3'ün işaret dili kart ve ürün detayında konuşuyor:
    - **`StockMark` + `StockNoticeButton`** (`components/customer/delivery/stock-mark.tsx`): `available` **hiçbir işaret basmaz** (iyi haber sessizdir), `shipping` kum hap, `elsewhere` amber hap + "Gelince haber ver", `out_of_stock` burada değil kartın köşe rozetinde (o hâl yere bağlı değil, evrensel).
    - **Metin YER ailesinde, sayfada değil.** Aynı dört cümle anasayfa/katalog/ürün/sepette görünüyor; dört `messages.json`'a kopyalansaydı biri değişince ötekiler eskirdi. `place-messages.json`'da tek nüsha (`place-chip` deseni). Yol üstünde bir tekrar da baştan kesildi: `markShipping` ile `shipTitle` aynı cümleydi → tek anahtar `shipMark`.
    - **Kart** (`storefront-cards.tsx`): `elsewhere` hâlinde görsel yarı solar (`grayscale-[.55]`, tükendideki tam soluklaştırma DEĞİL — ürün gerçek ve geri gelecek), fiyat `muted` tona düşer, tek eylem "Gelince haber ver" olur. Sepete ekleme yolu kapanmıyor: ürün detayında sürüyor.
    - **Ürün detayı**: `DeliveryLine` artık `status` alıyor ve iki bloğu tasarım diliyle konuşuyor. Blok rota tahmininin (`!inRoute && !shippable`) **yerine geçer**, yanına değil — o tahmin "bu ürün senin deponda var mı" sorusunu soramıyordu. Paket detayı `status` geçmediği için eski dalda kalıyor (paketin stok hâli kalemlerinden doğar, bugün yalnız `inRouteOnly` var).
    - **Üst rozet artık yalan söylemiyor:** `elsewhere`/`shipping` hâlinde yeşil "Stokta" yazmıyor, yerini yer işareti alıyor. Mobilde rozet elle boyanıyordu ve **tükendi hâlinde de yeşil çıkıyordu** ("Tükendi" yazan yeşil bir rozet) — K5'e taşındı, ton anlamdan seçiliyor.
    - **`Badge` dördüncü ağırlık `outline`** (açık zemin + çerçeve): işaret kart gövdesinin içinde, ad ile fiyat arasında duruyor; çerçevesiz `tint` orada bir metin satırı gibi okunuyordu. `Price` üçüncü ton `muted` aldı — fiyatı gizlemek ürünü bilinmez kılardı, ink bırakmak alınabilir gibi okuturdu.
    - **"Gelince haber ver" GERÇEK, stub değil:** arka uç kapıyı bu tur içinde indirdi (`recordVariantStockNoticeAction`) ve içine `BEKLEYEN(19.7)` bırakmıştı — düğme onu bağlıyor. Panel iki bekleyiş için TEK kutu: `NoticeDialog` (eski `ZoneNoticeDialog` genelleştirildi, ikinci bir form yazılmadı).
    - **(a) kapandı:** `setPostalCode` ve `clear` sonrası `router.refresh()`. Çerezi istemci yazıyor; tazeleme olmadan hap doluyor ama katalog kartlarındaki işaretler bir sonraki gezinmeye kadar ESKİ kalıyordu — yani işaret dili yazıldığı gün yanlış çalışacaktı.
  - ⏳ **(b) ilk kare gecikmesi AÇIK.** `PlaceProvider` çerezi okuyup `resolvePlaceAction`'ı yeniden çağırıyor; `readPlaceContext()` zaten sunucuda çözüyor. Çare `initialPlace` prop'u ama **bugün yazılamaz**: layout'un aynı `DeliveryPlace`'i kurabilmesi için `resolvePlaceAction`'ın içindeki dönüşümün ayrıştırılması gerekiyor ve o dosyayı (`lib/delivery/actions.ts`) arka uç şeridi şu an düzenliyor. Ayrıca dikkat: layout doğrudan `resolvePlaceAction` çağıramaz — içindeki `recordDemand` her sayfa açılışında "bölge dışı talep" sayacına yazardı, yani yukarıdaki ✅ kararın ihlali. Ayrıştırma yapılırken sayaç eylemin içinde kalmalı.
  - **Panelin dört hâli + öneri listesi indi (03.08).** `place-dialog` artık `resolvePlaceAction`'ın
    ayrık sonucunu (19.16b) dört ayrı cümleyle karşılıyor; önce dördü de tek bir "Bu kod geçerli
    değil" uyarısına düşüyordu ve metin yalnız `unknown` için doğruydu. Ötekilerde ekran müşteriye
    **onun hatası olmayan bir şeyi hata gibi** söylüyordu: `unresolved`'ın iki sebebi de bizim
    tarafımızın eksiği (`no_shipping_warehouse` yapılandırma, `ambiguous_zone` veri çakışması), ama
    müşteri "kodum yanlış" diye okuyordu.
    **`ambiguous` bir SEÇİM ekranı oldu.** Ülke normalde sorulmaz, koddan türer (19.8) — tek istisna
    kodun iki hizmet ülkemizde birden geçerli olduğu hâl: orada türetecek bir şey yok. Seçim
    `resolvePlaceAction`'a **isteğe bağlı ülke** olarak geçiyor ve motorda DEĞİL eylemde uygulanıyor:
    motor "bu kod hangi ülkelere düşüyor" sorusunun cevabı; hangi adayın seçildiği bir kullanıcı
    kararı. Çözülmüş yerin son hâli tek yerde kuruluyor (`finishResolved`) — iki giriş, tek yol.
    **Öneri listesi (kullanıcı kararı 02.08): müşteri yazar, biz öneririz, o seçerek onaylar.**
    Kapı arka uçtan geldi (19.19 · `searchPrefix`) ve **ayrı bir eylem** olarak bağlandı
    (`suggestPostalCodesAction`) — `resolvePlaceAction`'a bayrak eklemek her tuşlanan kodu
    `recordDemand`'e yazardı. Ayrım yapısal: **öneri bir okuma, onay bir niyet.** Gecikme 220 ms,
    geç dönen cevap yeni listeyi ezmiyor. **Ülke her satırda yazılı** (kullanıcı kararı): aynı kod iki
    ülkede geçerli olabiliyor ve yalnız ilçe adı gören müşteri hangisine baktığını bilemiyor.
    Öneri satırı ile belirsizlik seçicisi **aynı bileşen** — ikisi de aynı soruyu soruyor ve aynı üç
    bilgiyi taşıyor. Rota işareti satırı öne alıyor ama seçmiyor: iki adayın farkı yalnız teslimat
    yolu değil KDV oranı.
    **Sözleşme tipi `place-types.ts`'te** (`PlaceSuggestion`), `@lezzet/database`'ten alınmıyor:
    paneli bir istemci bileşeni okuyor ve yüzeydeki hiçbir istemci dosyası veri paketine bağlı değil.
    Kopya değil doğrulanan bir bağ — eylem dönüşünü bu tiple imzalıyor, veri tarafı ayrışırsa
    derlenmiyor. Tasarımı çizili değil: `design/BACKLOG §4`.
  - **Rota-only üründe satın alma eylemi posta kodu ister (02.08, kullanıcı kararı).** `shippable=false` ürün/pakette yer bilinmiyorken "Sepete ekle" yerine **"Posta kodunu gir"** durur (`PlaceGate`, `PlaceDialog` açar). Gerekçe fiyat değil örtük söz: soğuk zincir malı yalnız rota deposundan gidiyor, müşterinin rota içinde olup olmadığını bilmeden düğme "satın alabilirsiniz" diyor ve bunu doğrulayamıyoruz. Kod girilince kart dört hâlinden birine oturuyor — süzgeç değil, sıra. Fiyat gizlenmedi (yer bilinmiyorken teklifler okunmadığı için gösterilen sayı tavandır, kod girilince ya aynı kalır ya düşer). Tasarım sapması `design/BACKLOG §3`.
    - **İndi:** ürün detayı (`PurchaseBar routeOnly`) + paket detayı (`PurchaseBox routeOnly`) — ikisinde de veri yerinde (`StorefrontProductDetail.shippable`, `StorefrontPackage.inRouteOnly`).
    - ⏳ **Katalog + anasayfa KARTI açık, veri eksik:** `StorefrontProduct` (kart tipi) `shippable` taşımıyor — yalnız detay tipinde var. Bir alan + eşleyici satırı gerekiyor ve dosyalar arka ucun `touches` kümesinde (`lib/storefront/{storefront-types,map}.ts`). Alan gelince kart bağlanır; o güne kadar kilit yalnız detayda, yani müşterinin gerçekten karar verdiği yerde çalışıyor.
  - **İNDİ (02.08) — SEPETİN İKİ GRUBU + iki-checkout akışı (tasarım §4-§5).** Şeridin en büyük parçası:
    - **Grup ayrımı tek yerde:** `splitByRoute` (`lib/cart/cart-types`). Üç yer soruyor — sepet ekranı, mobil alt çubuk, checkout istemcisi ("siparişe hangi kalemler girecek") — ve üçü ayrı yazsaydı ayrışan kopya "ekranda gördüğüm kalem siparişe girmedi" demek olurdu. Kargo grubuna YALNIZ motorun oraya koyduğu kalem girer; yolu bilinmeyen (`null`: yer sorulmamış ya da PAKET) ve karşılanamayan kalemler ana grupta kalır — bilinmeyeni asıl akıştan çıkarmak siparişten sessizce kalem düşürmek olurdu.
    - **Başlık ve blok tek grupta ÇİZİLMEZ.** Ayrım ancak ayrılacak bir şey varken bilgidir; tek yollu sepette başlık olmayan bir seçimi varmış gibi gösterirdi.
    - **Kargo ücreti artık sepette yazıyor** ve özet kartının "sepette kargo satırı yok" kuralını bozmuyor, sınırını çiziyor: o kural ücretin teslimat türüne, türün de ADRESE bağlı olmasından doğuyordu — kargo grubunda tür zaten belli. `CartView.shippingTariffCents` ham tarifeyi taşır, kararı `shippingGroupFee` motora sorar (checkout `resolveShippingFee`'yi aynı eşikle çağırıyor). Türetilebilir `freeShippingRemainingCents` alanı KALKTI: aynı gerçeğin iki kopyasıydı.
    - **Satırın yol işareti motordan** (`line.route`), sezgiselden değil. Eskisi ("rota içindeyim + ürün kargolanabilir mi") çok depoda bir hâli tamamen kaçırıyordu: rota İÇİNDEKİ müşterinin kendi deposunda olmayan soğuk zincir ürünü "kapıya getiriyoruz" diye işaretleniyordu. Aynı düzeltme `restrictedLines`'a da girdi — o hâlde kısıt bloğu hiç çizilmiyor, kalem checkout'a kadar geliyor ve orada reddediliyordu (`cold_chain_unshippable`). Blok artık iki sebebi ayrı cümleyle söylüyor (bölge dışı ≠ bölgenizde şu an yok) ve rota içinde bölge notu yerine VARYANT notu kaydediyor.
    - **`/checkout?group=shipping`** — bayrak URL'de (yenileme ve geri gelme çalışsın), oradan `loadCheckoutAction` + `confirmCheckoutAction` + `createCheckoutDraft`'a. Ekran da söylüyor (`ShippingOrderNote` + kendi üst satırı): iki checkout tıpatıp aynı görünürse müşteri kapıya giden kalemlerinin nerede olduğunu sorar.
    - **Sepet temizliği KISMİ oldu** (`lib/cart/settle.ts`): sipariş kesinleşince yalnız O SİPARİŞİN kalemleri düşer. Toptan boşaltma tasarımın açık sözünü bozuyordu (*"verilmezse kalemler sepette bekler"*). Sipariş kaleminden okunur, istemciden değil — kartta temizlik webhook'ta oluyor ve orada istemcinin gönderdiği liste yok. Paketin İÇİNDEKİ varyantlar sayılmaz: aynı ürünü ayrı satırda da taşıyan müşterinin o satırı silinirdi.
    - **Yer değişince sepet YENİDEN OKUNUR ve fark bildirilir** (`diffCartByPlace` + `PlaceChangeCard`). Okuma şarttı: yol ve fiyat depodan çözülüyor, okunmazsa ekran eski yerin gruplarını göstermeye devam ederdi. Fark kalem kalem yazılır (tasarım: "sessiz daralma yok"); boşsa kart hiç çizilmez. Kart sağlayıcıda yaşar çünkü yer başlıktaki haptan da değiştirilebiliyor.
    - **`shipping_fee_cents` ortak anahtara taşındı** (`lib/settings-keys`) — sepet ile checkout aynı satırı okumak zorunda; bu dosya zaten o kural için var.
    - **ADET TAVANI — kullanıcı bildirimi (02.08), gerçek bir açıktı.** Yeri değişen müşterinin 5 adedi yeni yerde 2 olabiliyor. Motor bu cevabı 19.3'ten beri üretiyordu ama okuma onu ATIYORDU: satır 5 gösteriyor, "+" sonsuza basılıyor, `blocked` false (o bayrak "hiçbir depoda yok" demek), taslak da adede bakmıyordu. İş **rezervasyonda** patlıyordu — müşteri adresini ve ödeme yöntemini seçip "onayla"ya bastıktan sonra, hangi ürün olduğunu söylemeyen ve olguyu yanlış anlatan bir cümleyle ("bir ürün tükendi"; tükenen bir şey yok, o adrese o adet gitmiyor). Para güvendeydi (rezervasyon her iki yolda da tahsilattan önce), kaybedilen güvendi.
      - `CartLine.availableHere` — yolun işaret ettiği havuzun miktarı. Motorun `fulfillableQty`'si bu soruyu **cevaplayamıyor**: `min(istenen, mevcut)` döndürüyor, yani 2 isteyip 2 bulan satır ile 5 isteyip 2 bulan satır ondan ayırt edilemez. Hangi havuza bakılacağını yine motor söylüyor (`route`), okuma yalnız sayıyı alıyor.
      - Satırda **bal tonlu düzeltme düğmesi** ("…en fazla 2 adet · 2 adede indir"); adet otomatik DÜŞÜRÜLMEZ — müşterinin yazdığı sayıyı haber vermeden değiştirmek, kalemler için yasakladığımız sessiz daralmanın adetteki hâli olurdu (kullanıcı da bu yönde istedi). Tavana dayanılmışsa yalnız cümle yazılır. İki tavan ayrı şey söylüyor ve cümleyi gerçekten dayanılan yazıyor: `limitCap` "bu FİYATTAN", `availableHere` "bu YERE".
      - Sayı bir SÖZ değil: sepet stok ayırmıyor (`DOMAIN §4`), o yüzden cümle "şu an en fazla" diyor ve seçici aşılmış hâlde kilitlenmiyor.
      - Yer değişimi kartına `reduced` hâli; **checkout taslağına `insufficient_here`** — kalemi adıyla ve mümkün adediyle söyler, sepetin gösterdiği sayının aynısıyla. Ekran ile kasa aynı sayıyı söylemezse sürpriz sadece bir adım geriye taşınmış olurdu.
    - **Sapmalar** (`design/BACKLOG §3`): kargo başlığında "2-3 iş günü" YAZILMADI (arkasında ayar yok, uydurma bir taşıma süresi sözü olurdu); yer değişimi kartında tek eylem var (tasarımın iki düğmesi de müşteriyi bulunduğu ekrana götürüyordu); karşılanamayan kalem OTOMATİK olarak sonraya kaydedilmiyor — taşımayı kısıt bloğu yapıyor ve orada asgari sepet/ücretsiz kargo sonuçları da söyleniyor.
  - ⏳ **Belirsizlik + tanınmayan kod ekranları AÇIK** — `PlaceLookup` ayrık geliyor (19.16b) ama `place-dialog` hâlâ üçünü tek `t.invalid`e düşürüyor. Bir sonraki tur.
  - ⏳ **Ürün detayında "Sonraya kaydet" YOK, bilinçli** — tasarım `elsewhere` panelinde "Gelince haber ver" + "Sonraya kaydet" istiyor; sepet bağlamının `saveForLater`ı **sepetteki** kalemi taşıyor, sepette olmayan ürünü kaydedemiyor. Düğmeyi sahte koymak yerine "Sepete ekle" açık bırakıldı: kalem sepete girince kısıt bloğu (K32) zaten "sonraya kaydet" sunuyor, yani yol uçtan uca çalışıyor. Sepetin iki-grup turu bunu değiştirmedi (02.08): kaydetme hâlâ sepetteki kaleme bağlı ve doğrusu da bu — sepette olmayan bir ürünü "sonraya kaydetmek", listeye adet ve fiyat bağlamı olmayan bir satır yazmaktır → `design/BACKLOG §3`.
- [x] (19.8) **Posta kodu referansı + ülkesiz yer çözümü** *(19.7'nin ön koşulu)*: `postal_code_place` tablosu (GeoNames FR+DE, ~31 bin satır, CC-BY; tek seferlik içe aktarma betiği) + `resolvePlaceByPostalCode` kapısı dört hâlle (`route` / `shipping` / `ambiguous` / `unknown`) + `matchZones`'un ülkesiz turu (tek kaynak korunur) + şehir adı çözümü (~~tek yer varsa adı, birden çoksa üst idari birim~~ → indirgeme 19.17'de geri alındı) — touches: `supabase/migrations/`, `scripts/`, `packages/domain-core/delivery/`, `packages/database/`, `apps/web/lib/delivery/`
  - *Bitti:* ülke hiçbir yerde SORULMUYOR, sonuç olarak türüyor (vergi beyanı riski kapanır); geçersiz kod sessizce kargoya düşmüyor; `place-types.ts`'in "elimizde posta kodu veritabanı yok" itirafı kalkıyor
  - **Durum (01.08):** `0044` = tablo + 16.878 satır veri, TEK dosyada (tablo boşken sistem her kodu "tanınmadı" sayar — veri opsiyonel yükleme değil, tanımın parçası). Üreteç `scripts/build-postal-codes.mjs` (`pnpm postal:build`), yılda bir koşar. Motor `resolvePlaceByPostalCode` + 8 birim testi, servis + 5 entegrasyon testi. `resolvePlaceAction`'daki `country: 'FR'` sabiti kalktı.
  - **Belirsizlik HİZMET ALANIYLA kesişiyor:** 610 çakışmanın hepsi bizim sorunumuz değil — kod Almanya'da geçerliyken biz oraya hizmet vermiyorsak ortada seçim yok. Bugün (tek ülke aktif) `ambiguous` hâli hiç oluşmuyor; Almanya açıldığı gün yalnız gerçekten iki anlama gelen kodlar soruyor. Rota adayı sıralamada öne alınır ama **seçilmez**: iki adayın farkı yalnız teslimat yolu değil KDV oranıdır, sessizce tahmin edilemez.
  - ~~**Ad kuralı ölçümle düzeldi:** ilk sürüm çok yerleşimli kodda departmanı yazıyordu (51300 → "Marne"); bir alt idari birime (FR arrondissement / DE Kreis) inince ana yerleşimi buldu.~~ **BU KURAL GERİ ALINDI (19.17, 02.08).** Arrondissement'a inmek "ana yerleşimi bulmak" değildi: Fransız arrondissement'ı merkez kasabasının ADINI taşır, yani üretilen etiket geçerli bir belediye adı gibi okunuyor ve yanlış şehri söylüyordu (`67800` → "Strasbourg", oysa Bischheim / Hœnheim). Künyedeki *"daha geniş ama ASLA yanlış değil"* iddiası yanlıştı ve kodların ~%39'unu etkiliyordu. Artık indirgeme yok: `places` tüm yerleşimleri taşıyor, ad ondan türetiliyor ve **çok yerleşimli kodda gösterilmiyor**.
  - ⚠ **Müşteri şeridinden not (01.08) — `unknown` KAPI OLMASIN, UYARI OLSUN.** Dördüncü hâl doğru bir ekleme (typo bugün sessizce kargoya düşüyor), ama referans tablosu bir **anlık görüntüdür**: GeoNames'te olmayan geçerli kod vardır ve yenileri açılır. `unknown` sert engel olursa o müşteri hiç alışveriş yapamaz — oysa gerçek kapı zaten checkout adresidir ("adres kazanır"). Ekranda: uyarı + **"yine de devam et"**. Devam edilirse yer `null` sayılır — yani bugünkü "yer bilinmiyor" davranışı (depo-üstü okuma, yere bağlı vaat yok); yeni bir yol açılmıyor. Kapının `unknown` dönmesi yeterli, kararı ekran veriyor.
  - **Gerekçe ölçüldü (01.08):** FR 6.065 + DE 10.813 kod, **610'u iki ülkede birden geçerli** — her on Fransız kodundan biri (ilk sayım CEDEX kayıtlarını da katıp 20.418 demişti; onlar posta kutusudur, teslimat yeri değil). Bugünkü 8 rota kodu çakışmıyor ama çakışmalar genişleme koridorunda (Bas-Rhin ↔ Rheinland-Pfalz aynı `67` önekini paylaşıyor: 67240 Bischwiller ↔ Bobenheim-Roxheim). IP'den ülke tahmini **yapılmıyor** — kendi sunucumuzda barındırıyoruz (`STACK §271`), MaxMind maliyeti 610 kodluk sıralama ipucuna değmiyor → `BACKLOG`.
- [x] (19.9) **Yer çerezi + istek başına çözüm + checkout sırası** *(19.7'nin ön koşulu)*: `lezzet.place.v2` çerezi yalnız müşterinin CEVABINI taşır (`{country, postalCode}`; `warehouseId`/`zoneId`/`nextDate` yazılmaz — istemcinin yazabildiği bir alan hangi deponun stoğunu göstereceğimizi belirleyemez) + React `cache()` ile istek başına tek çözüm + posta kodu tablosuna uzun TTL önbellek + `createCheckoutDraft`'ta `resolveDelivery` yukarı taşınıp `getCartView` depo bağlamıyla çağrılır ("adres kazanır") — touches: `apps/web/lib/delivery/`, `apps/web/lib/order/checkout-draft.ts`, `apps/web/lib/cart/`
  - *Bitti:* dört `BEKLEYEN(19.7)` işaretinin arka ucu açık (katalog/anasayfa/ürün/boş sepet artık `null` depo geçmiyor); checkout sepeti depo-üstü okumuyor — "sepette gördüm, ödemede kayboldu" kapanır
  - **Durum (01.08):** çerez indi (`lezzet.place.v2`, yalnız `{country, postalCode}`), `readPlaceContext()` React `cache()` ile istek başına tek çözüm, **dört sayfa bağlandı** (anasayfa · katalog · katalog `loadMore` · ürün detayı · boş sepet seçkisi). `localStorage` yalnız "soruyu atladım" işaretleri için kaldı — onları sunucunun bilmesine gerek yok.
  - **Yol üstünde çıkan duplication:** yer çözümünün girdilerini (aktif bölgeler + aktif depolar) `resolveDelivery` ve yeni `readPlaceContext` AYRI AYRI okuyordu. İki okuma iki farklı ana ait olabilirdi ve o hâlde aynı istekte sepet bir depoyu, katalog başka bir depoyu görürdü. `lib/delivery/inputs.ts`'te birleşti (saatlik önbellek, `DELIVERY_ZONES_TAG` paylaşımlı) — yan kazanç: çözüm sıcak yolda DB'ye hiç gitmiyor.
  - **Checkout sırası düzeldi ("adres kazanır"):** döngü göründüğü gibi değildi — depo seçimi yalnız ADRESE bağlı, sepet yalnız "kargo da kapalı mı" kararını etkiliyor. Teslimat iki kez çözülüyor (önce depo, sepetten sonra kargo kararı); girdiler ortak ve istek-kapsamlı bellekte olduğu için ikinci tur bedava.
  - ⚠ **Sıra değişikliği bir hâli maskeledi ve test yakaladı:** sepet depo bağlamıyla okunmaya başlayınca, rota dışı adreste kargo deposunda bulunmayan soğuk zincir kalemi "şu an alınamıyor" (`blocked_lines`) diye işaretlendi. Doğru ama yanıltıcı cümle — müşteri stok beklemeye başlar, oysa o ürün o adrese HİÇ gitmeyecek. Yapısal engel artık geçici olandan önce söyleniyor (`cold_chain_unshippable` → `blocked_lines`).
  - **`unstable_cache` yerine React `cache()`:** ilk sürüm kalıcı (saatlik) önbellek kullanıyordu ve `resolveDelivery`'nin 7 entegrasyon testi "incrementalCache missing" ile düştü — Next'in kalıcı önbelleği istek bağlamı dışında çalışmıyor. Amaç zaten hız değil TUTARLILIK'tı (aynı istekte tek çözüm); test edilebilirlik bir mikro-optimizasyona feda edilmedi.
- [x] (19.10) **Kargo deposu stok haritası**: `loadProductContext(db, rows, warehouseId, shippingWarehouseId)` ikinci harita (`shippingStock`) — depo-üstü toplam bu soruyu cevaplayamaz (C3 korunur: toplam yalnız "hiçbir yerde yok mu"nun dayanağı) + "adresime gönderilebilir" çipinin SQL süzgeci (iki depo kimliği üzerinden; sayfalama bozulmaz) — touches: `apps/web/lib/storefront/`, `supabase/migrations/0032_product_listing.sql`
  - *Bitti:* "📦 Kargoyla gönderilir" işaretinin üç koşulu da ölçülebiliyor; 19.11'in girdisi hazır
  - **Durum (01.08):** üç sayı okunuyor (yerel · kargo deposu · ağ geneli) ve `StockStatus` dört hâle çözülüyor: `available` · `shipping` · `elsewhere` · `out_of_stock`. Yerel depo zaten kargo deposuysa ikinci okuma atlanıyor; yer bilinmiyorsa üçüncü okunmuyor (`stock` zaten ağ toplamı) — sabit sayıda sorgu, N+1 yok.
  - ⚠ **GERİLEME DÜZELTİLDİ — müşteri şeridinin yakaladığı, bugün canlıydı.** 19.9 yeri sunucuya taşıyınca `availableQty` depo süzgeçli hâle geldi ve `soldOut = availableQty <= 0` sessizce ANLAM DEĞİŞTİRDİ: "hiçbir depoda yok"tan "senin deponda yok"a. Sonuç: posta kodunu giren müşteri, kargoyla gönderebileceğimiz ürünü "Tükendi" ve pasif düğme olarak görüyordu — **sistem müşteriyi tanıdıkça daha az satıyordu.** C3 tam olarak bunu yasaklıyor. `soldOut` artık yalnız `out_of_stock` hâlinde true.
  - **Dördüncü hâl neden gerekli:** "yerelde yok + kargoda yok" iki AYRI şeydir — ürün başka depoda duruyor olabilir (soğuk zincir, o bölgeye gitmiyor) ya da hiçbir yerde olmayabilir. İlkinin cümlesi "bölgenizde şu an yok" + "gelince haber ver" (19.12), ikincisininki "tükendi". Aynı kelime gelmeyecek malı bekletir ya da gelecek malı kaçırır.
  - **`PlaceWarehouses` tek nesne:** iki depo ayrı konumsal parametre olarak taşınsaydı ilk unutan çağıran gerilemeyi geri getirirdi. Kargolanamayan ürün (soğuk zincir) kargo deposunda dursa da `shipping` hâline geçmez.
  - **BEKLEYEN(19.7):** dört hâlin EKRAN dili (rozet, düğme metni, "gelince haber ver") müşteri şeridinin işi; veri hazır. Katalogdaki "adresime gönderilebilir" SQL süzgeci de o turda — bugün çip yalnız yerel depoya bakıyor.
  - ⚠ **19.9 `soldOut`un ANLAMINI değiştirdi ve ekranlar bunu bilmiyor — bugün canlı** (müşteri şeridi, 01.08). Vitrin görünümünde tek bir bayrak var: `soldOut = availableQty <= 0` (`storefront/map.ts:102,147`) ve `availableQty` artık **depo süzgeçli** haritadan geliyor (katalog ve ürün sayfaları `readPlaceWarehouseId()` geçiyor). Yani anlamı "hiçbir depoda yok"tan "senin deponda yok"a kaydı; adı ve okuyanları aynı kaldı. Sonuç: **posta kodunu giren müşteri, yerel deposunda olmayan her ürünü "Tükendi" ve pasif "Sepete ekle" olarak görüyor** (`purchase-panel.tsx:153` → `sellable = !soldOut`) — kargoyla gönderebileceğimiz ürün dahil. Sistem müşteriyi tanıdıkça daha az satıyor; yer bilmeyen ziyaretçi daha çok ürün görüyor. C3'ün ihlali: "tükendi" yalnız ürün hiçbir depoda yokken söylenebilir.
  - ⚠ **Bu yüzden ikinci harita YETMİYOR, ÜÇÜNCÜ sayı da gerekiyor: depo-üstü toplam.** Dört hâl üç sayıdan doğuyor — `local` · `shipping` · `total`. `local=0 & shipping=0 & total>0` hâli (mal başka bir ROTA deposunda) tasarımda **"bölgenizde şu an yok"**tur, "tükendi" değil; ikisi tasarım sözleşmesinde açıkça ayrı mesaj ("ilki yere bağlı ve değişebilir, ikincisi evrenseldir"). İki haritayla bu ayrım yapılamaz. İstenen: `loadProductContext` üç sayıyı da versin ve görünüm `soldOut: boolean` yerine dört hâlli bir alan taşısın — bayrağın adı korunursa aynı yanlış okuma bir sonraki turda geri gelir.
- [x] (19.11) **Sepet grupları + kargo eşiği**: `CartLine.route` (`local`/`shipping`/`unavailable`) + `CartView` grup toplamları `lib/cart/read.ts`'te (ekranda DEĞİL — iş kuralını ekran hesaplayamaz, `STACK §4`) + `decideCartAgainstWarehouse` çağıranına kavuşur + **`freeShippingCents` alanı bölünür**: eşik sepetin değil KARGO GRUBUNUN tutarından hesaplanır (K37) — touches: `apps/web/lib/cart/`
  - *Bitti:* 80 €'luk rota siparişi kargo kalemini bedava taşıtmıyor; sepet ekranı ile checkout aynı eşiği söylüyor
  - **Durum (01.08):** `decideCartAgainstWarehouse` 19.3'ten beri çağıransızdı, kablosu döşendi. `CartLine.route` (`local`/`shipping`/`unavailable`/`not_shippable_here`) + `CartView`'a üç alan: `shippingSubtotalCents`, `freeShippingRemainingCents`, `shippingOnly`. Karar `read.ts`'te veriliyor, ekranda değil — aynı ayrımı checkout da istiyor ve iş gerçeğini ekran hesaplayamaz (`STACK §4`).
  - **Eşik KARGO grubundan ölçülüyor** (K37): ücretsiz kargo bir kargo MALİYETİ kuralıdır, rota grubunun tutarının onunla ilgisi yok. Hesap kapıda yapılıyor, çağırana bırakılmıyor — sepet ekranı ile checkout iki ayrı hesap yapsaydı "sepette bedava yazıyordu" şikâyeti doğardı.
  - **`route: null` = yer bilinmiyor** ve bu bir eksik değil: hangi yoldan geleceğini bilmediğimiz bir kaleme yol atamak, bilmediğimiz şeyi söylemektir. Paket satırı da ayrımın dışında — kalemleri farklı depolarda olabilir ve "paketi ikiye böl" diye bir şey yok (DOMAIN §13).
  - `resolveDelivery` artık `shippingWarehouseId` de döndürüyor (19.15'in girdisi): kargo deposu ÜLKEDEN türer, rotadan değil — rota içindeki müşteri de kargo dolgusu alabilir. 5 entegrasyon testi.
  - ⚠ **YARIM KALMIŞTI — kablo rota DIŞINDA hiç bağlanmamış (bulundu 10.08, mobil şeridin cihaz ölçümü;
    düzeltildi denetimde).** `decideRoutes` yalnız rota deposunu alıyordu ve o boşsa BOŞ harita
    dönüyordu; satırlar kuruluş değerinde kalıyordu (`route: null` → `group: 'local'`). Yani **rota
    dışındaki her adreste, her yüzeyde** sepet her kalemi "kapıya teslim ediyoruz" diye gösteriyordu —
    soğuk zincir kalemi de dahil, o adrese hiç gelemezken. Yan hasarlar: `undeliverableSubtotalCents`
    daima 0 (asgari sepet matrahı yanlış), `shippingOnly` daima `false` (salt-kargo sepeti rota sepeti
    sanılıyor), 10.08'in grup işi rota dışında ölü.
    - **Kök, `warehouseId` boşluğunun İKİ ayrı şey demesiydi ve fonksiyonun ikisini tek saymasıydı:**
      "yer bilinmiyor" (iki depo da yok — yol atamamak doğru) ile "yer biliniyor, rota dışı" (rota
      deposu yok ama kargo deposu dolu — yol atanmalı). Çıkış koşulu artık "iki depo da yok".
    - **Motor zaten doğruydu, çağrılmıyordu** (`decideCartAgainstWarehouse`). İkinci havuz da zaten
      okunuyordu (`product-context`: rota dışında yerel havuz BOŞ harita, kargo havuzu ayrıca
      çekiliyor) — düzeltme bir parametre ve bir koşul, ek sorgu yok.
    - **Künye ile kod ayrışmıştı ve bulgunun en pahalı yanı buydu:** `read.ts`in `shippingWarehouseId`
      künyesi "bunu `decideCartAgainstWarehouse` motoruna veriyor (19.11)" diyordu. Vermiyordu.
      Okuyan taraf künyeye güvenip kodu bir daha açmadığı için arıza aylarca görünmedi. Künye
      düzeltildi ve yalanın kaydı orada bırakıldı.
    - **Neden aylarca kimse görmedi:** aynı posta kodunda KATALOG doğru çalışıyor (ürün soluk +
      "bu adrese teslim edemiyoruz"), çünkü katalog kargo havuzunu doğrudan okuyor. Yer ekseni
      sağlamdı; bozuk olan yalnız sepetin yol kararıydı — yani belirti, kaynağın komşusunda görünmüyordu.
    - 2 regresyon testi (`cart-route.test.ts`): rota dışı adreste kargolanabilir kalem `shipping`
      + `group: 'shipping'` + `shippingOnly`; soğuk zincir kalem `not_shippable_here` +
      `group: 'undeliverable'` + tutarı `undeliverableSubtotalCents`e sayılıyor.
  - ⚠ **`cart.updated_at` yazmada hiç tazelenmiyormuş (aynı ölçümde çıktı, düzeltildi 10.08).**
    `CartService.write` damgayı gönderiyordu ve künyesi "her dokunuşta tazelenir" diyordu, ama
    `upsert` girdiyi `insertSchema.parse`ten geçiriyor ve `CartInsertSchema` alanı taşımıyordu —
    Zod tanımadığı anahtarı sessizce atıyor. Kolon yalnız `default now()` ile, yalnız INSERT'te
    doluyordu. Ölçüldü: bir sepetin damgası 10:29, aynı sepetin son kalemi 15:27'de eklenmiş.
    Sepet kurtarma / terk edilmiş sepet okuması bu sütuna bakacak; bugün canlı tüketeni yok ama
    veri sessizce yanlış birikiyordu.
    - **Asıl kusur `as CartInsert` dönüşümüydü:** alan şemada olmadığı hâlde çağrıyı geçerli
      gösteriyordu. Cast kaldırıldı — bundan sonra alan yeniden düşerse derleyici uyarır.
      **Tarandı: servislerdeki altı `upsert` çağrısından cast kullanan TEK bu vardı**, yani tuzak
      yayılmamış. Ders `upsert`e değil cast'e ait: `as` ile susturulan bir kapı, kapının olmaması demektir.
- [x] (19.12) **Varyant+yer bazlı "gelince haber ver"**: `variant_stock_notice (variant_id, country, postal_code, email | customer_id)` — anahtar depo DEĞİL yer (söz müşterinin adresi hakkındadır; bölge ileride başka depoya bağlanırsa söz ayakta kalır) + kısmi unique (`where notified_at is null`). `zone_notice` yerinde kalır, anlamı farklı ("bölgenize gelmiyoruz" ≠ "bu ürün burada yok"). Tetikleyici bu turda değil — kayıt tutulur, ekran "not aldık" der — touches: `supabase/migrations/`, `packages/types/`, `packages/database/`
  - *Bitti:* aynı kişi+varyant+kod için tek açık kayıt; ekran söz vermiyor
  - **Durum (01.08):** `0045` + `VariantStockNoticeService` + `recordVariantStockNoticeAction`. Anahtar YER (`ülke, kod`), depo DEĞİL — bölge ileride başka depoya bağlansa da söz ayakta kalır ve "müşteri depoyu hiç görmez" kuralı veride korunur. Yer istemciden parametre olarak alınmaz, çerezden okunur: kaydın hangi yere ait olduğu bir tercih değil, sistemin bildiği gerçek.
  - **Kısmi unique (`where notified_at is null`) iki kuralı birden tutuyor:** aynı bekleyiş ikinci kez yazılmaz (düğmeye tekrar basmak yeni bir bekleyiş değil), ama haber verilmiş kayıt yeni bekleyişi ENGELLEMEZ — müşteri aynı ürünü aylar sonra yeniden bekleyebilir ve bu yeni bir sözdür. 5 entegrasyon testi.
  - **Tetikleyici bu turda YAZILMADI** (stok girince haber gönderme): ekran "not aldık" der, "haber göndereceğiz" demez. Söz verip tutmamak hiç söz vermemekten kötüdür — `zone_notice`'ın dürüstlük tonu aynen korunuyor. Tetikleyici 06/12 şeridinde ayrı bir görev.
- [x] (19.13) **Kapsam-farkındalıklı çoklu-depo okumaları** *(19.5'in ön koşulu)*: `StockService.listAvailableAcross(warehouseIds, variantIds)` — bugün yalnız iki uç var (tek depo / depo-üstü toplam), tasarımın "Tüm depolar" görünümü tam ortada duruyor + `OrderListFilters.warehouseId` → `warehouseIds` ve `order_counts(p_warehouse_ids uuid[])` (sayaç RPC'nin içinde hesaplanıyor; süzgeç dışarıda kalırsa sayaç ile liste ayrışır) + `WarehouseService.list({ warehouseIds })` + `getAvailableTotalMap` daraltma/yeniden adlandırma + ~~`pnpm test:purge`~~ (**yazılmadı** — gerekçe aşağıda) — touches: `packages/database/`, `packages/types/`, `supabase/migrations/0012_order.sql`
  - *Bitti:* çok kapsamlı personelin "tümü"sü kapsamıyla sınırlı; boş kapsam hiçbir satır getirmiyor (sorgu atılmadan); sayaç ile liste aynı kümeyi sayıyor
  - **Durum (01.08):** üç okumanın imzası hizalandı — `listAvailableAcross` (yeni), `listInStockDetailed`, `OrderListFilters` + `order_counts(p_warehouse_ids uuid[])`, `WarehouseService.list`. **Tek sözleşme:** `undefined` = depo-üstü · dizi = o depolar · **boş dizi = hiçbiri** ve o hâlde sorgu HİÇ atılmaz (fail-closed veriye değil koda yazıldı; PostgREST'te `in.()` güvenilmez).
  - **Bu bir metot eksiği değil İMZA AİLESİ eksiğiydi:** aynı soru ("kapsamımdaki depolar") üç ayrı okumada soruluyordu ve üçü de tek depo alacak şekilde yazılmıştı. Operasyon şeridi ikisini bildirdi, üçüncüsünü (`listInStockDetailed`) cevap yazıldıktan sonra kendisi buldu.
  - **`getAvailableTotalMap` → `getNetworkAvailabilityMap`.** İtiraz haklıydı: eski ad `getAvailableMap`'e bir kelime eklenmiş gibi duruyordu, oysa sözleşmesi bambaşka — orada unutulan argüman DERLENMEZ, burada unutulan bağlam derlenir ve makul görünen bir sayı döner. **Dönüş tipini `boolean` haritasına daraltma ÖLÇÜLDÜ ve yapılamıyor:** paket okuması `available < item.qty` karşılaştırması yapıyor, gerçek miktara ihtiyacı var.
  - **~~`pnpm test:purge`~~ YAZILMADI (gerekçeli):** ajanın istediği "reset dışında bir süpürme yolu"ydu; kullanıcı reset'i ucuz buluyor (01.08) ve `db:reset` test artıklarını zaten tam temizliyor. Ajanın ikinci önerisi (test depoları `is_active=false` doğsun) sistemi bozardı — `available_stock` görünümü pasif depoyu süzüyor, stok testlerinin çoğu kırılırdı.
  - ⚠ **`db:reset` gerektirir** — `order_counts` imzası değişiyor (`uuid` → `uuid[]`), fonksiyon drop+create ile gidiyor. Reset **kullanıcının** kararı (`CLAUDE.md`).
  - **Kaynak:** operasyon şeridinin talebi (`depo-ekseni-operasyon-arka-uc-talebi.md` §1, §1b, §2, §3, §5, §6b). Madde 5 bir **itirazdı ve haklıydı**: `getAvailableTotalMap` yanlış çağrıldığında derleniyor ve makul görünen bir sayı dönüyor — T8'in `getAvailableMap` için kapattığı sınıf burada açık kalmıştı. Sıra: kapı iner → iki `BEKLEYEN(19.5)` çağıranı kalkar → metot daralır.
- [x] (19.14) **Operasyon bağlam kapısı** *(19.5'in ön koşulu)*: `lib/warehouse/context.ts` → `readWarehouseContext()` = `{ scope, warehouses, activeWarehouseId, warehouseIds }`; `lezzet.ops.warehouse` çerezi (`<uuid> | "all"`, URL'e YAZILMAZ — paylaşılan bağlantı alıcının bağlamını ezmemeli) **her okumada kapsama karşı doğrulanır**: doğrulanmayan çerez yetki atlatmadır (depocu başka deponun kimliğini yazıp stoğunu okur), uymayan değer sessizce düşer ve bağlam "kapsamımdakiler"e döner — touches: `apps/web/lib/warehouse/`, `apps/web/lib/guard.ts`
  - *Bitti:* kapsamı sonradan daraltılan personel hata ekranı değil daha az veri görüyor; `warehouseIds` dönüşümü tek yerde (beş sayfada tekrarlanmıyor)
  - **Durum (01.08):** `readWarehouseContext()` indi; ilk çağıranı stok ekranının parti kuyruğu — artık kapsamla süzülüyor (depocu başka deponun raf ömrü kuyruğunu görmüyor). Çerez her okumada `canAccessWarehouse` ile doğrulanıyor ve uymayan değer **sessizce düşüp "tüm depolar"a** iniyor: kapalı kapı değil, daha geniş ama yetkili bir görüş — kapsamı daraltılan personel çalışmaya devam edebilmeli.
  - **Guard yetkiyi, kapı bağlamı verir.** `requireWarehouseScope` "bu kişi personel mi, kapsamı ne" sorusunu cevaplar; bu kapı "bugün hangi depoya bakıyor" sorusunu. Guard'a bağlam eklemek, yetki kontrolünü bir tercihe bağlamak olurdu.
  - **BEKLEYEN(19.5):** stok SEVİYE satırı hâlâ ağ-geneli toplam okuyor. Kapı hazır (`listAvailableAcross`) ama satır modeli değişiyor — tasarım "tek satır + N depoda ipucu" istiyor ve o ekranın işi.
  - **Neden arka uçta:** `apps/web/lib/` bu şeridin `touches`'ı, kapı bir **yetki** kapısı ve `cache()` düzeni 19.9 ile ortak — iki kez kurulmuyor.
- [x] (19.15) **Kargo siparişi taslağı** *(müşteri şeridinin bulduğu açık — 01.08, altı maddenin hiçbiri kapsamıyor)*: `createCheckoutDraft` sepetin alt kümesini alabiliyor (`entries`) ama teslimatı **adresten** çözüyor; adres rota içindeyse `resolveWarehouseForPostalCode` `kind:'route'` + **rota deposunu** dönüyor (`warehouse-resolve.ts:83`). Yani kargo grubunun kalemleriyle açılan ikinci taslak, malın bulunmadığı depodan `route` siparişi üretir. Taslağa "bu kargo siparişidir" diyecek bir girdi gerekiyor (yol açık seçim, varsayılan yok — `DOMAIN §17`); kargo deposu ve `deliveryType:'shipping'` oradan gelir. Ödeme kısıtı muhtemelen kendiliğinden düzelir (`resolveCheckoutPayment` yöntemi `deliveryType`'tan türetiyor) ama **doğrulanmalı**: tasarım kargo siparişinde yalnız online peşin istiyor (K37) — touches: `apps/web/lib/order/checkout-draft.ts`
  - *Bitti:* kargo grubundan açılan sipariş kargo deposundan ve `shipping` türüyle doğuyor; kapıda ödeme o siparişte hiç listelenmiyor; rota siparişi bu akıştan etkilenmiyor (iki ödeme birleşmiyor)
  - **Durum (01.08):** `CheckoutDraftInput.shippingOrder` — açık girdi, türetme DEĞİL. Türetseydik ("kalemlerin hiçbiri yerelde yok, demek ki kargo") aynı sepet iki kez okunur ve arada stok değişirse ikinci taslak sessizce tür değiştirirdi; yol açık seçimdir, varsayılanı yoktur (C2). `true` iken depo kargo deposudur, tür `shipping`'tir, gün seçilmez, bölge `null`'dur (kargo siparişi bir bölgeye ait değildir).
  - **Ödeme kısıtı doğrulandı, kod yazılmadı:** motor `deliveryType === 'shipping'` görünce kapıda yöntemleri zaten kapatıyor (`payment/checkout-options.ts:59`). Ama bu ancak türü EZDİĞİMİZ için işliyor — adres rota içi olduğundan zincirin kendi cevabı `route` olurdu ve kapıda ödeme açık kalırdı.
  - ⚠ **İki açık çıktı ve kapatıldı.** (1) Rota İÇİ adresten açılan kargo siparişinde soğuk zincir kontrolü atlanıyordu: `shippingBlockedReason` yalnız rota DIŞI adreste doluyor, o yüzden ayrı bir kontrol kondu. (2) **19.10'un `blocked` gevşetmesinin checkout karşılığı eksikti** — kargoyla gelebilen ürün artık "tükendi" sayılmıyor (doğru, C3) ve sepette satılabilir görünüyor, ama sipariş TEK depodan çıkar (K5); bayraksız çağrı malın olmadığı depodan taslak açabiliyordu ve iş rezervasyonda, müşteri ödemeye geçtikten SONRA patlardı. Sepet "alınabilir mi", checkout "bu siparişle gelir mi" sorusunu yanıtlıyor — ayrım artık kodda.
  - 3 entegrasyon testi; kurulum `DE` üzerinde (FR'de "ülke başına tek aktif kargo deposu" kısıtı seed'in deposuyla çakışırdı) ve damgalı posta kodu kullanıyor — o kod GeoNames'te yok, yani test 19.16a'yı da sınıyor.
- [x] (19.16) **Yer çözümünün iki açığı** *(müşteri şeridinin 19.8/19.9 okumasından — 01.08)*: **(a) `unknown` KENDİ bölgemize bakmıyor.** `resolvePlaceByPostalCode` daha ilk satırda karar veriyor (`warehouse-resolve.ts:168`: `matches.length === 0 → unknown`) ve `zones`'a hiç bakmıyor. Yani operatörün `delivery_zone_postal_code`'a elle girdiği, fiilen ARAÇLA GİTTİĞİMİZ bir kod GeoNames'te yoksa vitrin "bu posta kodunu tanımadık" diyor — oysa checkout aynı kodu kabul edip siparişi açıyor (`resolveDelivery` bu tabloya hiç bakmıyor, `grep` kanıtı). Tek sistem, aynı koda iki cevap. Kendi bölge tablomuz hizmet alanımız için OTORİTEDİR; referans tablosu ad çözümü ve hizmet vermediğimiz ülkeler içindir. Bağ satırı `(ülke, kod)` taşıdığı için ülke GeoNames'siz de türüyor — düzeltme `unknown` dönmeden önce kendi bölgelerimize bakmak. **(b) Dört hâl ekrana VERİ olarak ulaşmıyor.** `resolvePlaceAction` `ambiguous`/`unknown`/`unresolved` hâllerinde `throw` ediyor ve `ActionResult<DeliveryPlace>` hepsini tek bir `error: string`e indiriyor (`lib/error.ts:36`). Belirsizlik seçicisi (19.7) adayları — `{country, placeName}` çifti — göremediği için yazılamaz; hata metnine bakarak hâl ayırt etmek bir dizgi eşleştirmesi olurdu ve üç dilde çalışmazdı. İstenen: ayrık sonuç (`{kind:'route'|'shipping'|'ambiguous'|'unknown'|'unresolved', …}`), `throw` yalnız gerçek arıza için — touches: `packages/domain-core/src/delivery/warehouse-resolve.ts`, `apps/web/lib/delivery/actions.ts`
  - *Bitti:* kendi bölgemizdeki bir kod GeoNames'te olmasa da çözülüyor (vitrin ile checkout aynı cevabı veriyor); ekran dört hâli hata metni ayrıştırmadan ayırt edebiliyor
  - **Sıra:** 19.7'nin belirsizlik/tanınmayan kod ekranları (b)'yi bekliyor; (a) bugün de yanlış cevap veriyor, ondan bağımsız.
  - **Durum (01.08):** ikisi de kapandı. **(a)** `resolvePlaceByPostalCode` artık `unknown` demeden önce kendi bölge tablomuza bakıyor; bulunan ülke referanstakiyle BİRLEŞTİRİLİYOR (referanstaki ad daha bilgilendirici olduğu için o kazanır, ama kendi kaydımızdan gelen ülke referans bilmese de ayakta kalır). Pasif bölge de sayılıyor — rota kapalı olabilir ama kod bizim kaydımızdır ve doğru cevap "kargoyla gönderiyoruz", "tanımadık" değil. Aynı gerekçeyle iki okuma noktasında `activeOnly` süzgeci kalktı (`actions.ts`, `inputs.ts`): aktiflik kararı MOTORUN işi, okumanın değil. Yer adı uydurulmuyor — referansta yoksa `placeName: null` ve ekran bölge adını gösteriyor (zaten daha bilgilendirici). 6 yeni birim testi, biri düzeltmenin `unknown` hâlini yutmadığını çiviyor.
  - **(b)** `resolvePlaceAction` artık `ActionResult<PlaceLookup>` dönüyor: `resolved` · `ambiguous` (adaylar `{country, placeName, inRoute}` ile) · `unknown` · `unresolved` (sebebiyle). `throw` yalnız gerçek arıza için kaldı (biçimsiz kod, DB düşmesi). `place-context.setPostalCode` de hâli tip olarak veriyor ve **yer yalnız `resolved` hâlinde saklanıyor** — belirsiz bir cevabı saklamak, müşterinin vermediği kararı vermiş gibi göstermek olurdu.
  - **BEKLEYEN(19.7):** üç hâlin EKRANI yazılmadı; `place-dialog` bugün üçünü de tek uyarıya düşürüyor (metin `unknown` için zaten doğru, ötekiler için eksik). Hâl artık veri olarak elde — ekranı yazmak dizgi ayrıştırması gerektirmiyor.
- [~] (19.17) **Posta kodu → TÜM yerleşimler (yer adı doğruluğu + adres tutarlılığı)** *(müşteri şeridinin bulduğu açık — 01.08, kullanıcı bildirimi)*: `postal_code_place` bugün **kod başına tek satır** tutuyor (`primary key (country, postal_code)`) ve çok yerleşimli kodda üst idari birime çıkıyor. İki ayrı şeyi birden bozuyor: **(a) gösterim** — indirgenen ad geçerli bir belediye adı gibi okunuyor ve yanlış şehri söylüyor (aşağıdaki bulgu); **(b) doğrulama** — "yazılan şehir bu koda ait mi" sorusu cevaplanamıyor. İstenen: kod başına tüm yerleşimler (GeoNames dökümü zaten hepsini taşıyor, üreteç eliyor) + adın güvenilirliğini söyleyen ayrım. Sonra kapı: `placesForPostalCode(country, code) → string[]` — touches: `supabase/migrations/0033_postal_code_place.sql`, `scripts/build-postal-codes.mjs`, `packages/database/`, `packages/domain-core/src/delivery/`
  - *Bitti:* "67000 + LINGOLSHEIM" gibi bir adres kaydedilemiyor; çok yerleşimli kodda (67800 → Bischheim/Hoenheim) yanlış alarm ÖTMÜYOR; hiçbir yüzey müşteriye ait olmadığı bir belediye adını göstermiyor
  - **Kural FORMDA DEĞİL KAPIDA olmalı.** Adres formu istemcidedir ve atlanabilir; asıl karar noktası `createCheckoutDraft` — rota siparişi açılırken şehir o kodun yerleşimlerinden biri değilse sipariş `route` olarak doğmamalı. Form aynı kuralı erken söyler (müşteri ödeme adımına gelmeden düzeltsin), ama tek başına bir kapı değildir.
  - ⚠ **YAŞANMIŞ SİPARİŞ, operasyonel maliyet gerçek.** `LA-26-RFRWKK` (01.08, yerel): `postal_code: "67000"`, `city: "LINGOLSHEIM"`, `line1: "192C rue du Maréchal Foch"`, **`payment_method: cash`, `delivery_type: route`**. Ölçüm: referans tablosuna göre `67000 → Strasbourg`, `Lingolsheim → 67380`, ve **67380 rota kodlarımızda YOK** (`67000 · 67100 · 67200 · 67300 · 67400 · 67540 · 67800 · 77694`). Yani sipariş rota + kapıda ödeme olarak açıldı, oysa adres rotamızın dışında: kurye kapıya gidemez, operasyon müşteriyi aramak zorunda kalır. Yolu belirleyen tek şey posta kodu ve o kod hiçbir yerde adresle karşılaştırılmıyor.
  - 🔴 **ASIL BULGU: `place_name` bugün GÖSTERİM İÇİN DE YANLIŞ (kullanıcı yakaladı, 01.08).** 19.8'in indirgeme kuralı ("birden çok yerleşim varsa bir üst idari birime çık") kendi künyesinde *"daha geniş, ama ASLA yanlış değil"* diyor. **Bu iddia yanlış.** Fransız arrondissement'ı çoğu zaman kendi merkez kasabasının ADINI taşır, yani üst birim geçerli bir belediye adı gibi okunur ve doğru cevaptan ayırt edilemez:
    - `67800` → **"Strasbourg"** · gerçekte Bischheim / Hoenheim
    - `51300` → **"Vitry-le-François"** · gerçekte 46 köy
    - (`67240` → "Haguenau-Wissembourg" gibi tireli olanlar en azından belediye gibi okunmuyor — sorun sadece merkez-kasaba adını taşıyanlarda)

    Ölçek istisna değil kural: **kodların ~%40'ı çok yerleşimli** (FR 4.289 + DE 2.392 / 16.878). Yani vitrin hapı, adres formu ve `DeliveryPlace.placeName`'i okuyan her yüzey Bischheim'lı müşteriye "Strasbourg" diyor — tam olarak `place-types.ts`'in yasakladığı şey ("uydurulmuş şehir adı yazılmaz").
  - **İstenen ikinci alan: adın GÜVENİLİR olup olmadığı.** Öneri: `place_name` yalnız kod tek yerleşimliyken dolsun (gerçek belediye adı), indirgeme yapıldığında `null` kalsın; geniş etiket gerekiyorsa ayrı bir kolonda dursun. O zaman ekran adı **yalnız güvenilirken** gösterir, kalanında çıplak kod kalır — "📍 67800" yanıltmaz, "📍 67800 Strasbourg" yanıltır.
  - **Bu tur inen palyatif GERİ ALINDI (01.08).** Önce "şehir boşsa koddan doldur" + "kodun yerini yaz" yazılmıştı; kullanıcının bulgusundan sonra ikisi de kaldırıldı, çünkü ilki Bischheim'lı müşterinin adresine "Strasbourg" YAZIYORDU — bir yanlışı düzeltmek değil, doğruyu bozmak. Bugün bu şeritte güvenle yapılabilecek bir şey yok: hem doldurma hem "şehir bu koda ait mi" kuralı aynı veriyi bekliyor. Gerekçe `address-form.tsx`'in künyesinde duruyor ki kimse geri eklemesin.
  - **Durum (02.08) — veri, kural ve kapı indi.** `postal_code_place.place_name` KALDIRILDI, yerine `places text[] not null`: kodun kapsadığı tüm yerleşimler, indirgemesiz. Gösterilecek ad artık saklanmıyor, **türetiliyor** (`placeLabel`: tek yerleşimse adı, çoksa `null`) — tek kaynak liste, ad onun sonucu. 19.8'in yanlış adı üretmesinin sebebi tam olarak kuralın veriye gömülmüş olmasıydı; şimdi tek bir saf fonksiyonda duruyor ve aynı liste doğrulamaya da hizmet ediyor.
    - **Yeni ölçüm:** 60.496 yerleşim kaydı; kodların **6.650'si (~%39) çok yerleşimli** (FR 4.258 + DE 2.392), en kalabalık kod 55 yerleşim. **Kendi rota kodlarımızın 8'inden 7'si tek yerleşimli** — pratik etki bizim bölgemizde tek kod (67800 → Bischheim / Hœnheim).
    - **Arrondissement türevi ayrı yerleşim sayılmıyor.** GeoNames `75011` için hem "Paris" hem "Paris 11" taşıyor; ham sayımla o kod "çok yerleşimli" görünür ve **Paris çıplak koda düşerdi**. Gövdesi listede olan numaralı türev eleniyor — 30 kodu etkiliyor (Paris + Lyon), ama nüfusça en ağır 30 kod.
    - **Kapı `createCheckoutDraft`'ta ve YALNIZ rota siparişinde:** yeni `address_city_mismatch` hâli, `places` ile birlikte döner (ekran "şu olmalı" diyebilsin). Sipariş sessizce kargoya ÇEVRİLMİYOR — tür değişimi kargo ücretini ve açık ödeme yöntemlerini de değiştirir, müşteri sebebini anlamadan başka bir siparişe bakardı. Kargoda hiç sorulmuyor: adresi taşıyıcı doğrular, bizim reddimiz orada boşa sürtünme olurdu.
    - **Eşleşme cömert, çünkü bu kural REDDEDİYOR.** Diyakritik/ligatür/tire farkı siliniyor (`Hœnheim` ↔ `HOENHEIM`), arrondissement ve CEDEX eki atılıp bir kez daha deneniyor ("Paris 11e", "STRASBOURG CEDEX 2"). Ek atma yalnız kabul kümesini BÜYÜTÜR. **Boş liste engellemez:** kod referansta yoksa ölçüm yoktur ve ölçülemeyen değer sıfır değildir (`CLAUDE.md §1`) — 19.16a'nın kodları böyle korunuyor.
    - **Testlerde bulunan gizli tuzak:** iki entegrasyon dosyası rota kodunu damgadan üretiyordu (`67${damga}`, `10_000 + damga % 80_000`) ve adres şehri sabitti. Kapı inince bu, damga gerçek bir koda denk geldiğinde **tekrarlanmayan bir düşüşe** dönüşürdü (67xxx aralığında 96 gerçek FR kodu var → ~%10). Kodlar referansta karşılığı olmayan öneklere taşındı (FR 99, DE 43 — ölçüldü, 0 satır). `checkout-shipping-order`'ın künyesindeki "bu kod GeoNames'te de yok" iddiası zaten YANLIŞTI ve doğrulanmamıştı; şimdi ölçülmüş.
    - 20 yeni test (13 birim + 7 entegrasyon), ikisi yaşanmış vakaların kendisi: `67800 ≠ Strasbourg` ve `51300 + LINGOLSHEIM` reddi.
    - **Sınır düzeltildi (02.08):** bir ara `placeSummary` diye bir biçimlendirici `domain-core`'a yazılmıştı ("Bischwiller, Gries, Kaltenhouse +4" üreten). **Kullanıcı yakaladı: o bir görselleştirme, motorun işi değil.** Ayıraç ve "+X" biçimi tasarım kararıdır ve tasarımı yoktu — yani `CLAUDE.md §3`'ün yasakladığı improvizasyon. Geri alındı. Motor `placeLabel` veriyor (tek yerleşimse adı, çoksa `null`) ve **`places` listesini olduğu gibi taşıyor**; biçimi ekran kurar. Sınır şu: *"tartışmasız bir ad var mı"* bir VERİ sorusu, *"kaç ad yazılır, nerede +X denir"* bir EKRAN sorusu.
  - **Ekran şartnamesi — hapın etiketi (kullanıcı kararı, 02.08).** Adı hiç göstermemek doğru değil: kodu ezbere bilmeyen müşteri çıplak "67240"ten yerini tanıyamaz, ve orada tanımadığı tek bir köy/ilçe adı görmesi daha da kötü. Karar: **eşiğe kadar hepsini yaz, sonrasında "+X"** → "67240 · Bischwiller, Gries, Kaltenhouse +4". Bu bir iddia değil bir kümedir: Gries'te oturan kendi beldesini görür, görmese bile "+4" listenin devamı olduğunu söyler; yanlış bir tek ad ise ona başka bir yerde oturduğunu söylerdi.
    - **Önerilen eşik 3, ölçümle:** kodların **%60,6'sı zaten tek yerleşimli** (etiket = ad, liste hiç çıkmaz), %72,6'sı üç ve altı — yani "+X" ancak gerçekten kalabalık kodlarda görünür (çok yerleşimlilerin ortancası 6) ve üç ad bir hap satırına sığar. Sayı ekranın; ölçüm burada dursun diye yazıldı.
    - Veri hazır: `DeliveryPlace` ve `PlaceOption` ikisi de `placeName` (tartışmasız ad ya da `null`) **ve** `places` (tam liste) taşıyor.
  - ⚠ **`placeName` bir şehir adıdır ve öyle kalmalı** — bu yüzden çok yerleşimlide `null`. Otomatik doldurma buna güvenebilir; bir etiket (liste/"+X") oraya yazılsaydı adres alanına liste kaydedilirdi. Karşılaştırma yine de `cityMatchesPlaces`'in işi.
  - **BEKLEYEN(19.17):** iki ekran işi müşteri şeridinde — **(1)** adres formunda erken uyarı (kapı hazır: `placesForPostalCode` + `cityMatchesPlaces`), **(2)** hapta etiketin çizimi (yukarıdaki şartname).
  - ⚠ **`db:reset` gerektirir** (veri satırları migration'ın içinde). Reset KULLANICININ kararı.
  - **Kapsam dışı: sokak doğrulaması.** "Bu sokak o beldede var mı" sorusu ancak bir adres doğrulama servisiyle cevaplanır (FR için BAN — `api-adresse.data.gouv.fr`, kamu, ücretsiz, anahtarsız). Posta kodu için dış servisi reddetmiştik çünkü çözüm her katalog render'ında sıcak yoldaydı; burada çağrı **adres kaydında bir kez** olur, maliyet profili bambaşka. Ayrı bir karar — `design/BACKLOG`'a değil, sorulmayı bekleyen bir soru olarak burada duruyor.

- [~] (19.18) **Posta kodunun harita üstündeki yeri** *(tasarım kararının arka uç ön koşulu — `design/pages/admin-depolar.md` §2, 02.08)*: bölge kurulumu artık haritadan yapılıyor ve harita kod başına tek işaret basıyor; `postal_code_place` koordinat taşımıyordu. `lat`/`lng` üretece ve tabloya eklendi — touches: `supabase/migrations/0033_postal_code_place.sql`, `scripts/build-postal-codes.mjs`, `packages/types/`, `packages/database/`
  - *Bitti:* haritanın bastığı her kodun bir noktası var; noktasız kod basılmaz, (0, 0)'a düşmez
  - **Durum (02.08):** GeoNames dökümündeki enlem/boylam (sütun 10-11) okunuyor; nokta kodun kapsadığı **yerleşimlerin ortalaması** — birini seçmek keyfi olurdu, aynı "tek ad" hatasının coğrafi karşılığı. 6 hane (≈10 cm) sabit, üretim tekrarlanabilir kalsın diye. Ölçüldü: **60.496 kaydın hiçbirinde koordinat eksik değil.** Kolon yine de nullable ve `postal_code_place_point` kısıtı ikisini birlikte tutuyor — yokluk bir değer değildir, ve (0, 0) Gine Körfezi'nde bir işaret üretirdi. 19.17 ile **aynı reset**e binmesi için o tur içinde indi; ayrı bir reset istemiyor.
  - **BEKLEYEN(19.18):** haritanın OKUMASI yazılmadı — kaç kod hangi turda çekilecek operasyon şeridinin sorusu. Ölçüm karar için: FR+DE 16.878 nokta; yalnız `(ülke, kod, lat, lng)` ≈ 590 KB JSON (~120 KB gzip), `places` de eklenirse ~1,5 MB. Yani "hepsini tek turda" masaüstü bir kurulum ekranı için taşınabilir ama etiketler için görüş alanı (bbox) okuması daha doğru olabilir; şekli isteyen taraf söylesin, kapı bir metotluk iş. İndeks de o karara bağlı (bbox gelirse `(lat, lng)` yetmez, GiST gerekir).
  - 🔴 **CSP haritayı BUGÜN engelliyor — tasarım dokümanı bunu bilmiyor.** `apps/web/next.config.ts` sıkı bir politika yayınlıyor (`default-src 'self'`, host'lar tek tek sayılı: Supabase · R2 · Stripe). MapLibre + OpenFreeMap iki yerden düşer ve ikisi de **sessiz değil ama teşhisi zor**: harita boş çizilir, sebep yalnız konsolda görünür.
    - **`worker-src` yok** → `default-src 'self'`e düşüyor; MapLibre GL kendi işçisini **blob URL'inden** kuruyor ve engelleniyor. Gereken: `worker-src 'self' blob:`
    - **`connect-src`'de karo host'u yok** → stil JSON'u, vektör karolar, glif ve sprite çağrılarının hepsi `fetch` ile gidiyor ve düşüyor. Gereken: OpenFreeMap host'unun eklenmesi
    - ~~Bugün EKLENMEDİ ve bilerek: çağırmadığımız bir host'a izin vermek bedava değil, gereksiz yüzeydir (aynı gerekçe R2'nin S3 API host'u için de yazılı).~~ → **AÇILDI (07.08, kullanıcı kararı):** artık çağıracağız. `connect-src`'e `https://tiles.openfreemap.org`, ayrıca `worker-src 'self' blob:`. Dev sunucusunun yayınladığı başlıkla doğrulandı.
      - **Host bizim verimizi GÖRMEZ** ve bu ayrım kararın kendisiydi: haritanın iki yarısı var, yalnız zemin dışarıdan geliyor. Noktalar bizim (`postal_code_place`, 16.878 kod enlem/boylamıyla, `'self'`ten); dışarı giden istek yalnız karo koordinatıdır (z/x/y). Posta kodları, bölgeler ve müşteriler o tarafa hiç geçmiyor.
      - **Zemin ATLANAMAZ:** noktaları boş bir zeminde çizmek CSP'ye hiç dokunmazdı ama tasarımın gerekçesini yok ederdi (*"karar 'bu yol üstünde mi' olduğu için taban harita yol ağını gösterir"*) — o hâlde ekran bugünkü kod listesinden farksız kalırdı.
      - **SAYFAYA ÖZEL YAPILMADI:** tarayıcı birden çok CSP başlığında KESİŞİM uygular — ikinci bir başlık gevşetmez, kısar. Yalnız harita rotasına açmak, o rotayı global eşleşmeden çıkarmayı gerektirirdi; sessizce çalışmayan bir harita bırakma riski tek bir dış host'tan pahalı.
      - `next.config.ts` operasyon şeridinin dosyası DEĞİL; kullanıcı kararıyla dokunuldu ve arka uca bildirildi (`docs/talep/not-arka-uc-csp-harita-karosu.md`). Harita kodu inerken tek satırlık iş — ekran şeridi haber versin, açayım. Kendi karo sunucumuza geçilirse (tasarımın backlog notu) host `'self'` olur ve satır tamamen kalkar.
  - ⚠ **Tasarımla çelişen bir kural var — çözülmeli.** §2 *"Serbest metin girişi yoktur — haritada (yani referans tablomuzda) olmayan kod sisteme girilemez"* diyor. Ama 19.16a'nın tam gerekçesi bunun tersiydi: **GeoNames eksik olabilir ve operatörün oraya elle kod ekleyebilmesi gerekir** — kendi bölge tablomuz hizmet alanımız için otoritedir. Harita tek giriş yolu olursa referansta olmayan bir kod hizmete AÇILAMAZ; bugün kodumuz o kodu tanıyor (`resolvePlaceByPostalCode` kendi tablomuza bakıyor) ama operatör onu artık giremiyor. **Ama ölçüldü ve bugün TEORİK (02.08):** Bas-Rhin'de GeoNames 96 ayrı kod tanıyor ve örneklenen onu da (67380 Lingolsheim · 67960 Entzheim · 67114 Eschau · 67150 Nordhouse · 67210 Valff · 67600 Ebersheim · 67500 · 67700 · 67120 Molsheim · 67370 Wiwersheim) referansta mevcut — küçük köyler dahil. Yani hizmet bölgemizde harita ile elle giriş arasında fark doğuran bir kod yok. Karar ERTELENEBİLİR: haritayı yazan şerit istisna kapısını (liste üzerinden elle giriş + "bu kod referansta yok" uyarısı) koyar ya da koymaz; koymazsa 19.16a fiilen ölür ama bugünkü veriyle kimsenin önü kesilmez. Yeni bir ülkeye/bölgeye açılırken yeniden ölçülmeli — açık kalması gereken bir soru, çözülmesi gereken bir arıza değil.

## Netleşecekler

- ✅ **Tasarım paketi GELDİ (01.08):** iki sayfalar-üstü sözleşme (`design/project/uploads/operasyon-depo-ekseni.md` + `musteri-yer-ekseni.md`) + 14 `.dc` güncellemesi (müşteri: anasayfa/katalog/ürün/sepet/checkout/siparişler/envanter K36-K39; operasyon: stok/siparişler/satın alma/dashboard/rotalar/envanter/sidebar). Depolar ekranı **ayrı turda**.
- ✅ **Sayfa dokümanları YAZILDI, sonra BİLGİ MİMARİSİ DÜZELTİLDİ (01.08, kullanıcı kararı).** İlk tur `admin-depolar.md` + `admin-transfer.md` üretti; kullanıcı depo giriş/çıkışının dört ekrana dağılmasının yönetimi zorlaştırdığını bildirdi ve inceleme kök sebebi buldu: **parada birleşik hareket defteri var, stokta yok** (`DOMAIN §7` "para bir hesapta durur, hareketlerle girer/çıkar"; stokta sekiz RPC beş tabloya yazıyor, tek defter yok). Sonuçta:
  - **`admin-transfer.md` SİLİNDİ** — parada hesaplar arası transfer bir hareket TİPİDİR, kendi sayfası yoktur; depolar arası transfer onun stok karşılığıdır. İçeriği `admin-stok.md`'ye taşındı: **Mal kabul** (girenler + bekleyen tedarik siparişi/transfer) + **Çıkışlar** sekmeleri, sevk/kabul birer form. Sekme adları soyut "Hareketler" DEĞİL (kullanıcı itirazı, 01.08): depoda kimse "hareket yaptım" demez. Tek somut ad da yetmedi — "Mal kabul" içeriğin yarısını adlandırıyordu; çözüm yönü sekmeye çıkarmak oldu.
  - **Bölge tanımı Rotalar → Depolar'a taşındı.** Terminoloji karışıklığının kaynağı buydu: **bölge bir TANIM** (kodlar + günler + depo bağı, kurulum işi), **rota bir GÜN** (bugünün teslimat listesi — veride varlığı bile yok, `delivery_date`'ten türer). İkisi aynı sayfadayken sözcükler birbirinin yerine geçiyordu. Rotalar artık yalnız gün planı + kurye ataması.
  - **Depolar büyüdü:** künye + hizmet alanı (bölge/posta kodu tanımı) + **karne** (risk, eşik altı, yolda bekleyen). Karne SAYAR, listelemez — her sayı Stok'a o depo bağlamıyla gider; parti listesi orada yaşar, burada tekrarlanmaz.
  - **Hedef depo seçicisi kapsam süzgecine tabi DEĞİLDİR** (karar korundu): kapsam okunabilen veriyi sınırlar, hedef listesi malın nereye gidebileceğidir — aksi hâlde tek kapsamlı depocu hiçbir yere sevk edemezdi.
  - ⚠ **Arka uca tek istek doğdu:** `order_item_batch`'te zaman damgası yok (`id, order_item_id, stock_id, qty`) — zaman sıralı hareket defterinde hazırlık satırları yerini bulamaz. `created_at` gerekiyor. Defterin kendisi yeni tablo istemiyor: beş tablo da `stock_id`/`warehouse_id` taşıdığı için `stock_movement` bir **`union` görünümü** olarak türetilebilir (`available_stock` deseni).
- ✅ **Operasyon yüzeyinin arka uç talebi CEVAPLANDI (01.08)** → `depo-ekseni-operasyon-arka-uc-talebi.md` (arşiv). Altı madde, altısı da kabul; ikisinde biçim değişti: `WarehouseService.list` kapsamı `WarehouseScope` değil `warehouseIds` alır (motor tipi servise giremez — `STACK §4`, `boundaries` lint'i de geçirmez), ve test depoları `is_active=false` DOĞAMAZ (`available_stock` görünümü `where w.is_active` süzüyor; pasif depodaki stok hiçbir okumada görünmez ve stok testleri kırılır) → süpürme komutu. `cancelled` için kritik bulgu: `TransferStatusEnum`'da **taslak hâli yok**, sevk transferin ilk kalıcı anı — yani iptal her zaman sevk edilmiş bir kaydın düzeltilmesi; fiziksel geri dönüş ayrı (ters) transferdir.
- [x] (19.19) **Posta kodu autocomplete'i + tanınmayan adresin işareti** *(iki şeridin talebi — 02.08)*: `PostalCodePlaceService.searchPrefix(prefix, limit)` → `{ country, postalCode, places, inRoute }[]`; adres uyarısı motoru `addressAnomalies`; `constraintOf(err)` kısıt adını ekrana ulaştırır — touches: `packages/database/`, `packages/domain-core/src/delivery/`, `supabase/migrations/0033_postal_code_place.sql`
  - *Bitti:* müşteri kodu yazarken öneri alıyor; tanınmayan/çelişen adres operasyonda işaret taşıyor; kısıt ihlali indeks adı olarak düşmüyor
  - **Durum (02.08):** üç talebin karşılığı. Kaynak: `yer-ekseni-arka-uc-talebi.md` §7-§8 (müşteri şeridi) + `depo-ekseni-operasyon-arka-uc-talebi.md` §7 (operasyon şeridi).
    - **`searchPrefix` AYRI bir kapı, `resolvePlaceAction`'a bayrak değil** — o kapının içinde `recordDemand` var ve her tuşlanan kod "bölge dışı talep" sayacına düşerdi. 19.7'nin kararı bunu adıyla uyarıyordu (*"bayrağı unutan ilk çağrı sayacı yine kirletir"*); ayrı fonksiyon hatayı **yapısal olarak** imkânsız kılıyor. Öneri bir OKUMA, onay bir NİYET.
    - **`inRoute` sıralamayı belirler, seçimi DEĞİL:** rota adayı öne alınır (doğru cevap listenin dibinde saklanmaz) ama otomatik seçilmez — iki adayın farkı yalnız teslimat yolu değil **KDV oranıdır** (19.8).
    - **Etiket kurulmaz:** `places` ham döner, kısaltma (“+4”) ekranın kararı (`CLAUDE.md §3`) ve hazır bir dize üç dilde çalışmaz.
    - **İki harften kısa önek reddedilir** — başarım değil ANLAM: "6" kodların onda birine uyar, gösterilecek sekiz satır rastgele olurdu.
    - **İNDEKS ÖLÇÜLDÜ:** `postal_code like '672%'` seq scan ile **36,9 ms**, `text_pattern_ops` ile **0,11 ms** (16.878 satır, yerel `explain analyze`). Uç tuş yolunda. **İkinci indeks eklenmedi**, mevcut `postal_code_place_code`'un işleç sınıfı değişti — eşitlik o sınıfla da çalışıyor, kaybedilen tek şey harmanlamaya duyarlı sıralama ve bu kolonda öyle sorgu yok. ⚠ `db:refresh` ister; o çalışana kadar sorgu DOĞRU cevap verir, yalnız yavaş.
    - **Adres uyarısı TÜRETİLİR, siparişe yazılmaz.** Sorduğu soru bugünün sorusu ("bu adrese gidebilecek miyiz"), geçmişin kaydı değil; bölge genişleyince asılı kalan uyarı gürültü olur. Anlık görüntü ilkesiyle çelişmiyor: saklanan şey zaten ADRESİN KENDİSİ, uyarı onun bugünkü referansla karşılaştırılması.
    - **İki uyarı var, biri talepte istenenden güçlü:** `unknown_code` (kod ne bizde ne referansta) ve `city_mismatch` (kod tanınıyor, şehir çelişiyor). 19.17'yi doğuran yaşanmış vaka (`67000` + `LINGOLSHEIM`) **ikinci sınıftı** — yalnız tanınmayan kodu işaretlemek asıl vakayı kaçırırdı. Kendi bölge tablomuz referansın üstünde (19.16a); şehirsiz adres uyarı doğurmaz (eksik alan çelişki değildir); ikisi birden basılmaz.
    - **`constraintOf` ölçümle yazıldı:** PostgREST hatası yapılandırılmış `constraint` alanı TAŞIMIYOR, ad yalnız `message`'da ve tırnakta. Talep "mesajı regex'leme" diyordu — kırılgan olan mesajın NESRİ, tırnaklı tanımlayıcı değil (`lc_messages` cümleyi çevirir, `"%s"` yerinde kalır). İki ayrıntı ölçümden çıktı: **son** tırnaklı ad alınır (`23514` ikisini birden taşır, ilki tablo) ve **`23502` listede yok** (o mesajda kısıt adı yoktur, son ad ilişkinindir). RPC'lerin `P0001` hataları `null` döner — ayrı mekanizma, karıştırılmadı.
    - **Yan etki — `STACK §6` borcu ödendi:** bölge↔posta kodu junction'ı `DeliveryZoneService` içinde **ham `this.supabase`** olarak okunuyordu (iki yerde). Yeni bir ham sorgu eklemektense `DeliveryZonePostalCodeService` açıldı; eski iki çağrı da ona geçti. Kod normalizasyonu (`normalizePostalCode`) tek yerde.
    - **Toplu kapı (`checkAddresses`) BİLEREK inmedi:** tüketicisi olmayan kapı ölü koddur (`knip` geçirmez). Motor kuralı + iki toplu okuma hazır (`listByPostalCodes` · `listByCodes`); kapı bunların otuz satırlık birleşimi ve **sipariş listesi okumasıyla aynı turda** inecek — anahtarı `(ülke, kod)` olmalı.
    - 12 yeni test (6 önek + 6 uyarı) + 5 `constraintOf` birim testi. Örnekler uydurulmadı: kısıt hataları yerel veritabanına gerçek ihlaller yaptırılarak alındı.
- ✅ **Müşteri yüzeyinin arka uç talebi CEVAPLANDI (01.08)** → `yer-ekseni-arka-uc-talebi.md` (arşiv; durum tutmaz). Altı istek, altısı da kabul → görev satırları 19.8–19.12 + taşıyıcı/takip alanı `07`'ye. Tek ekleme: çözüm kapısına dördüncü hâl (`unknown` — geçersiz posta kodu bugün sessizce kargoya düşüyor). **Tasarım sapması onaylandı:** `musteri-yer-ekseni.md §6`'daki koşullu ülke seçici kalkıyor; ülke bir alan değil, posta kodundan türeyen sonuç. Gerekçe iki katlı — sürtünme (kullanıcı, 01.08) ve **vergi**: serbest seçilen ülke KDV oranını ve Alman B2B muafiyetini etkiler, beyan olamaz. Aynı dokümandaki `67000 FR+DE` örneği ölçümle çürütüldü (67000 Almanya'da geçerli değil) — tasarım metni düzeltilecek.
- **Kasa → merkez aktarım mekaniği** para modülünde (12) netleşir — şema değişmez (depo başına `Account` satırı yeter).
- ⚠ **Almanya ön koşulu:** DE deposu açılmadan mali danışman (`DOMAIN §5` KDV uyarısı + §17).

- [~] (19.20) **Bölge kurulumu — harita** *(tasarım: `Depolar - Bolge Haritasi.html`, `design/pages/admin-depolar.md §2`)*: bölge kararı coğrafi bir karardır (operatör kodu değil YOLU bilir) ve tasarım asıl giriş aracını harita olarak tanımlıyor; bugün yalnız liste yolu var (aranabilir seçici, `PostalCodePlaceService.searchPrefix`). ~~MapLibre GL JS + OpenFreeMap vektör karoları (teknik karar 02.08, bağlayıcı)~~ → **Leaflet + raster karo** (07.08, gerekçe aşağıda); kodlar `postal_code_place.lat/lng`'den biner (19.18 indirdi); tıkla-ekle/tıkla-çıkar, kod hâlleri ayrışır (bu bölge · başka bölgede tanımlı · boşta), poligon sınırı v1'de YOK — touches: `apps/web/app/(operations)/operations/deliveries/`, `apps/web/components/operation/`
  - *Bitti:* haritadan seçilen kod listeye düşüyor ve tersi; başka bölgenin tuttuğu koda tıklayınca kim tuttuğu ve taşıma yolu görünüyor
  - **Durum (07.08) — HARİTA ÇALIŞIYOR, "boşta" kodlar hariç.** Kurulum Depolar'dan **Teslimat'a taşındı** (kullanıcı kararı 07.08): *"bir bölge tanımlamak = bir dağıtım güzergâhı tanımlamak."* `/operations/deliveries` iki sekme (`Gün planı` · `Rotalar`), sekme adreste (`?tab=routes&route=<id>`), diyalog yerine tam sayfa `grid-cols-[1fr_380px]`. Arayüz dili tek kelimeye indi: **rota**; `delivery_zone` iç ad olarak kaldı. Depolar rotaları yalnız okuyor, `Düzenle` buraya köprü veriyor.
    - **MapLibre → Leaflet (kullanıcı bildirimi):** *"Claude Design bunu gayet düzgün çalıştırmıştı."* Görev satırındaki teknik nota uyup çalışan tasarımı açmamıştım; tasarım Leaflet + raster karo kullanıyor. Fark üç turluk arızanın kendisiydi — MapLibre vektör karoyu **Web Worker**'da çözüp **WebGL** ile boyuyor, zincirin bir halkası kopunca stil yükleniyor ama tuval BOŞ kalıyor. Leaflet karoyu `<img>` yapar: worker yok, WebGL yok, CSP'de yalnız `img-src` gerekir (`worker-src`/`connect-src` geri alındı). **Ders:** görsel karar `.dc.html`'de verilidir (CLAUDE §3); bir görev satırının teknik notu çalışan tasarımı ezmez.
    - **Tasarıma birebir getirildi (07.08):** üç kod hâlinin yarıçapı ve rengi (8 · 7 · 6,5 — kendi kodun en iri, boşta içi boş), sol üstte "Kod hâlleri" lejantı, sağ üstte yakınlaştırma, alt ortada 2,6 sn'lik ipucu şeridi, **z13'ten sonra kalıcı etiket** (kod + yerleşim adı). Tasarımın ham hex'leri palette birebir yok; aile korunarak token'a eşlendi (`taken` → mavi ailesi = "bilgi", engel değil).
    - **Sayfa SSR'da 500 dönüyordu — ÖLÇÜLDÜ ve kapatıldı.** `leaflet` modül düzeyinde `window`a dokunuyor, Next ise istemci komponentlerini de sunucuda bir kez çiziyor: `GET ?tab=routes` → **500** (`Error: window is not defined`). Ekran istemcide toparladığı için gözle görünmüyordu; `ui:shot` konsol kaydı ile HTTP durumu açığa çıkardı. Çözüm üç dosyaya ayırmak: `zone-map-model.ts` (saf tip/sabit, sunucudan da okunur) · `zone-map-leaflet.tsx` (gövde) · `zone-map.tsx` (`next/dynamic`, `ssr: false`). Aynı koşuda ikinci bir arıza daha çıktı: `ToggleField` dış `<button>`'ı `Toggle`'ın iç `<button>`'ını sarıyordu (geçersiz HTML + hidrasyon hatası) — `Toggle` artık `onChange` verilmediğinde `<span>` üretiyor, rolü koruyarak.
    - **Boşta kodlar için ölçüm yapıldı (kullanıcının sorusu: "hepsini birden göstermek problem olur mu, performans?").** Toplam 16.878 kod (FR 6.065 · DE 10.813), hepsinin koordinatı var. Komşu kodlar arası ortanca mesafe 0,039°; 1200×800 px haritada z=13'te 229 px, z=10'da 29 px, z=9'da 14 px, z=8'de 7 px. Nokta çapı 13–16 px → **z=9'da noktalar değiyor, z=8'de tek lekeye dönüşüyor.** Yani sınırlayan şey çizim maliyeti değil (tuval 4.000 noktayı taşır), **tıklanacak noktanın ayırt edilememesi.** Eşik `FREE_CODE_MIN_ZOOM = 10` (`zone-map-model.ts` künyesinde tam tablo): z=10 hem ayrık hem sınırlı (en yoğun yer Ruhr: 661 kod ≈ 30 KB) ilk kademe.
    - **BOŞTA KODLAR ÇİZİLİYOR — görüş alanına göre (07.08).** Ekran boştaki kodları haritanın o anki kutusundan okuyor: `readMapCodesAction` → `readPostalCodesForMap({bbox})` (arka uç şeridi aynı gün teslim etti). Harita `moveend`'de görüş alanını 250 ms gecikmeyle bildiriyor, istemci çekiyor, eski cevaplar damgayla eleniyor (yarış koşulu). Nereye kaydırılırsa orası çiziliyor — Paris'te de, Colmar'da da.
      - **Ara çözüm ve neden bırakıldı:** kapı gelmeden önce havuz "rotaların dokunduğu bölgeler"in (kodun ilk iki hanesi) kodlarından kuruluyordu — `searchPrefix(önek, 400)` + `listByPostalCodes`. Çalışıyordu ama bilinen bir sınırı vardı ve **kullanıcı onu ekranda gördü**: *"Paris tarafına gittiğim zaman herhangi bir şey görünmüyor, bu normal mi?"* Kapı gelince havuz sökülü; sayfa okumasından dört sorgu düştü.
      - **Yakınlık eşiği isteğin kendisini kapatıyor:** z10'un altında istek HİÇ atılmıyor (`FREE_CODE_MIN_ZOOM`). Sebep çizim maliyeti değil — o yakınlıkta noktalar birbirine değip tıklanamaz hâle geliyor, yani getirilen veri kullanılamazdı. Tanımlı kodlar ve öneriler her yakınlıkta kalır: onlar aday değil, ekranın kendi mesajı.
      - **`truncated` YAZILIYOR:** kapı tavana dayandığını bildiriyor ve lejant bunu cümleye çeviriyor. Sessiz kesme, operatöre görmediği kodu "yok" diye okuturdu.
      - **`places[0]` KULLANILMIYOR, `placeLabel` motoru kullanılıyor** (arka uç şeridinin uyarısı, 07.08): çok yerleşimli kodda keyfi ilk ad otorite gibi okunuyor — `67800` "Strasbourg" değil, Bischheim/Hœnheim; kodların ~%39'u etkileniyor. Motor çok yerleşimlide `null` veriyor, etiket yalnız kodu gösteriyor. Okuma ve öneri motoru ikisi de buna geçirildi.
      - **Ağırlık rayı adaylar için de okunuyor:** operatör boştaki bir koda tıklayıp eklediğinde sipariş/ciro anında görünüyor. Kargo yoluyla o koda giden siparişler tam olarak *"burayı rotaya almalı mıyım"* sorusunun kanıtıdır.
    - **Tıklamanın üçüncü dalı bugün de ulaşılabilir:** operatör kendi kodunu çıkarınca nokta "boşta"ya döner ve aynı noktaya ikinci tıklama onu geri getirir. O dal olmasaydı yanlışlıkla çıkarılan kod geri konulamazdı.
    - **Kod analitiği rayı yazıldı (kullanıcı isteği 07.08: *"rota düzenlemesi yapılırken posta kodlarıyla alakalı analitikler"*).** Seçili rotanın kodları **ciroya göre** sıralı: kod · sipariş sayısı · ciro (+ bekleyen varsa rozet), üstte rota toplamı. Sıra alfabetik DEĞİL çünkü soru "hangi kod" değil *"yükü kim taşıyor"* — ölçüldü, Strasbourg Merkez'in üç kodundan yalnız `67000` sipariş getiriyor (10 sipariş / 1.163,60 €), ötekiler sıfır. Kapılar zaten vardı: `AnalyticsReportService.postalCodeOrders(codes)` (kod listesiyle çalışan RPC) + `ZoneNoticeService.pendingCountByPostalCode()`; yeni kapı istenmedi. **Sınır bilinçli** — ürün kırılımı, marj, geri bildirim puanı, kohort/RFM dışarıda: onlar Analitik'in işi ve rota kurarken verilecek kararı değiştirmiyorlar.
      - **Anonim talep sayacı (`postal_code_demand`) bu raya KONMADI:** tek okuması bir liderlik tablosu (`listTop`), kod bazında sorulamıyor. Listede olmayan kod için "0 talep" yazmak, ölçülemeyeni sıfıra düşürmek olurdu (`CLAUDE §1`). Sayacın evi Depolar'daki talep tablosu (19.21). Kod bazında sorulabilmesi gerekirse ayrı bir kapı ister. BEKLEYEN(19.20)
      - **Ölçülmemiş kod "0 sipariş" yazmaz, "ölçülmedi" yazar** ve listenin SONUNA düşer: taslağa yeni eklenmiş koda RPC'ye hiç sorulmadı. Sıfır yazmak, kaydedilmemiş bir kodu "hiç getirmiyor" diye okutup çıkarılmasına yol açardı.
      - ⚠ **RPC ülkeye bakmıyor** (`analytics_postal_code_orders` yalnız `postal_code` eşliyor): `67000` hem FR hem DE'de geçerli olduğu için iki ülkenin siparişi tek sayıda birleşir. Bugün tek ülkeli rotalarda görünmez; sınır deposu (Kehl) büyürse ayrışması gerekir. BEKLEYEN(19.20)
    - **ÖNERİLEN KODLAR — "akıllı bölge" (kullanıcı isteği 07.08).** *"Bir bölgeye eklenmemiş posta kodlarından kullanıcıya önerilerde bulunulması gerekiyor… o kodların rengi farklı olabilir, üzerine gelince neden önerildiğini ifade eden bir şey çıkabilir."* Üçü de yazıldı: harita üzerinde **mor** nokta (envanterin kendi "öneri" ailesi — `--color-ops-violet-bg` künyesinde "öneri kutusu" yazıyor), üzerine gelince gerekçe, sağ rayda `Önerilen kodlar` listesi (tıkla → rotaya ekle).
      - **Öneri bir TAHMİN değil, HATIRLATMA.** Uydurulan sinyal yok; sistemin zaten topladığı üç kanıt bir araya getiriliyor: **bekleyen kişi** (`zone_notice` — kimlikli, izinli), **gitmiş sipariş** (`analytics_postal_code_orders` — bugün kargoyla hizmet ediliyorlar), **sorulma** (`postal_code_demand`). Ekran ham sayıları gösteriyor, puanı DEĞİL: *"3 kişi haber bekliyor · 47 kez soruldu"*. Bir "87 puan" sorgulanamaz, kanıt sorgulanabilir. "Nüfus / gelir düzeyi / rakip yoğunluğu" gibi elimizde OLMAYAN sinyaller eklenmedi — olmayan veriyi tahmine çevirmek öneriyi kehanete döndürürdü.
      - **Talep liderlik tablosu (`listTop`) burada TAM DOĞRU kapı** ve bu, ağırlık rayındaki kısıtın tersi: ray "şu kodun talebi kaç" diye soruyordu (kod bazında, kapı yok); öneri "en çok sorulanlar hangileri" diye soruyor — liderlik tablosunun cevapladığı soru. Aynı kapı, sorunun yönü değişince engelden kaynağa döndü.
      - **Talep sayacındaki kodlar bölge havuzunun DIŞINDA olabilir ve değeri tam da bu:** `68000` (Colmar) hiçbir rotanın bölgesinde değil ama 18 kez sorulmuş, 1 kişi bekliyor. Havuzla sınırlı kalsaydı öneri motoru yalnız zaten baktığımız yeri önerirdi.
      - **Uzaklık ELEMİYOR ve SIRALAMAYA da girmiyor (kullanıcı kararı 07.08).** Bir süre 80 km'lik bir tavan vardı ve onu ekran koymuştu (`CLAUDE §4`: parametrik değer sorulmaz, varsayılan konur ve bildirilir). Kullanıcı sorguladı — *"böyle bir sınırdan bahsettiğimi hatırlamıyorum, neden var?"* — ve kaldırılmasına karar verdi: uzak bir kodu rotaya almak anlamsız olabilir ama **karar operatörün**, ekranın değil. Yerine geçen şey süzgeç değil sıralama: kanıtı güçlü olan üste çıkıyor, altı satırlık tavan zayıfları zaten dışarıda bırakıyor. **Ders:** eşik koymak serbest, ama koyduğunu ayrıca işaretlemek gerekiyor — özet metnin içine gömülen bir "bildirdim" bildirim sayılmıyor.
      - **Uzaklık yine gösteriliyor ama İSTEMCİDE ve DÜZENLENEN rotaya göre.** Sunucuda hesaplanırken bir kusur vardı (kontrol sırasında bulundu): mesafe tüm rotaların kodlarına ölçülüyordu, yani Strasbourg düzenlenirken Kehl rotasının 5 km yanındaki kod "rotaya 5 km" diye görünüyordu. Taslağa göre hesaplanınca hem doğru oluyor hem kod ekledikçe canlı güncelleniyor. Rotanın hiç kodu yoksa mesafe YAZILMIYOR — "0 km" ölçülemeyeni ölçmüş göstermek olurdu.
      - **Ağırlıklar parametrik ve gerekçeli** (`WAITING 10 · ORDER 6 · REQUEST 1`): oran sinyalin MALİYETİdir — bekleyen kişi e-postasını verip izin işaretledi, sipariş veren ödedi ama zaten hizmet alıyor, soru soran yalnız bir kutuya yazıp çıktı. Puan ekranda hiç görünmüyor, yalnız sıralamayı belirliyor.
      - **Motor I/O'dan AYRI dosyada** (`routes-suggest.ts`) ve testli (12 test, DB'siz): `CLAUDE §1`'in "domain-core = saf karar (DB'siz, testli); database = saf I/O" ayrımı. Sessizce bozulabilecek üç kural test altında — sinyal şartı, eleme (zaten rotada / uzak / ölçülemez), sıralama. `now` motora DIŞARIDAN veriliyor ki test tarihe bağlı olmasın.
    - ⚠ **Karo kaynağı `tile.openstreetmap.org` — KAMUSAL sunucu**, ağır kullanım için değil. Bir avuç operatörün rota kurduğu iç ekran için uygun; yayına çıkarken kendi sağlayıcımıza geçilmeli. BEKLEYEN(19.20)
    - **ÜÇ AÇIK KAPANDI — kullanıcının kendi arayüz testinden** *(14.08 · `docs/denetim/backlog-operasyon-web.md` `OB-01` · `OB-03` · `OB-04`; yazıldı 15.08)*. Üçü de aynı ekranın açığıydı ve tek turda gitti; kalan `OB-02` (Shift+sürükle alan seçimi) aynı ekranın işi ve sırada.
      - **`OB-01` · YENİ ROTA HİÇ KURULAMIYORDU.** Kaydet'e basınca ekran *"Depolar sayfasından depoyu seçip 'Rota ekle' ile gelin"* diyordu. Kök ölçüldü: depo yalnız KAYDETME anında üç kaynaktan çözülüyordu (seçili rotanın deposu · adresteki `warehouseId` · sistemde tek depo varsa o) ve **formda depo seçecek hiçbir kontrol yoktu** — yani ekran, operatöre o sayfada yapamayacağı bir şeyi tarif ediyordu. Depo artık taslağın alanı; seçici **addan ÖNCE**, çünkü depo rotanın ülkesini belirliyor (`homeCountry` → kod etiketleri) ve sonra sorulsaydı seçilmiş kodlar sessizce yabancı ülkeye düşerdi. Kaydet düğmesi artık ad gibi depoyu da bekliyor: **engel tıklamadan önce okunuyor.** Pasif depo listeden SÜZÜLMÜYOR, işaretleniyor — süzmek, bugün pasif bir depoya bağlı rotayı açan operatöre kendi deposunu gizlerdi. Var olan rotanın deposu değiştirilebilir ve bu yeni bir yetenek değil: `saveZoneAction` `warehouseId`'yi zaten her kaydetmede yazıyordu, yalnız hep aynı değeri.
      - **`OB-04` · ÇOK YERLEŞİMLİ KOD HARİTADA ADSIZDI** (kodların ~%39'u). Kök beklenenden derindi: ad kararını `placeLabel` veriyor (tek yerleşimse adı, çoksa `null`) ve **iki okuma da diziyi orada atıyordu** — ekranın elinde hiç ad kalmıyordu. Doğru çözüm `placeLabel`in kendi künyesinde zaten yazılıydı: *"`null` gördüğünde ne yazılacağı … ekranın kararıdır ve `places` onun elinde."* Ayrım baştan tasarlanmış, ama veri ekrana hiç ulaşmıyormuş. `places` artık ham geçiyor; kırpma çizim anında (`placesLabel`, operasyon sözlüğü): kalıcı harita etiketi **3 ad**, üzerine gelince **tam liste**. Yük gerekçesi ("yüzlerce noktada tüm listeyi taşımak") ölçülünce tutmadı — kod başına ortalama ~1,4 ad, nokta tavanı 1200. Bu arada bir kopya öldü: aynı "iki ad + `+k`" biçimi seçicide ikinci kez yazılmıştı.
      - **`OB-03` · KODU BİLMEYENİN HİÇBİR YOLU YOKTU.** Arama yalnız kodun ilk haneleriyle çalışıyordu; rota kurarken operatörün bildiği şey ise YER. `places` bir dizi (`text[]`) ve elemanlarında parça araması okuma katmanından (PostgREST) yapılamıyor — bu yüzden aranabilir hâli veritabanı tutuyor: `places_search` **üretilmiş** kolon (`generated … stored`) + `pg_trgm` GIN indeksi. Önek değil PARÇA araması, çünkü düzleştirilmiş metin kodun BÜTÜN adlarını yan yana taşıyor ve önek yalnız ilkini bulurdu — `OB-04`'te kapattığımız körlüğün arama tarafı olurdu.
        - **ÖLÇÜM BİR TUZAK YAKALADI:** ilk tasarım kolonun içine doğrudan `array_to_string(places,' ')` yazıyordu; `provolatile` sorulunca **STABLE** çıktı ve üretilmiş kolon yalnız IMMUTABLE kabul eder — migration uygulanırken patlayacaktı. Yerine `text[]` alan IMMUTABLE bir sarmalayıcı (`place_search_text`); daraltılmış girdi üzerinden verilen geçerli bir taahhüt. Mantığın tamamı **geri alınan bir işlem içinde** doğrulandı (`Hœnheim → hoenheim`, `Straßburg → strassburg`, kolon kabul edildi, GIN indeksi kuruldu) — veritabanına kalıcı hiçbir şey yazılmadan.
        - **`searchPrefix` → `search`: tek kapı, iki dal.** Dal seçimi TERİMİN kendisinden okunuyor (harf var mı — FR/DE kodları tamamen sayısaldır), çağırandan bayrak istenmiyor: bayrağı geçirmeyi unutan ilk çağrı sessizce eski davranışa düşerdi. Kod dalı birebir korundu.
        - **`normalizePlaceName` `domain-core`'dan `@lezzet/helper`a TAŞINDI.** `packages/database` aynı normalleştirmeyi uygulamak zorunda ama `domain-core`'u bilemez (`STACK §4`); ikisi de `helper`ı bilir. Kural artık üç yerde yaşıyor — TS iki, **SQL üçüncü** (`place_search_text`) — ve testleri karşılaştırma yerine SABİT DEĞERLE çiviliyor: iki taraf birlikte kaysa bile kırılsın diye.
        - **MÜŞTERİ YÜZEYİ BİLEREK GENİŞLEMEDİ.** Kapı harf görünce ad arıyor; adres formu (`suggestPostalCodesAction`) ve mobil öneri ucu (`suggestPlaces`) yan etkiyle ad aramaya başlayabilirdi — istenmemiş ve o yüzeylerde ölçülmemiş bir davranış. İkisinde de bugünkü sonuç birebir korundu (harfli terim zaten boş liste dönüyordu). Genişletmesi `21.28`in web karşılığıyla aynı turda yapılır.
      - **EKRAN YERLEŞİMİ YENİDEN KURULDU (17.08, kullanıcının arayüz turu).** Harita artık içerik alanının TAMAMI; sağdaki 380 piksellik sütun **haritanın üstünde yüzen** 320 piksellik bir panele döndü ve katlanabiliyor. Gerekçe ekranın kendi künyesinde zaten yazılıydı — *"asıl iş yüzeyi harita"* — ama düzen bunun tersini yapıyordu: haritanın sağ kenarı hiç yoktu. Katlama **koşullu render DEĞİL `hidden`**, çünkü panel her açılışta yeniden kurulsaydı kod arama kutusunun yazısı ve rayın kaydırma yeri silinirdi.
        - **Rota seçimi ile "yeni rota" TEK açılır kutuda** *(kullanıcı: "seçimi hiç anlaşılır olmamış")*. Liste panelin en değerli yerini yiyordu ve satırların tıklanabilir bir seçim olduğu görünmüyordu. `+ Yeni rota` o listenin son maddesi — operatörün sorusu tek: "hangi rotayı düzenliyorum?", cevaplardan biri "henüz yok".
        - **Öneri rayı TAM LİSTEDEN "ekran dışındakiler"e indi.** Liste haritayla neredeyse tamamen örtüşüyordu; ipucu zenginleşince (aşağıdaki madde) geriye haritanın **yapısal olarak** yapamadığı tek iş kaldı: bakılmayan yeri göstermek. `68000 Colmar` görüş alanının dışındayken haritada hiç yoktur. Tıklama artık "ekle" değil **"oraya bak"** (`ZoneMapProps.focus`; yakınlaşma `FREE_CODE_MIN_ZOOM`in altına düşmez, yoksa gidilen yerde nokta çizilmezdi). Görüş alanı ölçülmeden ekran hiçbir şey iddia etmez — "0 öneri dışarıda" demek ölçmediğini ölçmüş göstermek olurdu (`CLAUDE §1`).
        - **Harita ipucu ÇOK SATIRLI ve zenginleşti** *(kullanıcı: "her şey tek satırda okuması karmaşık, birden fazla şehir birleşmişse çok uzuyor")*. `ZoneMapPoint.note` tek dizeden **satır dizisine** çevrildi: satırın nerede biteceğini metni kuran taraf bilir, haritanın dizeyi ayırıcıdan bölmesi ilk noktalı adda yanlış bölerdi. İpucu artık DOM kuruyor (`textContent`), HTML dizesi değil — satırların içinde veritabanından gelen yerleşim adları var. Eksik üç bilgi de eklendi: noktanın neden mor olduğu · rotaya uzaklık · talebin yaşı; üçü de yalnız rayda vardı ve ray tam bu yüzden kaldırılamıyordu.
        - **Depo bağlamı BAĞLANDI** *(kullanıcı: "yukarıdaki depo seçimi ile rotaların süzülmesi gerekir")*. Ölçüldü: sayfa `readWarehouseContext`'i **hiç okumuyordu** — `?depo=` bir süzgeç değil, yeni rota taslağının varsayılan deposuydu; öteki operasyon ekranlarının hepsi bu bağlamı okuyor. Karar **liste süzülür, harita bütün kalır** (kullanıcı 17.08): `routes-read` başlığındaki eski "depo süzgeci YOK ve olmamalı" gerekçesi (*"komşu iki güzergâhı birbirinden gizlerdi, oysa çakışan posta kodu tam orada doğar"*) haritada aynen korunuyor — `membership.taken` süzülmüş listeden değil tam kümeden hesaplanıyor. Süzülen rota sayısı menüde YAZILIYOR: sessiz süzgeç "hepsi bu" diye okunur.
        - **İKİ KATMAN ÇAKIŞMASI ÖLÇÜLDÜ VE KAPANDI.** (1) Leaflet denetimleri `z-index: 1000`, açılır menüler `body`'ye portal edilip `60` ile çiziliyor → **başlıktaki depo seçicisinin listesi haritanın ALTINDA kalıyordu**; çözüm sayıyı büyütmek değil `isolation: isolate` — Leaflet'in iç sayıları kendi kutusuna hapsedildi, yarış kazanılmadı **bitirildi**. (2) Yakınlaştırma denetimi sağ üstteydi ve yüzen panelin `+ Rota` düğmesine biniyordu → sol alta alındı (üç köşe dolu, dördüncüsü boş). Panel `bottom-8`de bitiyor: sağ alttaki OSM atıf yazısı lisans gereği görünür kalmalı.
      - **BEKLEYEN(19.20):** `OB-03` **`db:reset` bekliyor** — `places_search` kolonu henüz yerel veritabanında yok, dolayısıyla ad araması reset'e kadar boş liste döner. Bozulan bir şey yok: harfli terim bugün de boş dönüyordu, davranış aynı kalıyor. Reset kullanıcının kararı (`CLAUDE.md`).
      - **BEKLEYEN(19.20):** kullanıcı üçünü de ekranda denemedi.

- [x] (19.21) **Bölge dışı talep tablosu + bölge aktifleşince haber** *(kullanıcı kararı 04.08 — `ANALYTICS.md §6`; tasarım: `design/pages/admin-depolar.md §2`, `.dc.html` ısmarlanacak)*: hangi posta kodu hangi sıklıkla soruldu (`postal_code_demand`, anonim sayaç) + kaç kişi haber bekliyor (`zone_notice`, kimlikli/izinli — **iki sütun, toplanmaz**) + kapsanıyorsa hangi bölge/depo tutuyor; satırdan bölge kurulumuna tek adım. Tablo **Depolar ekranındadır** çünkü karar orada veriliyor; analitikte yalnız işaret + köprü olur. Bölge aktifleşince bekleyenlere gönderim bir OLAYDIR (düğme değil): idempotent, `zone_notice` üzerinde damgalı, kaydetme akışını BEKLETMEZ — touches: `apps/web/app/(operations)/operations/warehouses/**`
  - ~~⚠ **Arka uç ön koşulu (talep açık, cevap bekliyor):** okuma kapısı … bugün yok~~ → **OKUMA KAPISI İNDİ (04.08):** `readZoneDemand(limit)` (`lib/delivery/zone-demand.ts`) → `postalCode · requestCount · waitingCount · covered · firstSeenAt · lastSeenAt`. Kapsananlar dışlanmıyor (`covered` satırda), iki sayı ayrı alan — ekranda toplanamasınlar diye. Kapsama kararını **motor** veriyor (`isInRoute`), ikinci bir karşılaştırma yazılmadı. **RPC yok:** üç kümenin üçü de sınırlı (liderlik tablosu · operatörün kurduğu bölgeler · bekleyenler), `STACK §13` eşiği karşılanmıyor. Talep sayacında ÜLKE yok (`0023` yalnız kodu tutuyor — sayaç artarken ülke henüz çözülmemiş olabilir), kapı iki ülkeyi de deniyor.
  - **Aktifleşme tetikleyicisi: OLAY DEĞİL, UZLAŞTIRMA İŞİ (arka uç kararı 04.08).** Talep "bölge kaydedilince gönderim doğsun" diyordu; gerekçesi doğru ama yetersiz — **olay da kaçırılabilir.** Bölgeye kod migration'la, elle SQL'le ya da bugün olmayan ikinci bir ekrandan da girebilir ve o yolların hiçbiri tetiği çalıştırmaz; kaçan gönderim hata vermez, müşteri yalnızca hiç haber almaz. Yerine: her koşuda *"kapsanmış hâle gelmiş ve haberi gitmemiş"* bekleyişleri arayan iş — **hangi yolla kapsandığı önemsiz**, sistem kendini onarır. İdempotentlik zaten şemada (`notified_at`), sorgu `is null` olduğu için ikinci tur aynı kişiyi hiç görmez. Servis tarafı hazır: `listPending` · `markNotified` (toplu — bir bölge açılınca onlarca kişiye birden gider) · `pendingCountByPostalCode`.
  - ~~**BEKLEYEN(19.21):** gönderimin kendisi~~ → **TAMAMLANDI (04.08, `14.10`):** `zone_available` olayı + üç dilli şablon + `zone_available` uzlaştırma işi (saatte bir). Kapsama kararını yine **motor** veriyor (`isInRoute`) — okuma kapısıyla aynı kaynak, üçüncü bir kopya yazılmadı. Damga gönderim tamamlandıktan SONRA atılıyor: tersi, sağlayıcı düştüğünde müşteriyi kalıcı sessizliğe mahkûm ederdi. `zone_notice` ayrıca `locale` kolonu kazandı — ziyaretçi kaydında profil yok, dil kaydedilmeseydi Alman müşteri Fransızca haber okurdu. Kanal bugün yalnız e-posta; WhatsApp `15.x` inmeden gidemez ve ekran "WhatsApp da gitti" dememeli (`packages/notify` yeteneğe kendi bakıyor, iş değişmeden ikinci kanal devreye girer).
  - **`ANALYTICS §3` gereği "sepeti bölünen / vazgeçen" kırılımı da bu tabloya iner**, olay defterine değil: `b2b + posta kodu + zaman` üçlüsü tek işletmeye indiği için defterde posta kodu yok. İki sayaç yan yana konursa huni ilişkisi ima edilir — ilişki kurulmuyorsa çizilmez
  - **Durum (04.08 — TABLO İNDİ, gönderim ayağı arka uçta bekliyor):** `/operations/warehouses` LİSTE görünümünde (tesis panelinde değil — sorduğu şey "hangi tesis nasıl duruyor" değil, "ağ olarak nereye açılmalıyız"; bir tesisin altına konsaydı her tesiste tekrarlanır ve hiçbirine ait olmazdı). Sütunlar: posta kodu (+ "rotada" rozeti + son soruluş) · talep · haber bekleyen · sipariş · sip./talep.
    - **Kapsam DIŞI ve kapsam İÇİ tek listede** (denetimin önerisi + kullanıcı şartlı onayı, 04.08): arka uç `readZeneDemand`'e `orderCount`/`revenueCents`/`orderRatio` ekledi. İkinci bir ekran aynı soruya iki cevap olurdu. Üç şart da tuttu — şema değişmedi, var olan arayüze dokunulmadı, yük küçük.
    - **`Sip./talep` bir DÖNÜŞÜM YÜZDESİ DEĞİL ve ekran bunu yazıyor:** payda aynı ziyaretçinin tekrar sormasını da sayıyor (tekilleştirmek kimlik tutmayı gerektirirdi), yani sayı gerçek dönüşümden küçüktür — kodlar arası sıralamada anlamlı, mutlak yüzde olarak okunamaz. Dönem süzgeci de YOK ve gerekçesi satırın altında: biri süzülüp öteki süzülmese oran pencere daraldıkça sessizce düşer ve düşüş bir sinyal sanılırdı.
    - **Kapsam dışı kodda sipariş sütunu SOLUK:** orada sıfır bir eksiklik değil, tanımın kendisi. Asıl okunacak satırlar "rotada" olup siparişi düşük olanlar — gidebildiğimiz hâlde sorulup alınmayan yer bir fiyat, çeşit ya da teslimat günü sorunudur.
    - **Mobilde YOK ve bu karar** (`design/pages/admin-depolar.md §7`): bölge kurulumu masa işidir, telefon künye sorar.
  - *Bitti:* kapsanmayan kodlar talep sıklığına göre sıralı listeleniyor; bir kod bölgeye eklenip kaydedilince bekleyenlere haber gidiyor ve ikinci kayıtta tekrar gitmiyor

- [~] (19.22) **Paket × depo: paketin yol kararı** *(denetim incelemesinin bulgusu + kullanıcı kararı 08.08 — iki okuma `docs/talep/denetim-paket-stok-ve-depo-okumasi.md`'de)*: paketin stok okuması depo tanımıyor (`storefront/packages.ts` ağ-geneli `getNetworkAvailabilityMap`) ve sepetin paket satırı `route`/`availableHere`'i `null` bıraktığı için checkout'un iki stok kapısı (`checkout-draft.ts:256` unfulfillable, `:264` overCap) paketi MUAF geçiyor. Tek depoda görünmez (ağ toplamı = deponun kendisi); çok depoda iş rezervasyonda, onaydan SONRA patlıyor — C3'e aykırı bir "tükendi" cümlesiyle (mal öbür depoda duruyor) ve sepette düzeltilecek şey görünmeden (kısır döngü). Ayrıca paket bugün HİÇBİR koşulda kargo grubuna binemiyor (route-null `splitByRoute`'ta ana grupta kalır) ve kalemleri depoda OLAN paketin adet tavanı hiç ölçülmüyor. **Kullanıcı kararı: ürünle aynı kural — C8 paket BÜTÜNÜNE uygulanır.** Tüm kalemler müşterinin deposundaysa `local`; değilse, paket kargolanabilir (`inRouteOnly` değil) VE tüm kalemler kargo deposunda tam takımsa `shipping` (bölge dışına paket satışı böylece açılır); ikisi de değilse erken ve dürüst durdurma ("bu adrese verilemiyor" ≠ "tükendi"). "Paketi ikiye böl" YOK — paket bütün olarak TEK depodan (K5); adet tavanı en zayıf kalemden (`min⌊mevcut/kalem-adedi⌋`). Katmanlar: karar `domain-core`'a (`decideCartAgainstWarehouse`'un paket ikizi; DB'siz, testli) · okumaya yeni metod GEREKMEZ (`getAvailableMap` + kargo deposu çağrısı ya da `listAvailableAcross` mevcut) · bağlama `lib/`de (`decideRoutes` paket satırını motora taşır, `packages.ts` yer belliyken depo süzgeçli `soldOut`) · **checkout kapıları DEĞİŞMEZ** — null yerine değer akınca `:256`/`:264` paketi kendiliğinden yakalar · ekran ucu musteri şeridinde (paket satırı varyantla aynı cümleler). `DOMAIN §13` paket-stok maddesi karara göre güncellendi (08.08) — touches: `packages/domain-core/src/delivery/`, `apps/web/lib/cart/read.ts`, `apps/web/lib/storefront/packages.ts`
  - *Bitti:* kalemleri iki depoya dağılmış paket son onaya ulaşamıyor (sepette/checkout'ta adıyla ve doğru cümleyle duruyor); kargo deposunda tam takımı olan kargolanabilir paket kargo siparişine binebiliyor; üç `BEKLEYEN(19.22)` işareti (read.ts ×2 + packages.ts) kapanmış
  - **Durum (08.08 · arka uç) — MOTOR + BAĞLAMA TAMAM; ekran ucu müşteri şeridinde.**
    - **Karar `domain-core`'da:** `decideBundleAgainstWarehouse` (`delivery/bundle-warehouse.ts`) — paketin BÜTÜNÜ için `route` + `maxBundles`. Sıra varyant motoruyla birebir aynı (yerel → soğuk zincir kapısı → kargo); farklı sıralasalardı aynı sepette ürün ile paket birbirine ters cevap verirdi. 10 birim testi.
    - **Tavan `min⌊mevcut ÷ kalem-adedi⌋`** — bölme şart, çıkarma değil: iki "Bayram Sofrası" iki kat malzeme demek (`expandToOrderItems` da `item.qty * line.qty` yazıyor). Kalem adedini yok saysaydık 2 baklava kalan stokta 3 paket satılırdı.
    - **`soldOut` AĞ GENELİ kaldı** (C3): "tükendi" ancak hiçbir depoda yoksa denir. Yere bağlı hâli yeni `route` alanı taşıyor — ikisini tek bayrakta toplamak, öbür depoda duran malı tükenmiş ilan etmek olurdu (denetimin bulduğu yanlış cümlenin ta kendisi).
    - **Yeni kapı YAZILMADI, mevcut kapı beslendi** (denetim önerisi): `bundleLine` artık `route`/`availableHere` taşıyor, checkout'un iki süzgeci (`:256` unfulfillable · `:264` overCap) paketi kendiliğinden yakalıyor.
    - **Yer PARAMETRE** (`PackagePlace = Partial<PlaceWarehouses>`), çerez okuması değil — 34 testin dersi (`settings-scope.ts` künyesi). Verilmezse davranış bugünküyle aynı: ağ-geneli okuma, yol bilinmiyor.
    - **Ölçüldü (tazelenmiş DB, gerçek paketler):** STR müşterisi → dördü de `local`. KEHL müşterisi → `bayram-sofrasi` kalemleri `14/1 0/1 0/1` dağılmış + soğuk zincir kalemi var → `not_shippable_here`; `maras-dondurma-seti` soğuk zincir → `not_shippable_here` (kargoya VERİLMİYOR); `fistik-sevenler` ve `baklava-ikilisi` yerelde yok, STR'de tam takım → **`shipping`** — paket ilk kez kargo grubuna binebiliyor.
    - **Kalan (müşteri şeridi):** paket kartı/satırı `route`'u okuyup varyantla aynı cümleleri göstermeli (`StockMark` deseni); `/packages` ve `/package/[slug]` sayfaları yeri kapıya geçirmeli (parametre isteğe bağlı, bugünkü davranış varsayılan). Talep açıldı.

- [x] (19.23) **Rota DIŞI müşteri "Strasbourg'daymış" gibi okunuyordu — `warehouseId` tek kutuda iki anlam taşıyordu** *(kullanıcı bildirimi 09.08 → müşteri şeridi paket tarafında gördü → arka uç ölçtü ve genişletti)* · `touches: apps/web/lib/delivery/read-place.ts, apps/web/lib/delivery/place-types.ts, packages/application/src/catalog/{product-context,storefront-types}.ts, scripts/seed/coverage.ts`
  - *Bitti:* rota dışı müşteri `shipping`/`elsewhere` işaretleri alıyor; teklifleri kaybetmiyor; kapsam denetiminde "rota dışı" kovası var
  - **Ölçülen (aynı 12 ürün, `stockStatusOf`):**
    ```
    67000 rota içi                 available 12
    KEHL  rota içi, yerelde yok    shipping 5 · available 5 · elsewhere 2   ← ÇALIŞIYOR
    75011 ROTA DIŞI                available 12                            ← 67000 ile BİREBİR AYNI
    ```
  - **Kök sebep:** `resolvePlaceByPostalCode` `shipping` hâlinde `warehouseId` alanına **kargo deposunun** kimliğini koyuyor (`warehouse-resolve.ts:96`), `read-place` onu olduğu gibi yayıyordu. Tek kutu iki anlam: rota hâlinde "aracın çıktığı depo", kargo hâlinde "kargonun çıktığı depo". Okuyan taraf ayırt edemiyor, hepsini birincisi sanıyordu.
  - **Dört hâl motoru SAĞLAMDI** — bozuk olan girdiydi. Bu ayrım önemli: `stockStatusOf`, `StockMark`, `decideCartAgainstWarehouse`, iki-checkout akışı hepsi doğru yazılmıştı ve KEHL senaryosunda çalışıyordu.
  - **Neden bugüne dek görülmedi:** üçüncü senaryo hep **KEHL müşterisiyle** denendi — rota İÇİNDE olup kendi deposunda mal bulunmayan müşteri. Orada iki depo farklı, ayrım çalışıyor. **Rota DIŞI müşteri hiç denenmedi**; orada iki depo aynı kimliğe düşüyor (STR hem rota hem kargo deposu) ve karşılaştırılacak iki şey aynı oluyor. Yapısaldır, seed artefaktı değil: kargo deposu ayrı bir depo olsaydı da `warehouseId` ile `shippingWarehouseId` yine eşit olurdu.
  - **Düzeltme — yeni alan YOK, yeniden adlandırma YOK.** `warehouseId` artık YALNIZ rota deposu; üç hâl iki alandan türüyor: `(null,null)` bilinmiyor · `(rota,kargo)` rota içi · `(null,kargo)` **rota dışı**. `mode` alanı bilerek eklenmedi — türetilebilen bir şeyin ikinci kaynağı bir gün ötekiyle çelişir.
  - **Yerel havuz rota dışında BOŞ HARİTA, ağ-geneli DEĞİL.** Müşteri şeridinin önerdiği "kutuyu boşalt" düzeltmesi tek başına yetmiyordu ve sebebi buydu: `warehouseId` null olunca okuma ağ-geneline düşüyor, toplam sıfırdan büyük çıkıyor ve motor yine `local` diyordu — hata yer değiştirir, kaybolmazdı (ölçüldü).
  - **Teklif havuzu = malın GELDİĞİ depo** (`warehouseId ?? shippingWarehouseId`): alan daraltılınca rota dışı müşteri indirimlerini sessizce kaybedecekti. Düzeltmenin yan hasarı önlendi — ölçüldü, 12/12 ürün teklifini koruyor.
  - **Sonuç (ölçüldü):** `75011 → shipping 9 · elsewhere 3` (kargolanamayan soğuk zincir kalemleri doğru biçimde `elsewhere`). 67000 · KEHL · yer-bilinmiyor hâlleri DEĞİŞMEDİ.
  - **Kapsam denetimine "Yer çözümü" alanı eklendi** (rota içi kod · rota DIŞI ama hizmette · kargo çıkış deposu) — senaryonun ön koşulu artık makineyle sorgulanıyor. 93 kova.
  - **Sipariş tarafı zaten sağlamdı:** `checkout-draft` yeri ADRESTEN çözüyor, `deliveryType` doğru çıkıyor. Bozuk olan gösterim ve sepet akışıydı.

- [x] (19.24) **Teslimat talebi: yeni tablo AÇILMADI — `zone_notice` zaten oydu, üç alan eksikti** *(mobil şeridin talebi 09.08 · `docs/talep/talep-web-teslimat-talebi-kaydi.md`)* · `touches: supabase/migrations/0023_notices.sql, packages/types/src/entities/zone-notice.schema.ts, packages/database/src/services/zone-notice.service.ts, apps/web/lib/delivery/notice-actions.ts, apps/backend/src/jobs/zone-available.ts, scripts/seed/delivery.ts, scripts/seed/coverage.ts`
  - *Bitti:* bölge dışı müşterinin "buraya gelin" cevabı ülkesi, yer adı ve geldiği yüzeyle birlikte kaydediliyor; haber işi artık ülke tahmin etmiyor; kapsam denetimi dokuz kova ile bekliyor
  - **Talep ayrı bir `delivery_interest` tablosu istiyordu; açılmadı ve gerekçesi ölçüm.** Mobil şerit aradı ve bulamadı (`%interest%`, `%waitlist%`, `%lead%`, `%consent%`) — aranmayan sözcük `notice` idi. Kayıt zaten vardı: posta kodu + iletişim kanalı + müşteri bağı + gönderim damgası, **üstelik okuyan işiyle birlikte** (`zone-available.ts` — talebin kendisi o dosyayı bağ kurulacak yer diye anıyor, oysa okuduğu tablo tam da istenen tablo). İki tablo `CLAUDE §1`'e aykırı olurdu ve somut bedeli var: haber işi ikisini birden okumak zorunda kalır, o gün biri unutulur ve o listedeki müşteri hiç haber almaz.
  - **`postal_code_demand` ↔ `zone_notice` ayrımı talebin istediği ayrımın TA KENDİSİ:** ilki anonim sayaçtır (*"bu koddan N kez soruldu"* — zayıf sinyal), ikincisi kimliği ve kanalı olan kayıttır (kuvvetli). İkisi 0023'te zaten yan yana ve *"birleştirilemez"* diye yazılı.
  - **`country` bir kolaylık değil DÜZELTME — ölçüldü (09.08).** `postal_code_place`'teki **16 878** satırın **610 kodu iki ülkeye birden** çözülüyor. Kayıt yalnız kodu taşırken haber işi iki ülkeyi de deneyip *"biri tutarsa kapsanmış say"* diyordu (`zone-available.ts`, eski `COUNTRIES` sabiti): Fransa'da açılan bir bölge, aynı kodu yazmış Alman müşteriye *"bölgeniz açıldı"* diye gidebilirdi — hem yalan hem geri alınamaz (damga yazılır, satır bir daha hiçbir turda görünmez). Küçük kardeşi bu dersi 19.8'de almıştı (`variant_stock_notice.country`); burası ondan eski olduğu için geride kalmıştı. Tekillik indeksi de ülkeyi aldı.
  - **Ülke ÇEREZDEN okunuyor, parametreden değil** (`recordZoneNoticeAction`): kaydın hangi yere ait olduğu bir tercih değil, sistemin zaten bildiği bir gerçek — kardeş eylem `recordVariantStockNoticeAction` de böyle. Bu sayede müşteri şeridinin bileşeni (`place-restriction.tsx`) hiç değişmedi.
  - **`place_name` kayıt ANINDA donuyor:** kod tablosu ileride değişse de operatör *"68000"* değil *"Colmar"* okur. `source` (`web` · `app-account` · `app-onboarding`) enum DEĞİL — bir karar girdisi değil denetim izidir; yeni ekran açmanın migration yazdırmasının karşılığı yok.
  - **Telefon/WhatsApp kolonu EKLENMEDİ ve bu bilinçli.** Talep *"en az bir iletişim kanalı"* istiyordu; kanal İZNİ zaten profilde (`updateCustomerPreferences`, kanal bazlı damga) ve girişli müşteride telefon profilde duruyor — kaydın içine ikinci bir kopya koymak `CLAUDE §1`'e aykırı olurdu. Ziyaretçide e-posta soruluyor (bugünkü web diyaloğunun aynısı). WhatsApp gönderimi zaten `15.6`'yı bekliyor ve bildirim altyapısı kanal yeteneğine kendi bakıyor (`supports`).
  - **`pendingCountByPostalCode` imzası DEĞİŞMEDİ** — operasyonun Depolar ekranı kodu gösteriyor, ülke ayrımı satırın kendisinde duruyor. Sayı bir karar girdisi değil yoğunluk göstergesi; imzayı değiştirmek başka şeridin dosyasını bu turda kırardı.
  - **Kapsam denetimi: "Bölge talebi" alanı — 9 kova**, ikisi yeni sınıf: `ALMAN kayıt (ülke ayrımı)` (hepsi FR olsaydı düzeltmenin çalıştığı hiçbir koşuda görülmezdi) ve `native uygulamadan gelen` (hepsi `web` olsaydı yüzey izi hiç sınanmazdı). Ayrıca dil kolonunun dolu ve BOŞ hâli birlikte — 14.10 kolonu seed'de altı satırın altısında da `null`du, yani profilsiz dil çözümü dalı hiç koşmamıştı.
  - **Mobil tarafa kalan:** `sendZoneInterest` içindeki `BEKLEYEN(21.15)` — kapı hazır, tek çağrı yeter (`source: 'app-account'` / `'app-onboarding'`).

- [ ] (19.25) **Besleme verisi sepetin İKİ GRUBA bölündüğü hâli hiç üretemiyor** *(mobil şeridin ölçümü 10.08; 15.08'de yeniden ölçüldü, hâlâ geçerli)* · `touches: scripts/seed/warehouse.ts, scripts/seed/delivery.ts, scripts/seed/stock.ts, scripts/seed/coverage.ts`
  - **Kod arızası DEĞİL — kural doğru yazılmış, motor doğru çalışıyor; üretilemeyen şey SENARYONUN kendisi.** Bugünkü beslemede üç aktif bölgenin üçü de STR deposuna bağlı ve STR aynı zamanda FR kargo çıkışı (`ships_online = t`). Yani rota içi bir FR adresi için `warehouseId` ile `shippingWarehouseId` **aynı depoyu** işaret ediyor; `decideCartAgainstWarehouse` `localAvailable` ile `shippingAvailable`'ı aynı havuzdan okuyunca bir varyant ya ikisinde birden var (`local`) ya ikisinde de yok (`unavailable`) — **`shipping` yolu rota içi bir adres için doğamıyor.** İkinci ölçüm de aynı yere çıkıyor: aktif ve `shippable` varyantların hiçbirinin STR stoku sıfır değil.
  - **Sınanmamış kalan davranışlar:** iki grup başlığı · *"Kargolu ürünleri ayrıca sipariş ver"* ikinci siparişi · kargo eşiğinin KENDİ matrahından hesaplanması (`shippingSubtotalCents`) · `shippingOnly` bayrağı · kargo KDV'sinin oransal bölünmesi. Hiçbiri gerçek veriyle koşmadı.
  - **En ucuz çözüm (mobil şeridin önerisi, karar kullanıcının):** COLMAR'ı aktif edip ona bir teslimat bölgesi bağlamak (`ships_online = f` KALIR) ve bazı kargolanabilir varyantların COLMAR stokunu SIFIR, STR stokunu POZİTİF bırakmak. O zaman Colmar bölgesindeki bir adres için rota deposu COLMAR, kargo çıkışı STR olur → aynı sepette hem `local` hem `shipping` kalem doğar. Aynı besleme ikinci bir hâli de kapatır: iki depoda da olmayan varyant (`unavailable`) — bugün onun da örneği yok.
  - **Kurgu künyeye YAZILMALI**, yoksa ileride biri *"stoklar neden böyle dağıtılmış"* deyip sadeleştirir ve senaryo yeniden ölür. Kapsam denetimine (`coverage.ts`) de bir kova girmeli.
  - **`db:refresh` gerektirir — kullanıcının kararı** (`CLAUDE.md §0`).

- [x] (19.26) **Depolar TEK ekran oldu — liste ↔ detay ayrımı kalktı** *(kullanıcı kararı 16.08: "depolar sayfasıyla depo detay sayfasını birleştir; başlığın hemen altına depo isimlerini koyalım, seçtiği deponun detayı aşağıdaki bölümde görünsün; header'daki tüm depolar butonu da gereksiz, kaldıralım")* · `touches: apps/web/app/(operations)/operations/warehouses/{page,warehouses-client,warehouses.desktop,warehouses-sections,loading}.tsx, apps/web/components/operation/ui/page-header.tsx, apps/web/components/operation/ui/ops-nav.ts`
  - *Bitti:* tek görünüm — başlık · tesis şeridi · seçili tesisin detayı; kabuğun depo seçicisi bu sayfada gizli; sıcaklık sol menüden kalktı
  - **Sorun ölçülebilirdi:** ekran iki hâl arasında gidip geliyordu (seçim yokken liste, seçim varken sol ray + kart) ve ikinci tesise bakmak için önce "tüm depolar"a dönmek gerekiyordu. Aynı nesnenin iki ayrı sayfası varmış gibi duruyordu; oysa `page.tsx` künyesi baştan beri *"tesisin kim olduğu, nereye hizmet ettiği ve nasıl durduğu — üçü aynı nesneye ait olduğu için ayrılmaz"* diyordu. Ayrılmayan şey iki görünüme bölünmüştü.
  - **Sıra KAYBOLMADI, şeride geçti** (`FacilityStrip`, sürükle-bırak): operatörün dizdiği sıra sistemdeki **bütün** depo seçicilerinde geçerli (bağlam seçicisi · tablo süzgeci · transfer hedefi). Liste görünümüyle birlikte silinseydi sıralamanın yapılabileceği tek yer yok olurdu. Sürüklemek ile seçmek çakışmıyor — 5 px hareket eşiği (galeri karesinin ölçülmüş deseni).
  - **Kabuğun depo seçicisi bu sayfada GİZLİ** (`PageHeader.hideWarehousePicker`). Kabuk kuralının ilk istisnası ve dar tutuldu: bayrak *"bu sayfa depo bağlamını kullanmıyor"* der. Depolar zaten bağlamı daraltıcı olarak okumuyor (*"depolar bir yönetim nesnesidir, hepsi her zaman listelenir"*); sayfanın kendi şeridi gelince barda iki seçici yan yana düşüyordu — biri ekranı değiştiriyor, öteki hiçbir şey yapmıyor ve farkı ekrandan okumanın yolu yok.
  - **Seçim boşsa İLK AKTİF tesis açılır**, URL'e yazılmadan: tek görünümün "hiçbiri seçili değil" hâli yok. Kapalı tesis tercih edilmiyor — karnesi zaten okunmuyor, ekranı boş bir kartla karşılamak olurdu.
  - **`WarehouseListRow` + `RowStats` SİLİNDİ** — liste görünümüyle birlikte çizecek yerleri kalmadı; taşıdıkları kayıp değil: künye başlık barına, sayılar Karne bölümüne, kurulum uyarısı detayın üstündeki `SetupGapNote`a düştü.
  - ~~**Ağ geneli iki blok ayrı bölüme alındı** (kargo çıkışı eksik ülkeler · posta kodu talebi)~~ → **BÖLÜM TAMAMEN KALKTI (17.08, kullanıcının ekran turu; `19.27` bununla kapandı).** Ayrı bölüme almak, bir sayfada "bu bölüm bu sayfanın sorusuna cevap vermiyor" diye başlık atmaktı — kayıt doğruydu ama çözüm değildi. Gerekçe: talep tablosunun evi 04.08'de "**kararın verildiği yer burası**" diye seçilmişti; o gerekçe 07.08'de sona erdi (bölge kurulumu haritayla birlikte Teslimat & Rota'ya taşındı, karar da onunla gitti). Kargo boşluğu uyarısı ise tek ülkeli kurulumda hiç tetiklenmiyor ve tetiklendiği gün bir KURULUM kararıdır — künye penceresinin işi.
  - **Sıcaklık sol menüden kalktı** (`ops-nav.ts`) — `22.29`'un ilk yarısı cevaplandı: kendi sayfası değil. **Rota SİLİNMEDİ**, `/operations/temperature` çalışıyor (ölçüldü: 200); ölçüm noktalarının nereye tanımlanacağı henüz konuşulmadı ve ekranı silmek kararı vermeden çalışan bir yolu yıkmak olurdu.
  - Doğrulama: `typecheck` temiz · `lint` temiz · `knip` bu ağaçta bulgu vermiyor · ekranda ölçüldü (depolar 200, seçici barda yok, menüde sıcaklık yok, ilk tesis açık).
  - **Durum (17.08 — SAYFA BUDANDI, kullanıcının ekran turu).** Ölçüt kullanıcının kendi cümlesiydi: *"mevcut depo sayfasında olmasının anlamı olmayan kısımlar."* Sayfanın sorusu — *"bu tesis kim, açık mı, nereye hizmet ediyor, ne durumda"* — her bölüme tek tek soruldu; cevap veremeyen çıktı. Sayfa tam boy **~1400 px'den ~660 px'e** indi (ölçüldü, 1440 genişlikte).
    - **Künye bölümü (4 kart) KALKTI.** Üçü başlığın kelimesi kelimesine tekrarıydı: `Kod` alt satırda, `Ülke` adresin sonunda, `Kargo çıkışı` başlığın rozetinde yazılı. Dördüncüsü (`Bağlı personel`) bir SAYIYDI ve hemen altındaki bölüm aynı kişileri ADLARIYLA listeliyor. Geriye yalnız belge önekleri (`IMH-…` · `TRF-…`) kalıyordu — belgenin üstünde zaten yazılı, burada bir karara girdi değil. Bir bölümün tamamı dört kutu yer kaplayıp tek yeni cümle söylemiyordu.
    - **Karne beş kutudan dörde indi.** `Yolda bekleyen` depolar arası transferi sayıyordu ve tek depolu kurulumda tanımı gereği daima `0 / 0` — sabit sıfır gösteren bir ölçü, ölçü değildir. Veri **silinmedi**: `inTransitIn` okunmaya devam ediyor, çünkü asıl tüketicisi ekran değil **kapatma uyarısı** (yoldaki mal hiçbir deponun stoğunda değil, hedefi kapanırsa kabul edilecek yer bulamaz). `inTransitOut` gitti — tek tüketicisi kutuydu.
    - **`Eşik altı` kutusundaki "eşik depo bazlıdır" notu düştü:** karşılaştırılacak ikinci depo yokken bir ayrım değil, kuralın tekrarı. Kural veride ve `DOMAIN §17`de yaşıyor.
    - **Üç öğretici paragraf kalktı** (üç katmanlı yapı · eşik altının iki yolu · kapalı depo neden silinmez). Ders metniydiler: ekran zaten çakışmayı kısıt cümlesiyle reddediyor, sayı zaten Stok'a gidiyor. `Son mal girişi` kaldı — o bir VERİ.
    - **Ölçülen bir hata düzeltildi:** karne `RİSK 0 · tutar bilinmiyor` yazıyordu. `totalRiskCents` boş kümede `null` dönüyordu, ekran da onu "ölçülemedi" diye okuyordu — oysa riskli parti YOKSA tutar bilinmez değil, **gerçekten sıfır**. Kuralın (`CLAUDE §1`) ters yönü: ölçülemeyen sıfır değildir, ama bilinen sıfır da "bilinmiyor" diye gösterilemez. Fonksiyon iki hâli ayırdı; "fiyatı girilmemiş partiler var" dalı aynen duruyor.
    - **Sayfa bir sorgu eksildi:** talep okuması (`readZoneDemand`) ilk dalgadaydı; çizilmeyen bir veriyi okumanın bedeli ödenmiyor artık.

- [x] (19.27) **Posta kodu talebi tablosunun evi** — Depolar mı, Teslimat & Rota mı *(kullanıcı sorusu 16.08: "posta kodu talebi konusu neden depolar sayfasında bulunsun, teslimat ve rota sayfasında bulunması gerekmiyor mu")*
  - *Bitti:* tablo tek bir yüzeyde yaşıyor — **hiçbirinde tablo olarak değil, HARİTADA.**
  - **Karar (17.08, kullanıcı): Depolar'dan kalktı, Teslimat & Rota'ya da TAŞINMADI.** Taşımak ikinci bir defter kurmak olurdu; oysa aynı veri (`postal_code_demand`) Rotalar haritasında zaten okunuyor ve orada bir üstünlüğü var — kodun NEREDE olduğunu gösteriyor. 17.08'de ipucu zenginleşince (bekleyen · sipariş · sorulma · uzaklık · yaş) tablonun taşıdığı kanıtların tamamı haritaya geçti; ekran dışında kalanlar için de ayrı bir ray var. Bir defterin iki yeri olmaz, ama bir defterin daha iyi bir hâli olabilir.
  - **Kaybolan iki okuma, bilerek:** `sip./talep` oranı ve **zaten rotada olan** kodların satırları (harita yalnız rotaya girmemiş kodları çiziyor). İkisi de bir kurulum ekranının değil **Analitik**'in malzemesi; ihtiyaç doğarsa orada, dönem süzgeciyle birlikte kurulur.
  - **Ölü kod silindi:** ~~`apps/web/lib/delivery/zone-demand.ts`~~ + testi. Tek tüketicisi bu tabloydu; kaldırılınca geriye kendi testinin ayakta tuttuğu bir modül kalmıştı (`knip` `ZoneDemandRow`'u işaretledi). Git'te duruyor — Analitik günü gelirse oradan çıkar.
  - **Ölçüldü (16.08):** aynı veri (`postal_code_demand`) iki yerde okunuyor — Depolar'da 50 satırlık tablo (`readZoneDemand`), Rotalar'da altı satırlık mor öneri rayı (`routes-read.ts`, `buildSuggestions`). İkisi TEKRAR değil, iki farklı çözünürlük: ray bir DAVET (hiçbir rotada olmayan, sinyali en güçlü kodlar), tablo bir DEFTER (hepsi, sayılarıyla).

- [x] (19.28) **Ölçüm noktaları — depo içi stoklama alanları + araçlar** *(kullanıcı isteği 17.08: "depo konusunda özellikle yapılması gereken araç tanımlama ve depo için stoklama alanları tanımlama")* — touches: `supabase/migrations/0045_storage_area_vehicle.sql`, `packages/types/src/entities/storage-point.schema.ts`, `packages/database/src/services/storage-point.service.ts`, `apps/web/app/(operations)/operations/warehouses/`, `apps/web/app/(operations)/operations/temperature/`
  - *Bitti:* nokta serbest metinden TANIMLI kayda geçti; "bugün ölçülmedi" ilk kez sorulabiliyor.
  - **Kök: `temperature_log.location` serbest metindi** ve içine hem dolap adı hem araç plakası yazılıyordu. Üç zarar ölçüldü: (1) ekran noktaları birebir metinle grupluyor — `Dolap 1` ≠ `Dolap-1` ≠ `dolap 1`, geçmiş bölünüyor; (2) "bu nokta genelde şu kadar okuyor" uyarısı o bölünen geçmişe dayanıyor; (3) **ölçülmeyen tespit edilemiyor** — var olduğu bilinmeyen bir dolabın eksik ölçümü de bilinemez. Tasarımın *"ölçülmemiş nokta amber görünür kalır"* vaadi bu yüzden hiç çalışmamıştı.
  - **`vehicle` tablosu GERİ GELDİ** — 0031'de tanımlanmış, 03.08'de tüketeni olmadığı için düşürülmüştü. O günün künyesi *"gerçekten gerekince geri gelir ve o gün doğru soruyu sorarız: araç bir depoya mı, bir güne mi, bir kuryeye mi bağlanır"* diyordu. **Cevap: araç bir ÖLÇÜM NOKTASIDIR** — soğuk zincirin yoldaki yeri. Kayıt silinmedi, üstüne yazıldı: bir tabloyu ne zaman kurmamak gerektiğinin en iyi örneği.
  - **İki tablo, tek tablo değil** (kullanıcı kararı): ortak bir `storage_point(type)` sıcaklık kaydının bağını tekilleştirirdi ama zorunlulukları yalanlardı — dolabın deposu zorunlu, aracınki değil; plaka araçta benzersiz, alanda karşılığı yok. Tek tabloda hepsi nullable olur, kural veriden ekrana kaçardı. Bedeli sıcaklık kaydında iki kolon + `num_nonnulls(...) = 1` kısıtı; polimorfik anahtar yerine bu seçildi çünkü polimorfik anahtar FK yazdırmaz ve silinen bir dolabın kayıtları sessizce sahipsiz kalırdı.
  - **`warehouse_id` araçta K8'den bilinçli sapma:** K8 bağı bir KISIT olacağı için reddetmişti ("araç bir depoya aittir"). Alan nullable ve hiçbir yerde zorlanmıyor — aidiyet değil adres. Kurye günü/kapanışı kurye-gün ekseninde aynen duruyor.
  - **Sapmanın ölçütü ikiye çıktı:** alanın **beklenen aralığı** (kesin) → noktanın **alışkanlığı** (tahmini). Sıra kritik: bozuk bir dolap her gün −8 okuyorsa alışkanlığı −8'dir ve eski tek ölçüt arızayı normal ilan ederdi. Aralığı olmayan noktada (raf, geçiş alanı) alışkanlık tek ölçüt olarak sürüyor ve örneklem azken susuyor.
  - **Sıcaklık ekranı:** serbest metin kutusu kalktı, çipler tanımlı noktalardan geliyor, ölçülmemiş nokta çipte işaretli. **Gün şeridi eklendi** — nokta · beklenen aralık · bugünkü ölçüm ya da "ölçülmedi", üstünde `N nokta bugün ölçülmedi` sayacı. Bu şerit bugüne kadar hiç çizilemiyordu (küme geçmişten türüyordu).
  - **Depolar ekranı:** "Ölçüm noktaları" bölümü (alanlar + araçlar, ekle/düzenle/pasife al). Hiç ölçülmemiş nokta sayılıyor ve uyarı yazılıyor — tanımlı ama tura girmemiş nokta bir kurulum eksikliğidir. **Silme yok**, susturma var.
  - **Bölge kartına ağırlık geldi** (aynı turda, kullanıcı isteği): sipariş · ciro · bekleyen · sıradaki çıkış günü. Yeni tablo yok — Rotalar'ın okuduğu RPC'nin aynısı (`analytics_postal_code_orders` + `zone_notice`), iki ekran aynı soruya iki sayı vermesin diye.
  - **Teardown sözleşmesi değişti:** `purgeTestData` artık `temperatureLocations` yerine `storageAreaIds`/`vehicleIds` alıyor. Alan depoyu `restrict` ile tutuyor — bildirilmeyen bir alan teardown'ı depoda takar ve artık depo operatörün seçicisinde görünür (14.08'de ölçülen arızanın aynısı).
  - ~~**BEKLEYEN(19.28):** `stock.location` da serbest metin…~~ → **KAPANDI aynı gün: `19.29`.**
  - **Reset yapıldı ve ekranda ölçüldü (17.08):** Depolar'da "Ölçüm noktaları" bölümü 5 alan + 1 araç çiziyor, hiç ölçülmemiş nokta uyarısı doğdu; sıcaklık ekranında gün şeridi çalışıyor ve `Derin dondurucu 1` bugün `−14,2°` okuduğu için **beklenen aralık** ölçütüyle kırmızı işaretlendi (alışkanlık ölçütü tek başına bunu garanti etmezdi).
  - **Reset sonrası çıkan tek hata derlemedeydi, veride değil:** `warehouses-read.ts`e servis importu konunca supabase-js İSTEMCİ paketine sızdı (o dosyayı kapatma penceresi de okuyor) ve iki sayfa `node:crypto` ile 500 döndü. `readLastMeasured` tek tüketicisi olan `page.tsx`e taşındı. **`typecheck`/`lint`/`boundaries` üçü de göremedi** — sınır burada dosya adından değil "kim import ediyor"dan doğuyor.

- [x] (19.29) **Partinin rafı da tanımlı alana bağlandı — `stock.location` → `stock.storage_area_id`** *(kullanıcı kararı 17.08: "bu serbest metin olma konusu bizim için problem gibi görünüyor… açık bıraktığın konuyu tamamlasak iyi olacak"; barkod etüdü `docs/feature/barkod-okuyucu.md §1.13` tüketici olarak talipti)* — touches: `supabase/migrations/{0006_stock,0010_supply,0045_storage_area_vehicle}.sql`, `packages/types/src/entities/{stock,supply}.schema.ts`, `packages/types/src/contracts/warehouse-api.schema.ts`, `packages/application/src/warehouse/`, `apps/web/`, `scripts/seed/`
  - *Bitti:* aynı fiziksel kavramın iki temsili kalmadı — alan tek yerde tanımlı, hem ölçüm hem parti ona bağlı.
  - **Zamanlama kullanıcının kararı ve gerekçesi ölçüldü:** değişiklik `db:reset` istiyordu ve veritabanı `19.28` için zaten az önce sıfırlanmıştı — ikinci resetin maliyeti o an sıfırdı, sonraya bırakmak elle girilen veriyi silmek olurdu.
  - **Sözleşmede ASİMETRİ var ve kasıtlı:** okuma tarafı alanın **ADINI** taşıyor (`PreparationSuggestion.areaName`, `StockBatchDetail.storageArea`), yazma tarafı **KİMLİĞİ** (`IntakeFormLine.storageAreaId`). Depocu rafta bir uuid aramıyor tabelayı okuyor; ama yazarken listeden seçiyor — ad kabul etseydik yazım hatası yeni bir "alan" uydurur ve serbest metne geri dönerdik.
  - **Gömme TEK YERDE tanımlı** (`StockAreaEmbedSchema` + `AREA_EMBED`): üç okuma aynı üç alanı istiyor (parti detayı · tarihli parti · varyant geçmişi); üç kez yazılsaydı biri bir gün `kind`ı unutur ve o ekran uyumsuzluk uyarısını kuramazdı.
  - **`0045`'in gerekçesi TAMAMLANDI:** `storage_area.kind` ile `product.storage_type` aynı kelimeleri konuşuyordu ama soru sorulamıyordu. Artık mal kabul **uyumsuzluk uyarısı** üretiyor (`storageMismatches`) — MLOR'un ikizi: **uyarır, engellemez** (`DOMAIN §4`). Dondurucu bozulunca malı geçici başka alana koymak meşru bir karardır; reddetmek depocuyu ya kaydı hiç yazmamaya ya da yanlış alan seçmeye iterdi. `staging` hiç uyarmaz — geçiş alanı bir saklama rejimi değil.
  - **Mal kabul formunda metin kutusu → seçim.** Boş seçenek meşru kaldı: rafı bilinmeden de kabul edilir, zorunlu kılmak depocuyu rastgele alan seçmeye iterdi.
  - **Seed'in sırası değişti:** ölçüm noktaları `seedTemperatureLogs`ten çıkıp `seedStoragePoints`e (depo seed'i) taşındı ve `seedWarehouses`'ın hemen ardına alındı — partiler de alana `restrict` ile bağlı, yani alanlar stoktan ÖNCE doğmak zorunda. Dağılım rejime uygun (donuk mal dondurucuda, süresi geçmiş mal Karantina'da); **bir parti bilerek rafsız** bırakıldı, `null` yolu da çizilsin diye.
  - **`Karantina` ayrı tür OLMADI** (kullanıcı kararı): `staging` zaten "saklama rejimi değil bekleme yeri" demek ve karantina da bir bekleme yeri — adı alanın adında duruyor, türü `staging`.
  - **Mobil şeridin iki fikstürü düzeltildi** (`warehouse-fixture.ts`, `preparation-screen.test.tsx`): sözleşme alanı `location` → `areaName` oldu. Mobil EKRANLAR etkilenmedi — hiçbiri parti konumunu çizmiyor (ölçüldü). Not bırakıldı: `docs/talep/not-mobil-parti-rafi-sozlesmesi.md`.
  - ~~**BEKLEYEN(19.29):** `db:reset` bekliyor~~ → reset yapıldı ve ekranda ölçüldü (17.08): 199 partinin rafı bağlı, 3'ü bilerek rafsız.

- [x] (19.30) **Hijyen takvimi — noktanın son 3 ayı, ve sıcaklık sayfasının kapanışı** *(kullanıcı tarifi 17.08: "bir iki üç aylık periyodu gösteren bir date time komponenti görünse… istenen aralıkta değilse farklı renkte görünse… üzerine gelerek tool tip olarak girilen tarihleri görsek veya yenisini ekleyebilsek")* — touches: `supabase/migrations/0045_storage_area_vehicle.sql`, `packages/types/src/entities/storage-point.schema.ts`, `packages/database/src/services/temperature-log.service.ts`, `apps/web/app/(operations)/operations/warehouses/`, `apps/web/components/operation/ui/ops-nav.ts`, `scripts/seed/`
  - *Bitti:* "şu dolabın son üç ayını göster" sorusu ilk kez cevaplanıyor; `22.29` tamamen kapandı.
  - **Kullanıcı iki renk tarif etti, ölçüm ÜÇÜNCÜ bir hâl gösterdi ve o en ağırı: hiç ölçülmemiş gün.** Sapma karar verilmiş bir olaydır (birisi baktı, gördü, yazdı); boşluk cevapsız bir sorudur ve denetimde cevapsız soru, kötü cevaptan pahalıdır. Dördüncüsü bir hâl değil bir SESSİZLİK (`idle`): beklenmeyen ölçümün yokluğu eksiklik değil.
  - **"Eksik" diyebilmek yeni bir alan istedi:** `expected_daily_checks` (alanda varsayılan 1, araçta 0, tavan 12). Bu alan olmadan yalnız "sıfır kayıt" görülüyordu — *"sabah alındı, akşam alınmadı"* görülemiyordu; yarım kalmış bir tur, hiç yapılmamış bir tur kadar sessizdi. **0 geçerli ve anlamlı bir değer**, kaçış değil: oda sıcaklığı rafından günlük ölçüm beklenmez ve o günleri boyamak denetimi gerçek eksiklere kör eden bir gürültü üretirdi.
  - **Eksik gün KIRMIZI DEĞİL, boş.** Ölçüldü: `warehouse` tablosunda çalışma günü/saati alanı yok, yani pazar günlerini kırmızıya boyamak her hafta yalancı bir alarm üretirdi. Yokluğun kendisi zaten görünür (boş kutu + üstteki sayaç). Renk suçlamaz, boşluk gösterir — çalışma günü tanımı gelirse ölçüt sıkılaştırılabilir.
  - **Geçmişe kayıt YAZILMIYOR** (kullanıcı kararı, üç şık sunuldu): hijyen defterine sonradan kayıt düşmek defteri denetimde değersiz kılar — **boş bir gün dürüsttür, sonradan doldurulmuş bir gün değildir.** Kapı da buna göre: `recordTemperatureAction` `recordedAt` almıyor, `now()` yazıyor. Takvimde yalnız bugüne yazılıyor ve ekran bunu SÖYLÜYOR ("geçmiş günlere yazılmaz") — sessizce engellenen bir kural, arayüzü bozuk gösterir.
  - **`22.29` KAPANDI ve `/operations/temperature` SİLİNDİ.** 16.08'de menüden kaldırılmıştı ama rota çalışıyordu; ölçünce asıl durum çıktı: sayfa **komut paletinde de yoktu**, yani ona yalnız adresi elle yazan girebiliyordu — çalışan ama ulaşılamayan bir ekran. İki yarısı yüzeye göre ayrıldı: **okuma/denetim** Depolar takvimine (yönetici sorusu, sayfa zaten `ADMIN_ONLY`), **sahadaki yazma** native uygulamaya. Kullanıcının cümlesi: *"web'de yazma olsun burada admin girsin, depocu zaten mobil uygulama üzerinden giriş yapacak."*
  - **BEKLEYEN(19.30) — depocunun ölçüm yazacak yolu bugün KAPALI.** Native uçta hiçbir şey yok (ölçüldü: `apps/mobile-api` ve `apps/mobile` içinde `temperature` geçen tek dosya yok) ve **kullanıcı zamanını erteledi** (17.08: *"mobil tarafına dokunma, zamanı gelince yapılacak"*) — açılan talep bu kararla geri alındı, kayıt burada duruyor. O gün gereken üç şey: (1) uç `POST /api/v1/warehouse/temperature` — depo kimliği gövdeden DEĞİL personelin çalışma deposundan gelir; (2) nokta listesi (`storage_area` + `vehicle`, `activeOnly`, beklenen aralık + günlük sayı + bugün ölçüldü mü); (3) üç kural aynen: **geçmişe yazılmaz** (`recordedAt` girdide yok), **uyarır engellemez**, ve **nokta bu tesise ait mi SUNUCUDA doğrulanır** (veritabanında bu kısıt yok — kontrol kapıdaysa vardır). Kural motoru hazır ve saf: `measure-rules.ts`.
  - **İki sütun → tek liste + tür süzgeci** (kullanıcı itirazı, ölçümle birebir örtüştü): 5 alan / 1 araç — iki sütun bu oranda hep dengesiz duracaktı.
  - **Okuma sayfalanmıyor ve bu KURALA UYGUN:** takvim sonsuz büyüyen bir liste değil, tavanı olan bir küme (gün × nokta × günlük ölçüm) — `CLAUDE §1`. Pencere hep 3 ay çekiliyor, 1/2/3 ay **istemcide** daraltılıyor: 550 gün nesnesi için sunucuya dönmek kazandırdığından çok bekletirdi. Tavana çarparsa (`listRange.truncated`) ekran ölçemediğini söylüyor — kesilen bir okumanın boş günleri "ölçülmemiş" diye boyanırdı, yani altyapı sınırı bir hijyen ihlali gibi görünürdü.
  - **"Hiç ölçülmedi" cümlesi düzeldi:** eskiden 200 satırlık bir taramanın dışında kalan nokta "hiç ölçülmemiş" görünüyordu (dört ay önce ölçülmüş bir dolap için YANLIŞ). Bugün "son 3 ayda ölçüm yok" deniyor — ölçtüğümüz şeyin birebir karşılığı (`CLAUDE §1`).
  - **`.next-prod/types` typecheck'ten çıkarıldı** (`apps/web/tsconfig.json`): donmuş bir prod derlemesinin ürettiği tipler `include`daydı ve silinen her rota `typecheck`i kırıyordu (bu turda kırdı). Aynı rotalar `.next/types` üzerinden zaten denetleniyor; bir snapshot'ı kaynak ağacıyla birlikte denetlemenin kazancı yok.
  - **Takvim BESLENDİ ve beslerken iki kusur ölçüldü** *(kullanıcı kararı 17.08: "takvimi besleyebilirsin")* — seri 5 günden **75 güne** çıktı (446 ölçüm). 16.08'de 22'den 5'e indirilmişti ve o gerekçe doğruydu ("uzun seri listenin uzayabileceğini göstermekten başka bir şey sınamıyor"); takvim gerekçeyi değiştirdi — beş günde görülemeyen üç hâl var: eksik gün, yarım tur, zamana yayılmış sapma. 75 gün 92 günlük pencerenin İÇİNDE bilerek: başta boş bir şerit kalıyor ve **"veri buraya kadar" sınırı ekranda görünüyor** — dolu bir takvim sınırın nerede olduğunu saklar.
  - **Kusur 1 — sapma ölçütü kendi gürültüsünde boğuluyordu.** Profillerin dalgası hedef aralığın sınırını aşıyordu (`−19,5 ± 0,8` ↔ hedef `−20…−18`) ve beş günlük seride fark edilmiyordu; 75 günde **günlerin yarısı kırmızıya döndü** (36/71). Yarısı sapma olan bir takvimde sapma bir işaret olmaktan çıkar, arka plan olur. Profiller aralığın içine çekildi; dışarı yalnız bilinçli `KAZA` çıkarıyor → **2/71**. Bu, uzun serinin gerçekten ne sınadığının kanıtı: kısa seride yanlış ayarlanmış bir eşik doğru görünüyordu.
  - **Kusur 2 — noktanın "doğum günü" yanlış ölçütle kuruluyordu.** Yalnız `created_at`e bakılıyordu; oysa **bir noktanın ölçümü varsa o gün VARDI** — tersi mantıken imkânsız. İkisi ayrışabiliyor (aktarım, seed, sonradan tanımlanmış bir dolabın geçmişe girilmiş kayıtları) ve yalnız `created_at`e bakan takvim, ölçümü olan bir günü "nokta henüz yoktu" diye sessize alıp o günün eksiklerini hiç sormuyordu. Doğum artık ikisinin ERKENİ.
  - **BEKLEYEN(19.30):** `measure-rules.ts` saf karar taşıyor (sapma ölçütü, gün hâli) ama sayfa klasöründe duruyor — `domain-core`un sözleşmesi "saf karar, **testli**" ve bu turda yeni test yazılmadı (kullanıcı kuralı 11.08). Testiyle birlikte `domain-core`un stok modülüne taşınacak; mobil ölçüm ucu aynı motoru kullanacağı için o gün kaçınılmaz olur.


