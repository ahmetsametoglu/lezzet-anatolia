/*
  GİRİŞİN ÖLÇEK DIŞI ÖLÇÜLERİ (21.312) — tasarımın temada durağı olmayan iki değeri; ekranın geri kalan her
  ölçüsü temanın durağından okunur.
*/

/** Google düğmesi (tasarım: 56) — `controlLg` (52) tek satırlık girdinin boyu. */
export const TALL_BUTTON_HEIGHT = 56;

/** Logo yükseklikten ölçülür (tasarım: 152); genişlik varlığın oranından (`logo-isaret.png`, 420×370). */
export const LOGO_HEIGHT = 152;
export const LOGO_ASPECT = 420 / 370;
