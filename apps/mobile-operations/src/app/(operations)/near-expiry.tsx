import { NearExpiryScreen } from '@/screens/warehouse/near-expiry-screen';

// D3 · YAKIN-SKT TURU — `/near-expiry`. Salt okunur; tek eylemi D4'e geçiştir.
export default function NearExpiryRoute() {
  return <NearExpiryScreen />;
}
