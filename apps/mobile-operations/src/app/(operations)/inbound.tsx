import { TransferScreen } from '@/screens/warehouse/transfer-screen';

// D5 · TRANSFER (rampada sayım) — `/inbound`. "Bana ne geliyor" listesi + kabul; sevk yolu YOK.
export default function InboundRoute() {
  return <TransferScreen />;
}
