import { SupplySuggestionScreen } from '@/screens/management/supply-suggestion-screen';

// Y4 · TEDARİK ÖNERİSİ (taslak TS) — `/supply-suggestion`. Gerekçe `complaint.tsx`te, tek yerde.
export default function SupplySuggestionRoute() {
  return <SupplySuggestionScreen />;
}
