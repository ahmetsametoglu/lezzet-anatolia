import { CourierReturnScreen } from '@/screens/warehouse/courier-return-screen';

// D6 · KURYE DÖNÜŞÜ KABULÜ — `/courier-return`. Döküm fixture, kayıt gerçek (ekran künyesi).
export default function CourierReturnRoute() {
  return <CourierReturnScreen />;
}
