import { OrderExceptionScreen } from '@/screens/management/order-exception-screen';

// Y2 · EKSİK TOPLAMA KARARI — `/order-exception`. Gerekçe ve ince-rota kuralı `complaint.tsx`te.
export default function OrderExceptionRoute() {
  return <OrderExceptionScreen />;
}
