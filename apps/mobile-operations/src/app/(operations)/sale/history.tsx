import { SaleHistoryScreen } from '@/screens/sale/sale-history-screen';

/* Son satışlar — kaydın kontrolü: ne yazıldı, kim yazdı. */
export default function SaleHistoryRoute() {
  return <SaleHistoryScreen />;
}
