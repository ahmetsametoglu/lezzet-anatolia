import { PickupScreen } from '@/screens/warehouse/pickup-screen';

// GEL-AL TESLİM (D9) — `/pickup`. İzinli müşteri hazır siparişini tezgâhtan alır: kutu okut, parayı al, teslim et.
export default function PickupRoute() {
  return <PickupScreen />;
}
