import { PrinterSetupScreen } from '@/screens/warehouse/printer-setup-screen';

// BU CİHAZ · YAZICILAR (07.12) — `/printers`. Envanter sunucudan, seçim cihazda kalır.
export default function PrintersRoute() {
  return <PrinterSetupScreen />;
}
