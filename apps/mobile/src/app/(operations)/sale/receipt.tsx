import { SaleReceiptScreen } from '@/screens/sale/sale-receipt-screen';

/* Fiş — az önce yazılan satışın sayfası; durum katalogla ortak (sale/_layout). */
export default function SaleReceiptRoute() {
  return <SaleReceiptScreen />;
}
