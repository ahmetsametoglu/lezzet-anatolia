import { SaleCartScreen } from '@/screens/sale/sale-cart-screen';

/* Sepet — son kontrol + tahsilat seçimi + yazma; durum katalogla ortak (sale/_layout). */
export default function SaleCartRoute() {
  return <SaleCartScreen />;
}
