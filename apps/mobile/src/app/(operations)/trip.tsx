import { CourierTripScreen } from '@/screens/courier/trip-screen';

/*
  Sefer künyesi (v3:1367) — yola çıkmadan önceki "ne taşıyorum" ekranı. Gövdeyi ekranın kendisi
  çizer; kurye kökünün açtığı yolu izliyor (`courier.tsx` emsali).
*/
export default function TripRoute() {
  return <CourierTripScreen />;
}
