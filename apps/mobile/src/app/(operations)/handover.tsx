import { HandoverScreen } from '@/screens/warehouse/handover-screen';

// KARGO DEVRİ (07.12) — `/handover`. Kutular taşıyıcıya veriliyor; ekran bir liste değil okutucu.
export default function HandoverRoute() {
  return <HandoverScreen />;
}
