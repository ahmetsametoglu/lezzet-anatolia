import { CourierLoadScreen } from '@/screens/courier/load-screen';

/*
  Araca yükleme (v3:1401) — kutuların araca bindiği ekran. v2'de günün rotasında tek satırlık bir
  sayaçtı; kırılımı ekranın künyesinde.
*/
export default function LoadRoute() {
  return <CourierLoadScreen />;
}
