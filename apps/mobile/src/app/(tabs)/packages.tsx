import { PackagesListScreen } from '@/screens/packages-list/packages-list-screen';

/* Rota dosyası İNCE (katalogla aynı gerekçe) — ekranın parçaları `src/screens/packages-list/`ta. */
export default function PackagesRoute() {
  return <PackagesListScreen />;
}
