import { ScreenPlaceholder } from '@/components/screen-placeholder';

/*
  MÜŞTERİ BİLDİRİMLERİ — bugün YER TUTUCU. Operasyon kabuğunun kendi bildirim ekranı ayrıdır
  (`(operations)/notifications.tsx`); müşteri listesi kendi dilimini bekliyor.
  Rota vitrinin zil düğmesi için var.
*/
export default function NotificationsRoute() {
  return <ScreenPlaceholder title="Bildirimler" />;
}
