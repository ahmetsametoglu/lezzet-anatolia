import { ScreenPlaceholder } from '@/components/screen-placeholder';

// Tek sade rota — ekranlar tasarım hattından gelecek (21.2 kapsamı bilinçli olarak ekransız).
export default function IndexScreen() {
  return <ScreenPlaceholder title="Lezzet Anatolia" />;
}
