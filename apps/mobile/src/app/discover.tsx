import { ScreenPlaceholder } from '@/components/screen-placeholder';

/*
  KEŞİF (kart turu) — bugün YER TUTUCU. Rota vitrinin kesikli davet kutusu için var: davet
  basılabilir olmalı, yoksa "ölü etkileşim" olurdu (CLAUDE §3 — statik ≠ işlevsiz).
*/
export default function DiscoverRoute() {
  return <ScreenPlaceholder title="Keşif" />;
}
